# MEMORY BANK — dash_project

**Dernière mise à jour :** 10 juillet 2026 (V21.0.56 + V21.0.57 — [COMMENT-REACTIONS] + [REACTION-PALETTE], ⚠️ **bump export v21**, non commité — validation Cowork, **commit groupé** tag V21.0.57)
**Nature :** index de mémoire. La **source canonique** reste `CLAUDE.md` (racine). Ce fichier pointe vers elle, il ne la recopie pas (anti-duplication — cf. ADR-001).

## Résumé global

Dashboard personnel auto-hébergé sur un **Zimaboard**, utilisé comme page d'accueil du navigateur : liens vers services self-hosted (catégories, statuts online/offline, favicons), mémos/tâches façon Planify (projets imbriqués, priorités, échéances, sous-tâches, récurrence), carte Leaflet des mémos géolocalisés, et partage externe par lien à jeton + PIN (invités identifiés, droits, commentaires). Recherche instantanée, horloge multi-fuseaux, météo, thème clair/sombre.

## Stack technique (réelle — PAS Django/Dash)

- **Backend :** Flask + SQLite **sans ORM, sans blueprint** — tout dans `app.py`. Migration de schéma additive dans `init_db()`.
- **Frontend :** **JS vanilla**, pas de framework, **pas de build**. Un fichier par page : `templates/index.html` (propriétaire) et `templates/share.html` (invité). Helpers purs mutualisés dans `templates/partials/_shared.js.html` (inclus au rendu Jinja — ADR-001).
- **Données :** SQLite (`data/dashboard.db`), uploads (`data/uploads/`), en volume Docker. Export/import JSON versionné (v1->v21), rétro-compatible et non destructif.
- **Déploiement :** `docker compose up -d --build` (gunicorn, port 8099), derrière **Caddy + Authelia** en prod. Pas d'auth dans l'app.
- **Dépendances front auto-hébergées** dans `static/` (Quill, GSAP, Leaflet) — pas de CDN au runtime.

## Sources canoniques (à lire avant toute modif importante)

- **`CLAUDE.md`** (racine) — architecture, **8 invariants**, format d'export, historique détaillé, procédure de test. **Référence.**
- **`docs/adr/`** — décisions d'archi. ADR-001 = mutualisation des helpers front owner/invité (Option C, partial Jinja).
- **`IDEAS.md`** — backlog (quick wins, confort/UX, dette technique, ambitieux), tags `[NOM]` repris par Claude Code.
- **`patterns.md`** (ici) — conventions transverses + pointeurs.
- **`dash-cicd-setup.md`** (ici, reference) — infra CI/CD : runner self-hébergé `zimaboard` (systemd, user `casaos`, `~/actions-runner`), dossier de déploiement `/mnt/StorageNaN/home_casaos/Documents/projects/dash_project`, workflows `ci.yml`/`deploy.yml`, process release (tag `VX.Y.Z` = tête de `REALISATION.md`) + rollback + gotcha « tag sur commit sans workflows ».

## État actuel (mise à jour 9 juillet 2026)

- **CI/CD EN PLACE** (⭐ nouveau, détail : [[dash-cicd-setup]] + `README.md` § Release) : runner GitHub Actions self-hébergé `zimaboard` (systemd, user `casaos`, `~/actions-runner`) + workflows `ci.yml` (push main/PR → cloud : py_compile + check JS templates + docker build) et `deploy.yml` (push tag `V*.*.*` → runner zimaboard : checkout tag dans le dossier persistant `/mnt/StorageNaN/home_casaos/Documents/projects/dash_project` + `docker compose up -d --build` + smoke test `/api/version`==tag). **Process release** : journaliser `[VX.Y.Z]` en tête de `REALISATION.md` + commit/push main, puis `git tag VX.Y.Z && git push origin VX.Y.Z` = déploiement auto. Le tag DOIT == entrée de tête de `REALISATION.md` (sinon smoke test rouge). **Fin du déploiement manuel** (il reste déclenché par le push d'un tag, jamais sur simple push main). Prod actuellement sur `V20.6.42`.
- **Version courante : V20.6.42** — format d'export toujours **v20**. Derniers lots (tous poussés/déployés) : `[MAP-BTN-COUNT]` V20.6.42 (bouton Carte du hub invité `hub.html` : libellé « 🗺 Carte (N) » + masqué à N=0, helper `syncMapBtn()` appelé à chaque `render()`, source `hubMapPoints().length` ; `share.html` déjà conforme, `index.html` owner inchangé), `[CI-CD]` V20.6.40 (workflows ci/deploy — voir puce ci-dessus) + V20.6.41 (doc mémoire [[dash-cicd-setup]]). Avant, lots UX carte (partial `_shared.js.html` → parité owner/share/hub) : `[MAP-MARKER-DAY-SYNC]` V20.6.39 (clic marqueur Leaflet = sélectionne le jour du point via le MÊME chemin que le chip « Jour N » — `marker.on('click', () => applyDay(p.due_date || null))` ; sans date/pin projet → « Tous » ; popup conservée) et `[MAP-CLOSE-UX]` V20.6.37 (en-tête `.map-head` sticky top:0 + bouton ✕ `.map-x` aria Fermer, visible desktop aussi ; footer carte non-flottant en mobile `position:static` — les AUTRES pop-ins gardent leur pied sticky [DIALOG-STICKY-ACTIONS]) **puis** V20.6.38 (revert : retrait du bouton flottant « ↑ » `#map-totop` ajouté en .37 — le ✕ suffit). Avant : `[VERSION-VISIBLE]` V20.6.36 (version de build visible — `_build_version()` parse `REALISATION.md` → `BUILD_VERSION`, footer owner version complète, route `GET /api/version` ; share/hub gardent `v20`) et `[MAP-DAYCHIP-ALL]` V20.6.35 (chip « 🌍 Tous » en tête de la frise ; `applyDay(d)` = chemin unique de (dé)sélection de jour). Encore avant : hotfixes mobile carte V20.6.32→34. Détail : `REALISATION.md` § V20. Déploiement désormais par tag via le pipeline CI/CD (voir puce en tête) ; après déploiement, hard refresh navigateur (HTML autonome caché).
- **Avant ces hotfixes (7 juillet, commités/poussés)** : `[MEMO-TABLES]` V20.5.30 (tableaux dans l'éditeur Quill des mémos, module `quill-table-better` 1.2.3 vendorisé, helper `applyTableBetter()` du partial, 3 pages, dégradation propre) et `[DIALOG-STICKY-ACTIONS]` V20.6.31 (pied des pop-ins en `position:sticky`, 3 pages, + fix du sélecteur `order:4` mort de la carte share/hub). `origin/main` avant ce push = `b1c56a8`.
- **Série carte v20 poussée** (détail normatif : `REALISATION.md` § V20 + `CLAUDE.md` invariant 1) :
  - `[MAP-TIMELINE]` `2089bc8` — **bump export v20**, `projects.is_trip` héritable (NULL/1/0, résolution au plus proche ancêtre, `_resolve_trip` serveur / `resolveTripLocal` partial, invités = `trip` résolu lecture seule) ; frise chronologique des mémos datés + curseur « aujourd'hui » + tracé itinéraire pointillé **réservé aux voyages**. Spec `docs/specs/MAP-TIMELINE-trip-timeline-and-route.md`.
  - `[MAP-TRIP-POLISH]` `3219a66` — marqueurs-vignettes photo numérotés ①② chronologiques (voyages), chips « Jour N », miniatures dans la frise ; **+ fix bloquant « carte noire »** (tuiles + `setView` AVANT tout vecteur, `drawRoute` idempotent, démontage try/catch — voir gotcha `patterns.md`).
  - `[MAP-DAYCHIP-ACTIVE]` + `[MAP-TIMELINE-MOBILE-ORDER]` `e5e7158` — chip « Jour N » actif (un seul, re-clic désélectionne) ; mobile : ordre carte→frise→liste (row aplatie `display:contents !important`, liste protégée `flex:0 0 auto` + min-height du collapse flex du dialog).
  - `[MAP-GROUPS-MENU]` `8e8709f` — liste de points épurée (plus de coches permanentes) + menu « Groupes ▾ » créer/modifier/copier/supprimer ; **gate share resserré à `canEditNow()`** (lien can_edit ET invité approuvé — un anonyme ne voit plus les outils d'édition).
- **Avant la carte (même journée)** : `[AGENDA]`/`[AGENDA-PROJECT]`/`[AGENDA-SMART-MONTH]` (vue calendrier 3 pages, D&D, filtre projet, mois d'atterrissage utile), `[GUEST-RESEND-LINK]`, `[GUEST-SUBPROJECT]`, et la vague V19.8→V19.13 (hub `/share/hub/`, cookie session HttpOnly, `[GUEST-EDIT]`, `[SETTINGS-TABS]`, `[SIDEBAR-TREE]`, `[AUTO-REFRESH]`). Tout est sur `origin/main`, tout validé Cowork/Chrome.
- **Hub invité** (inchangé) : routes sous `/share/hub/<hub_token>`, cookie HttpOnly `dashhubsession` 180 j (lecture `/data` seulement), écritures par jetons de dossier revalidées serveur ; `_data_version` exclut les volatils (`last_seen_at`/`session_token`/`activity_seen_at`).
- **⭐ EN COURS (non commité — validation Cowork) : `[COMMENT-REACTIONS]` réactions emoji sur les commentaires — V21.0.56** (spec `docs/specs/COMMENT-REACTIONS-emoji-votes.md`). ⚠️ **BUMP EXPORT → v21** : `APP_VERSION="21"`, entrée invariant 1 ajoutée à `CLAUDE.md`, § Versionnage X=21. Palette FIXE whitelist serveur `REACTION_EMOJIS = 👍 👎 ❤️ 😂 😮 🎉` (hors palette → 400), **toggle** par (commentaire, emoji, votant) via table additive `comment_reactions (comment_id, emoji, voter, created_at, UNIQUE(comment_id, emoji, voter))` — `CREATE TABLE IF NOT EXISTS`, non destructive. Votant = pattern `created_by` (`''`=owner résolu `_owner_name`, sinon « Nom <email> », match par e-mail robuste au renommage). Réagir = **invité APPROUVÉ requis, `can_edit` NON** (décision figée) ; réactions **hors** badge 🔔. Routes owner `POST /api/comments/<id>/react` + invité `POST /share/<token>/comment/<id>/react` (scope revalidé, bypass `/share/*`, hub route vers le share du mémo). Chaque commentaire (`GET …/comments`, `share_data`, hub `/data`) gagne `reactions:[{emoji,count,mine,voters}]` (`voters`=nom seul, e-mail owner-only). Helper partagé `reactionRow` (chips accent + mini-palette 😊+ façon card-menu, clic extérieur, pas de GSAP) sur les 3 pages. `_data_version` étendu. Purge en cascade (delete_comment + `_purge_memo_row` avant les commentaires). **Export/import v21** : `reactions` (voter BRUT) nichées dans `comments` ; import `INSERT OR IGNORE` (ré-import = 0 doublon), commentaire résolu par (memo_uid, created_at, author) ; compat v1→v20 (absent = rien). **Backend testé** (Flask test client, copie de base — migration, toggle/multi/mine, `_data_version`, 400/404, 403 anonyme, nom seul share, purge cascade, export v21 + ré-import 0 doublon, import v20 0 fantôme) + `py_compile` + `node --check` 3 pages/partial + **rebuild local OK** (`/api/version` export=21, table réelle). **[REACTION-PALETTE] V21.0.57 greffé PAR-DESSUS** (même v21, PAS de bump v22) : palette de réactions passée de constante à **config owner** (`app_state.reaction_emojis`, seed = 6 base, ajout/retrait dans Paramètres → onglet « 😊 Réactions ») ; validation stricte d'un **seul grapheme emoji** `_clean_reaction_emoji` (Python pur, plages Unicode + accessoires ZWJ/VS/teinte/keycap/régionaux + garde ≤ 10 cp, zéro lib/CDN, texte/HTML/2-emojis → 400) ; palette exposée (`/api/settings`, `share_data`, hub `/data`) et **exportée si custom** ; réactions à l'import validées en FORMAT (préserve l'historique des emojis retirés) ; `reactionRow` du partial lit `REACTION_EMOJIS` **mutable** écrasé par la palette serveur au chargement (3 pages). Testé (25 assertions, copie de base : validateur, react base/custom, ajouts invalides 400, retrait → 400 + historique préservé, export/round-trip 0 doublon, import v20 base par défaut). **Reste** : sonde bypass Authelia `/share/.../react` à confirmer sur le Zimaboard (Cowork), validation Chrome Cowork (owner + invité approuvé + anonyme + palette custom), puis **UN commit GROUPÉ** [V21.0.56 + V21.0.57] + tag **V21.0.57**. Détail : `REALISATION.md` [V21.0.56/57] + `CLAUDE.md` invariant 1 (v21).
- **`[IMPORT-PREVIEW]` dry-run + résolution de conflits — V20.14.55 (commité 9d1c4ad, tag V20.14.55, poussé)** (spec D1-D4, export v20). `POST /api/import?dry_run=1` = analyse lecture pure (pas de bump `_data_version`) → rapport arbre projets (new/merge) + mémos (new/skip/conflict[active|trashed] par uid+signature). `POST /api/import` accepte `resolutions {uid: overwrite|duplicate|skip}` (overwrite restaure un mémo corbeille dans le projet visé ; duplicate = uid neuf). **Import global Paramètres + POST sans param = INCHANGÉS (V20.12)**. Pop-in `#import-preview-dialog` (arbre coloré tokens 2 thèmes, légende, bilan, « Importer N »). Déployé.
- **`[VOTE-DECISION]` V1 + [VOTE-MULTI]/[VOTE-RESET]/[VOTE-EXCLUDE] + vague votes — déployés** (specs `docs/specs/VOTE-*`). Dossier basculable en mode vote, mémos directs = options, voix `(personne, dossier)` (single/multi), état clos dérivé + gel paresseux, deadline Europe/Paris (`tzdata`), routes owner + invité `POST /share/<t>/memo/<id>/vote` (approuvé sans `can_edit`). **Export voix NON exportées** (V1/V2 → v20). Puis `[VOTE-GROUPS]` (votes nommés multiples, tables `votes`/`vote_options`), `[VOTE-MAP]` (vote sur la carte), `[VOTE-V1.1]` (event agenda du gagnant à la clôture). Tous commités/taggés/poussés. Détail : `REALISATION.md` § V20 + `CLAUDE.md` § historique.
- **`[BOARD-SUBHIDE]` masquer/afficher mémos sous-projets — V20.13.54 (commité b645b19, tag V20.13.54, poussé)** (frontend PUR 3 pages, export v20). Helper partagé `subHideBar(cfg)` (`_shared.js.html`) : chips masquer/afficher par sous-projet présent + « (ce projet) » + badge « n masqués · tout afficher », sous les tuiles/barre d'ajout. **Affichage SEUL** — le masquage filtre le rendu des cards, PAS les tuiles/compteurs ni la carte (`boardMapPoints`/`shareMapPoints`/`hubMapPoints` intouchés). Persisté `boardSubHide:<id>` / `shareBoardSubHide:<TOKEN>:<PFILTER>` / `hubBoardSubHide:<FOCUS>` (clés distinctes du focus carte), orphelins ignorés. Validé Cowork + déployé.
- **`[EXPORT-SUBTREE]` export + import ciblé d'un dossier — V20.12.52+53 (commité dc64c78/601fe3d, tags V20.12.52/53, poussés)** (owner-only, PAS de nouveau format, export v20). **Export** `GET /api/projects/<id>/export` = `_build_export(db, root_id)` filtré au sous-arbre (mêmes exclusions que le global, ni votes/voix ni binaires) → JSON avalé par l'import existant. **Import ciblé** `/api/import?target_parent_id=<id>` (« Importer ici ») : rattache **seulement les RACINES NOUVELLES** du fichier (matché par NOM) au dossier cible, **jamais un existant** (invariant 2), cible validée + anti-cycle (400 si inconnue ou dans le fichier) ; absent = comportement d'avant ; import global Paramètres inchangé. Boutons ⬇ Exporter / ⬆ Importer ici (menu ⋯ pop-in projet). Déployé.
- **`[VOTE-V1.1]` event agenda du gagnant + fix menu Groupes — V20.11.50+51 (commité 2c8e765, tag V20.11.51, poussé)** (spec VOTE-DECISION §8, export v20). Colonne `votes.event_date` (votes nommés) ; à la clôture (`_freeze_named_vote` → manuelle/invité/deadline) le gagnant UNIQUE est planifié (`due_date`/`due_time` écrasés) ; ex æquo = pas de write ; reset/reopen conservent la date. UI champ « 📅 Événement » (création+gestion 3 pages) + info confirm clôture. Inclut le fix [MAP-GROUPS-MENU-DISMISS] (fermeture menu Groupes carte au clic extérieur + Échap). Déployé.
- **`[VOTE-MAP]` le vote sur la carte — V20.10.49 (commité b2dcd4b, tag V20.10.49, poussé)** (frontend PUR, `runMapDialog` du partial + glue index/share/hub, **zéro route, export v20**). Chips de scrutin dans la carte (`🗳 (dossier)` + nommés), pastilles « n voix » + ✓ sur options (clic = voter en place), repères gris pour les non-options, coche « 🗳 Voix » persistée par projet/page, vue gagnant (🏆 + anneau + fitBounds). Chaque page passe `cfg.voteData` (scrutins + `onVote`) — le partial ne connaît pas le réseau (invariant 5). Déployé. Détail : `REALISATION.md` [V20.10.49].
- **`[VOTE-GROUPS]` votes nommés multiples — V20.9.48 (commité 7888c5d, tag V20.9.48, poussé)** (spec `docs/specs/VOTE-GROUPS-named-votes.md`). Tables `votes`/`vote_options` + `memo_votes.vote_id` (NULL = vote-dossier V1/V2 inchangé) + `projects.vote_create` (permission héritable) ; **2 index partiels** par `vote_id` (D1 : mémo dans plusieurs votes). Routes owner `/api/votes*` + invité `/share/<t>/votes*` (créateur gère, 403 générique, porteur revalidé). UI 3 pages (chips scrutin, création par sélection, pop-in gestion). Export inchangé (v20). Backend testé (11 points §9) + déployé. ⚠️ sonde bypass Authelia des routes `/share/.../votes*` à confirmer sur le Zimaboard.
- **Backlog** : `[VOTE-MATCH]` V3 « vote live » (non planifié, brouillon `docs/specs/VOTE-MATCH-v3-draft.md`, décisions RGPD consignées). Aussi : `[NOTIFY-EMAIL]` (plomberie SMTP prête), `[TAG-NAV]`, `[IMAGE-TRASH]`, `[IMAGE-LAZY-LOAD]`, couleur de groupe (écartée de [MAP-GROUPS-MENU]), confort/UX — voir `IDEAS.md`.


## Instructions pour Claude

- Avant une modif importante : lire **`CLAUDE.md`** (surtout les 8 invariants + le format d'export), puis `docs/adr/`, puis ce fichier et `patterns.md` si pertinent.
- Ne jamais casser la **compat ascendante de l'export/import** ni les **invariants** de `CLAUDE.md`.
- Après une grosse feature ou décision d'archi : proposer une mise à jour de cette Memory Bank **et** de `CLAUDE.md`, sans dupliquer le contenu entre les deux.
- **Répartition des rôles de test (flux convenu, ne pas inverser)** : Claude Code fait les tests **automatisés/sans navigateur** (`py_compile`, `node --check` des templates, scénarios Flask test client sur une **copie** de la base) + `docker compose up -d --build` + **handoff**. La **validation Chrome au doigt (owner + invité)** est faite par **l'agent Cowork**, PAS par Claude Code — ne pas lancer d'automatisation navigateur (`mcp__claude-in-chrome__*`) pour valider une feature soi-même, sauf demande explicite de Fabien. Livrable CC type = code testé (backend) + rebuild + handoff, puis Cowork/Fabien valident en live. **Toujours purger le décor de test créé** (⚠️ `DELETE /api/projects/<id>` détache les mémos vers l'Inbox sans les supprimer → purger aussi les mémos via corbeille + `/api/trash/<id>`).

{
  "lot": "COMMENT-REACTIONS + REACTION-PALETTE",
  "version": "V21.0.57",
  "date": "2026-07-10",
  "status": "Deux lots v21 empilés dans le working tree, NON commités : [COMMENT-REACTIONS] V21.0.56 (réactions emoji) + [REACTION-PALETTE] V21.0.57 (palette configurable) greffé par-dessus. ⚠️ BUMP EXPORT → v21 (APP_VERSION=\"21\", entrée invariant 1 CLAUDE.md). La palette rejoint le format v21 (jamais déployé) → PAS de bump v22, export reste version:21. Code COMPLET (backend + 3 pages + partial), testé localement (25 assertions Flask test client sur copie de base + py_compile + node --check des 3 pages/partial), rebuild docker OK. SÉQUENCE : (1) validation Chrome au doigt (owner + invité approuvé + anonyme lecture + palette custom, 3 pages) par l'AGENT COWORK — PAS Claude Code, (2) UN SEUL commit GROUPÉ couvrant les deux lots [V21.0.56 + V21.0.57], (3) git tag V21.0.57 && git push origin V21.0.57 -> déploiement auto. ⚠️ TAG = V21.0.57 (tête de REALISATION.md, BUILD_VERSION=21.0.57 — smoke test deploy compare tag==/api/version). Aucun commit ni push sans feu vert explicite.",
  "app_url": "http://localhost:8099/",
  "export_decision": "BUMP X : format d'export v21. Chaque commentaire de `comments` gagne `reactions:[{emoji,voter(BRUT),created_at}]`. [REACTION-PALETTE] ajoute `reaction_emojis` (top-level) EXPORTÉ SEULEMENT si l'owner a configuré une palette custom (clé `app_state.reaction_emojis` présente) → export par défaut = pas le champ. Import : réactions `INSERT OR IGNORE` (0 doublon) ; palette restaurée si le fichier en fournit une ET aucune config locale (non destructif) ; réactions validées en FORMAT (`_clean_reaction_emoji`, pas la palette) → préserve les emojis retirés. Compat v1→v20 (champs absents = rien = rendu identique). `comment_seen` reste non exporté.",
  "resume": "Réactions emoji sur les commentaires (owner + invités approuvés). Palette CONFIGURABLE owner : `_reaction_palette(db)` (config `app_state.reaction_emojis`, seed = 6 base 👍👎❤️😂😮🎉, ajout/retrait dans Paramètres → onglet « 😊 Réactions »). Validation d'un AJOUT = un seul grapheme emoji `_clean_reaction_emoji` (Python pur, plages Unicode + accessoires ZWJ/VS/teinte/keycap/régionaux + garde ≤ 10 cp, zéro lib/CDN ; texte/HTML/2-emojis → 400). Toggle par (commentaire, emoji, votant) via UNIQUE(...). Votant = pattern created_by (''=owner résolu _owner_name, sinon « Nom <email> », match par e-mail). Réagir = invité APPROUVÉ requis, can_edit NON (figé). Réactions hors badge 🔔.",
  "fichiers_modifies": [
    "app.py [V21.0.56] : APP_VERSION=\"21\" ; migration comment_reactions ; helpers réactions ; _comment_dict/react routes owner+invité ; delete_comment + _purge_memo_row purge ; _data_version + comment_reactions ; export v21 (reactions brutes) ; import INSERT OR IGNORE",
    "app.py [V21.0.57] : REACTION_EMOJIS = SEED ; _is_emoji_core + _clean_reaction_emoji (validateur grapheme) + _reaction_palette(db) ; _valid_reaction_emoji(value, palette) (2e arg) ; _aggregate_reactions(..., palette) ordre palette + extras appended ; _comment_dict(..., palette) ; les 3 appels _comment_dict + _react_result + 2 routes react passent _reaction_palette(db) ; GET /api/settings expose reaction_emojis ; PUT /api/settings valide+enregistre reaction_emojis (400 emoji invalide, liste vide OK) ; share_data + hub_data exposent reaction_emojis ; import réactions via _clean_reaction_emoji (format, pas palette) ; import restaure palette custom (si absente localement) ; export top-level reaction_emojis si custom",
    "templates/partials/_shared.js.html : const→let REACTION_EMOJIS (mutable) ; reactionRow guard `canReact && REACTION_EMOJIS.length`",
    "templates/index.html : REACTION_EMOJIS = s.reaction_emojis au chargement settings ; onglet « 😊 Réactions » (markup) + SETTINGS_TABS + renderReactionManage() (chips ✕-retirables) + addReactionEmoji() + saveReactionPalette() (PUT auto, ✓ flash)",
    "templates/share.html : REACTION_EMOJIS = DATA.reaction_emojis au chargement ; reactionRow des commentaires (V21.0.56)",
    "templates/hub.html : REACTION_EMOJIS = DATA.reaction_emojis (loadDash + reload) ; reactionRow des commentaires (V21.0.56)",
    "docs/specs/COMMENT-REACTIONS-emoji-votes.md (spec verrouillée) ; REALISATION.md [V21.0.56/57] ; CLAUDE.md (invariant 1 v21 étendu palette + § historique + § Versionnage X=21) ; IDEAS.md ([REACTION-PALETTE] fait) ; .claude/memory/MEMORY.md"
  ],
  "tests_valides_localement": [
    "python3 -m py_compile app.py OK ; node --check des 3 blocs JS (index/share/hub) + _shared.js.html OK (extraction blocs façon ci.yml).",
    "Backend (Flask test client, copie de /app/data/dashboard.db dans le conteneur, PYTHONPATH=/app) — 25 assertions ALL PASS : validateur (emoji valide/2-emojis/texte/HTML/drapeau) ; défaut = 6 base ; react base 200 + chip mine ; react hors palette 🔥 → 400 → ajout 🔥 → react 🔥 200 ; ajouts invalides (texte, 2 emojis, <script>, espaces) → 400 ; retrait 🔥 → react 🔥 400 MAIS réaction historique toujours affichée (aggregate extras) ; export v21 palette custom + réactions brutes ; round-trip import → palette restaurée + ré-import 0 doublon ; import v20 sans palette → 200 + base par défaut.",
    "Rebuild docker local OK : /api/version export=21, /api/settings expose reaction_emojis (6 base). BUILD_VERSION passe à 21.0.57 après le rebuild post-journalisation."
  ],
  "a_verifier_par_fabien_en_live": [
    "OWNER (localhost:8099) : Paramètres → onglet « 😊 Réactions » → coller/taper un emoji + ＋ Ajouter (chip apparaît, sauvegarde auto ✓) ; ✕ retire ; saisie texte/2-emojis rejetée (notify). Sous un commentaire : bouton 😊+ propose la palette configurée (base + ajouts) ; réagir/retirer live. Retirer un emoji de la palette → il n'est plus proposable, mais les réactions déjà posées restent visibles.",
    "INVITÉ (partage + hub) : invité APPROUVÉ réagit avec la palette configurée (y compris un emoji custom ajouté par l'owner) même sur un lien LECTURE SEULE ; anonyme voit les chips en lecture, PAS le bouton palette ; nom seul dans les votants, jamais l'e-mail.",
    "SONDE BYPASS AUTHELIA (nouvelle route publique de V21.0.56, règle CLAUDE.md) — à confirmer sur le ZIMABOARD (pas testable en local sans Authelia) : fetch('/share/<token>/comment/<id>/react', {method:'POST', credentials:'omit', redirect:'manual'}) doit ATTEINDRE l'app (réponse 'basic', ex. 403/404/400) et NON être redirigée vers Authelia (opaqueredirect/302). Route sous /share/* -> structurellement identique aux routes commentaire/vote/image existantes."
  ],
  "notes": [
    "Décision figée : réagir ouvert à tout invité APPROUVÉ, can_edit NON requis. Config de palette OWNER-ONLY (PUT /api/settings) ; invités consomment.",
    "Deux validateurs distincts : _clean_reaction_emoji (FORMAT, un grapheme — ajout palette + import) vs _valid_reaction_emoji(value, palette) (MEMBERSHIP — routes react). Import utilise le FORMAT (non destructif : préserve les réactions d'un emoji retiré de la palette).",
    "_data_version couvre déjà app_state (ligne unique SELECT key,value ... WHERE key != activity_seen_at) → toute modif de reaction_emojis rafraîchit les clients sans code en plus.",
    "Palette vide autorisée (owner retire tout) → aucune réaction possible, bouton 😊+ masqué (guard REACTION_EMOJIS.length).",
    "Pas encore de commit/tag. Après validation Cowork : journalisation déjà faite (tête de REALISATION.md = V21.0.57) -> UN commit groupé [V21.0.56 + V21.0.57] + git tag V21.0.57 && git push origin V21.0.57 déclenche le déploiement (pipeline CI/CD [CI-CD])."
  ]
}

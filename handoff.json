{
  "lot": "GUEST-SETTINGS (+ DANGER-SOFT dans le même wagon)",
  "version": "V27.20.182",
  "date": "2026-07-31",
  "status": "Engrenage « Paramètres » dans l'en-tête des pages invitées (hub + share) : un seul composant, deux présentations (dialog centré ≥900 px, bottom-sheet ≤900 px / pointeur grossier). Sections Mon accès (renvoi du lien, route EXISTANTE, anti double envoi, toast avec e-mail masqué), Affichage (thème Auto/Clair/Sombre) et ⭐ Favoris (hub). La lune de l'en-tête et la ligne « Recevoir mon lien » de la sidebar disparaissent. Zéro route nouvelle, export 27 inchangé, aucune migration. Rebuild local OK (/api/version = 27.20.182), NI COMMIT NI PUSH — wagon du train n°2 avec [DANGER-SOFT] V27.19.181.",
  "app_url": "http://localhost:8099/",
  "volets": {
    "1_engrenage": "Icône Tabler `settings` via hdrIconAt (partial), posée AU BOOT en haut à droite du <header> du hub et du share, au gabarit de bouton LOCAL de chaque page (classe `btn` côté hub, `icon` côté share — invariant 9), monochrome au repos, classe `.on` tant que la pop-in est ouverte. #theme-btn (la lune) SUPPRIMÉ des deux pages ; la ligne « 📧 Recevoir mon lien par e-mail » RETIRÉE de la sidebar du hub.",
    "2_popin": "<dialog> natif. ≥900 px : centré, min(420px,94vw), max-height 88vh. ≤900 px OU (pointer:coarse) : BOTTOM-SHEET — inset auto 0 0 0, largeur pleine, coins hauts 16px, poignée .gs-handle, corps scrollable, max-height 82vh, entrée `gs-up` sous prefers-reduced-motion:no-preference. Voile ou « Fermer » (Luciole neutre) pour sortir.",
    "3_sections": "MON ACCÈS (hub seulement — la route est propre au hub) : POST /share/hub/<token>/send-link EXISTANTE, destinataire forcé serveur, bouton désactivé pendant ET après un envoi réussi (pas d'envoi répété), toast avec e-mail MASQUÉ. AFFICHAGE : chips Auto / Clair / Sombre ; « Auto » suit prefers-color-scheme et se re-résout au basculement OS ; préférence dans `themePref`, valeur RÉSOLUE toujours écrite dans `theme` par setTheme → script anti-FOUC du <head> INCHANGÉ ; migration douce (l'ancien choix `theme` est adopté la première fois). ⭐ FAVORIS (hub) : compteur + « Vider les favoris » en .danger-soft derrière confirmPopin ; réordonnancement annoncé en V2.",
    "4_serveur": "SEULE touche backend : la réponse de la route EXISTANTE send-link gagne `sent_to` = adresse MASQUÉE, via le nouvel helper _mask_email() (fabien.uhart@gmail.com → f•••@g•••.com). C'est un accusé de réception, pas une donnée : l'adresse complète ne circule pas. Aucune route, aucun champ exporté, aucune migration."
  },
  "pieges_trouves_au_smoke": [
    "Chaque page anime l'entrée de SES pop-ins (dialog[open]{animation:dlg-in} = scale(0.97) + translateY). Sur une feuille collée en bas, ce transform la rétrécissait de 3 % et la faisait dépasser sous le pli (mesuré : largeur 1852 au lieu de 1909, 9 px sous l'écran). Le bloc mobile neutralise animation + transform avant de poser gs-up.",
    "JINJA : la chaîne CSS contenait `…coarse){#guest-settings-dialog…` → le `{#` ouvrait un COMMENTAIRE Jinja dans le partial (500 au rendu de toutes les pages). La chaîne est coupée entre `{` et `#` (rendu identique). À retenir pour tout futur CSS écrit dans le partial."
  ],
  "fichiers_modifies": [
    "templates/partials/_shared.js.html : themePref/resolveThemePref/applyThemePref/initThemePref ; _guestSettingsStyle() ; guestGearButton(cls,onClick) ; openGuestSettings(cfg).",
    "templates/hub.html : lune retirée du header ; initThemePref() + engrenage posé au boot ; hubSettingsCfg() (e-mail + favoris) ; ligne e-mail retirée de la sidebar.",
    "templates/share.html : lune retirée du header ; CSS header .gs-gear ; initThemePref() + engrenage posé au boot (section Affichage seule).",
    "app.py : helper _mask_email() + `sent_to` dans la réponse de hub_send_link (route existante).",
    "REALISATION.md [V27.20.182], IDEAS.md (✅ [GUEST-SETTINGS], ⏳ [GUEST-SETTINGS V2])."
  ],
  "tests_valides_localement": [
    "python3 -m py_compile OK (+ _mask_email vérifié sur 4 entrées : adresse normale, courte, sans @, sans TLD) ; node --check 4 surfaces OK ; rebuild OK ; GET / = 200 ; /api/version = 27.20.182.",
    "SMOKE HUB : engrenage `btn gs-gear` SVG dans le <header>, lune absente, plus de ligne e-mail dans la sidebar ; pop-in ouverte avec les 3 sections (Mon accès / Affichage / ⭐ Favoris — « 2 dossiers épinglés »), chips Auto*/Clair/Sombre, pied `mab-btn luc-neutral | Fermer`, engrenage en .on ; bascule Clair → themePref=light, theme=light, data-theme=light, chip pressée mise à jour ; retour Auto OK.",
    "SMOKE SHARE : engrenage `icon gs-gear` SVG, lune absente, UNE seule section « Affichage », pop-in 407×185 centrée.",
    "BOTTOM-SHEET (simulé en rejouant les déclarations de la media query) : x=0, largeur 1909 = largeur client, collé en bas (gap 0), transform:none, coins 16px/0, poignée visible, corps overflow-y:auto, max-height 82vh. Règles @media (max-width:900px),(pointer:coarse) présentes au CSSOM.",
    "Aucune donnée touchée : les favoris réels de Fabien n'ont PAS été vidés, aucun e-mail n'a été envoyé (le bouton n'a pas été déclenché)."
  ],
  "non_teste": [
    "Le bottom-sheet au VRAI viewport ≤ 900 px : l'outil de redimensionnement de fenêtre ne change pas le viewport de l'onglet dans ce harnais — géométrie validée par simulation + CSSOM. À sentir sur iPhone.",
    "L'ENVOI réel de l'e-mail (SMTP) : non déclenché pour ne pas spammer la boîte de Fabien. Le chemin réseau est celui de [GUEST-RESEND-LINK], inchangé ; seule la réponse gagne `sent_to`.",
    "Le rate-limit 429 (2 envois/h) : non provoqué ; le message serveur est relayé tel quel dans le toast."
  ],
  "a_verifier_par_fabien_en_live": [
    "Engrenage présent et lisible sur les 2 pages invitées, 2 thèmes ; pop-in desktop propre.",
    "Envoi du lien : toast « Envoyé à f•••@… », bouton qui ne renvoie PAS deux fois.",
    "Thème : Auto suit l'appareil, Clair/Sombre persistent au rechargement ; aucun flash au chargement.",
    "« Vider les favoris » : confirm puis sidebar mise à jour.",
    "Disparition confirmée de la lune et de la ligne « Recevoir mon lien » de la sidebar.",
    "iPhone : la feuille monte du bas, poignée visible, corps scrollable, tap sur le voile pour fermer."
  ],
  "notes": [
    "Invariants : 5 (aucune route publique nouvelle ; l'action e-mail reste la route hub existante, destinataire forcé serveur), 6 (SVG inline, zéro CDN), 8 (pas de GSAP sur le <dialog> — entrée CSS), 9 (gabarits de boutons locaux, chips .prio-btn, .danger-soft).",
    "État du dépôt : deux lots NON commités dans ce wagon — [DANGER-SOFT] V27.19.181 et [GUEST-SETTINGS] V27.20.182. Dernier tag déployé : V27.18.180.",
    "⚠️ RAPPEL : sauvegarde export au format v27 toujours à refaire côté Fabien."
  ]
}

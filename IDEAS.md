# Idées d'évolution

Backlog d'idées pour le dashboard, par ordre approximatif d'intérêt/effort.

**Versionnage** : chaque tâche est planifiée en ciblant la granularité `VX.Y` (ex. « → prévu pour **V19.1** »). X = version du format d'export, Y = mineure (feature/lot). Les réalisations livrées sont consignées dans `REALISATION.md` avec un tag `[VX.Y.Z]` (Z = compteur de build continu). Voir `CLAUDE.md` § Versionnage.

**Ce fichier = backlog (idées à faire).** Ce qui est **livré** est journalisé dans [`REALISATION.md`](REALISATION.md) (par tag `VX.Y.Z`) ; les specs verrouillées dans `docs/specs/`.

### En cours / prochains lots (à jour 10 juil. 2026)

- ✅ **[FAVORITES]** — favoris owner-only en base, **non exporté** — **LIVRÉ [V21.1.58→60]** : V1 (favoris projets, section ★ desktop) + V1.1 (modèle typé `{kind:'project'|'view'}`, épinglage des vues Plan/Agenda/Mémos, entrée « ⭐ Favoris » mobile ouvrant la liste dans le main) + V1.2 (réordonnancement drag-and-drop, Pointer Events, desk souris + mobile appui long). Détail : `REALISATION.md`.
- ✅ **[HOME-RESET]** + **[LINK-ADD-RELOCATE]** — **LIVRÉ [V21.1.61→62]** : clic « Dashboard » = accueil (reset vue/recherche/scroll) ; « + Ajouter » (lien) déplacé du header vers la vue Liens, puis **regroupé avec « + Ajouter une catégorie »** dans la page Liens [V21.1.62]. `REALISATION.md`.
- ✅ **Cluster [NAV & MOBILE]** — **SOLDÉ** : `[HEADER-ADD-CONTEXT]` (« + » contextuel par bloc en mobile ≤ 900 px : en-tête « 🔗 Liens » + mini-menu Lien/Catégorie, « + » dans le summary Mémos → focus `#memo-quick`) **LIVRÉ [V23.3.91]** (frontend pur `index.html`, owner, export inchangé v23). Spec `docs/specs/HEADER-ADD-CONTEXT-contextual-add-mobile.md`, détail `REALISATION.md`.
- ✅ **[POMODORO]** — **LIVRÉ [V21.1.67→68]** : widget flottant minuteur/pomodoro/chrono (partial, 3 pages) ; timestamps absolus, alerte son (Web Audio) + vibration + notification desktop ; afficher/masquer, panneau + lanceur déplaçables, titre d'onglet, anneaux de progression bicolores (travail/pause). `REALISATION.md`.
- ⏸️ **[VOTE-MATCH]** V3 « vote live » — **non planifié**, brouillon `docs/specs/VOTE-MATCH-v3-draft.md` (RGPD/identité tranchés, bloquants restants). À re-cadrer plus tard.
- ✅ **[VOTE-MAP-SUBPROJECT]** — **CORRIGÉ [V21.1.66]** : les builders de vote carte agrègent désormais courant + descendants (owner/invité/hub en miroir via `voteScrutinsForProject`) → le vote d'un sous-projet apparaît sur la carte du parent. `REALISATION.md`.
- ✅ **[SAVE-INDICATOR]** + **[UNDO-DELETE]** + **[EMPTY-STATES]** — **LIVRÉS [V21.1.63→65]** (confort/UX) : ✓ « enregistré » à chaque patch mémo ; toast « Annuler » après suppression ; états vides soignés (icône + action). `REALISATION.md`.
- ✅ **[FOLDER-ATTACHMENTS]** + **[FILES-VIEW]** — **CODÉ [V23.0.74] (BUMP EXPORT v23, à valider Cowork)** : (A) pièce jointe rattachable à un **dossier** (colonne additive `attachments.project_id`, routes owner+invité, export niché par projet, purge cascade) ; (B) vues « Fichiers » (agrégation `_collect_files` avec **fil d'Ariane cliquable**) — bouton 📁 Fichiers par dossier (3 pages) + entrée « Fichiers » globale sidebar owner + endpoints lecture owner/global/invité scopé. `REALISATION.md` [V23.0.74].

_Vague VOTE (V1 → multi → groups → carte → event agenda), export/import de dossier, prévisualisation d'import, réactions : **tout livré et en prod** — voir `REALISATION.md`._

### 🐛 [VOTE-MAP-SUBPROJECT] — vote d'un sous-projet invisible sur la carte d'un parent (correctif) → cible V21.1.z

**Statut** : bug **confirmé en session Cowork (10 juil. 2026)** sur les données restaurants (vote-dossier **multi** du sous-projet « restaurants » sous « Voyage Japon »).

**Symptôme** : carte ouverte depuis le board du **sous-projet** qui porte le vote → OK (chip « 🗳 (dossier) », toggle « 🗳 Voix », badges de voix « ✓ n » sur les marqueurs). Carte ouverte depuis le board **parent** (ex. « Voyage Japon », qui inclut pourtant les points du sous-projet) → **aucun chip de scrutin, aucun toggle « 🗳 Voix »** ; et le filtre « Sous-projets » **dans** la carte ne ramène pas le vote. Se manifeste surtout **côté invité** : un partage du parent a pour racine le parent, donc la carte invité ne surface jamais le vote du sous-projet.

**Cause** : le calque de vote de la carte est construit pour le **seul projet du board courant** — owner `buildMapVoteData()` (index.html) lit `state.memoProject` ; invité `buildShareMapVoteData()` (share.html) lit `DATA.root_id` (ou le filtre `PFILTER`). Aucun ne parcourt les **votes des sous-projets** affichés sur la carte, et le filtre sous-projet in-carte ne reconstruit pas les scrutins.

**Correctif attendu** : agréger sur la carte les scrutins de **tous les projets présents** (le projet courant **ET** ses sous-projets/descendants dont des points sont affichés), pas seulement la racine. `buildMapVoteData` (owner) et `buildShareMapVoteData` (invité) **en miroir** (factoriser dans `_shared.js.html` si possible, invariant 9). Chaque scrutin conserve son `id`/nom — **préfixer par le nom du sous-projet** quand plusieurs projets portent un vote, pour les distinguer dans la barre de scrutins. Les compteurs (`opt` par mémo) et l'état viennent des **données déjà exposées** (mémos + `proj.votes`) → **zéro nouvelle route**. Cohérent avec le filtre sous-projet de la carte. **Portée invité** : n'agréger que les sous-projets **dans le périmètre du partage** (invariant 5) ; anonyme = lecture, pas de `onVote`. Frontend pur, **export inchangé (v21)**.

**Acceptation (validation Cowork)** : carte du parent (owner + invité approuvé) → le(s) scrutin(s) du/des sous-projet(s) apparaissent, badges de voix corrects, vote ouvrable ; sélection/bascule entre plusieurs scrutins OK ; carte du sous-projet direct **inchangée** ; anonyme = lecture seule. `node --check` + `py_compile`. Invariants 5/6/8/9.

## [FX-CONVERTER] — Widget convertisseur de devises (JPY↔EUR + sélecteur)

**Statut :** ✅ Fait — [V23.2.90] (voir REALISATION.md). Livré : cache `app_state.fx_cache` (pas de table `fx_rates`), routes owner/share/**hub**, `initFxWidget(cfg)` dans `_shared.js.html`, export inchangé v23 (la cible « v22 » ci-dessous était obsolète).
**Contexte :** Voyage Japon. Widget convertisseur calqué sur le Pomodoro (afficher/masquer, lanceur + panneau déplaçables, préférence par utilisateur), activable aussi par les invités depuis leur vue. Pas de changement de format d'export → APP_VERSION reste "23".

**Taux — 1 fetch/jour, conservé toute la journée (serveur) :**
- Source : Frankfurter `https://api.frankfurter.app/latest?from=EUR` (données BCE, gratuit, sans clé). Réponse = taux base EUR multi-devises (JPY, USD, GBP…).
- Cache serveur partagé : `{date, base:"EUR", rates_json, fetched_at}`. Refresh paresseux : à la 1ʳᵉ requête `/api/fx` où date stockée ≠ jour (Europe/Paris) → refetch + écrase ; sinon cache. Une seule sortie réseau/jour, pas de cron.
- Stockage : table `fx_rates` (additif, invariant 1) ou fichier JSON dans `data/`. Exclu de l'export (cache, pas de donnée utilisateur).
- Robustesse : fetch KO → garde le dernier taux, affiche « taux du {date} ». Pas de crash.

**Endpoints :**
- Owner : `GET /api/fx` → `{date, base, rates}`.
- Invités (invariant 5, bypass `/share/*`) : `GET /share/<token>/fx`, token revalidé.

**Widget (réutilise les patterns Pomodoro — invariant 9) :**
- Lanceur `#fx-launch` (glyphe monochrome `¥€`) + panneau `#fx-panel` déplaçables, clamp fenêtre, positions persistées (`fxPos`, `fxHidden`), re-show `#fx-reshow`.
- Case afficher/masquer `#fx-visible-cb` dans les réglages — owner (index.html) ET invités (share.html, hub.html) ; préférence locale par utilisateur.
- Conversion bidirectionnelle : 2 champs + 2 sélecteurs, défaut JPY↔EUR, bouton swap `↔`. Taper à gauche MAJ droite et inverse. Taux croisé côté client depuis la table base EUR. Arrondi (2 déc. EUR, 0 JPY).
- Sélecteur : JPY/EUR en tête + USD/GBP/CHF… ; couple mémorisé (localStorage).
- Style monochrome, pas de GSAP sur `<dialog>` (invariant 8), helpers mutualisés dans `_shared.js.html`.

**Contraintes :** additif (inv. 1), pas de CDN runtime navigateur (inv. 6, seul appel externe = fetch serveur), aucun commit/tag/push sans feu vert. Journaliser IDEAS→REALISATION une fois codé + build OK. Validation Chrome owner + invité par Cowork.

## Quick wins

- **Raccourcis clavier 1-9** : ouvrir directement les 9 premiers liens de la catégorie active.
- **Compteur de clics** : colonne `clicks` sur `links`, tri optionnel "par usage" — les services les plus utilisés remontent tout seuls.
- **Mode édition verrouillable** : un cadenas dans le header qui masque les boutons éditer/supprimer/drag pour éviter les fausses manips (surtout sur mobile).
- **Badge favicon "service down"** : si un service est offline, changer le favicon de l'onglet (point rouge) — visible d'un coup d'œil quand le dash est la page d'accueil.
- **[SUBPROJECT-IN-EDIT] Rattacher un projet à un parent depuis sa pop-in d'édition** (idée dash prod) : choisir le projet parent dans un sélecteur de la pop-in « Modifier le projet », en plus du drag & drop sidebar. Réutilise `_resolve_parent()` (anti-cycle déjà géré). Frontend + petit ajout API.
- ✅ **[SHARE-OPEN-AS-GUEST]** — **LIVRÉ [V23.3.92]** (frontend pur `index.html`, export v23 inchangé) : bouton rond ↗ « Ouvrir en tant qu'invité » à côté des 4 boutons 📋 de la vue Partages (`guestOpenBtn`, `window.open(url,'_blank','noopener')`). Détail `REALISATION.md`.
- **[SHARE-OPEN-AS-GUEST] Ouvrir un partage en tant qu'invité depuis 🔗 Partages** (idée Fabien, 23 juil. 2026) : à côté de chaque bouton 📋 « Copier le lien » de la vue Partages & activité (lien de partage, lien par accès, lien unique du hub — les 4 sites `index.html` ~l. 4555/4627/4726/4901), ajouter un bouton rond **↗ « Ouvrir en tant qu'invité »** qui ouvre l'URL du partage dans un **nouvel onglet** (`window.open(url, '_blank', 'noopener')`). Facilite les tests owner : l'identité invité vue dans l'onglet dépend du `guest_token` déjà en localStorage pour ce lien (sinon formulaire PIN — c'est le comportement voulu). Frontend pur owner (`index.html` seul), export inchangé, invariant 9 (gabarit `.task-actions`).
- ✅ **[MEMO-TITLE-FALLBACK]** — **LIVRÉ [V23.3.93]** (frontend pur 3 pages, export v23 inchangé, rien en base) : sans titre, l'accroche du contenu prend le style titre (gras) sans dupliquer le texte (helpers partagés `memoLeadText`/`memoRestText`/`MEMO_EMPTY_LABEL`) ; card vide corrigée (contenu HTML sans texte → placeholder) ; placeholders harmonisés. Détail `REALISATION.md`.
- **[MEMO-TITLE-FALLBACK] Intitulé de repli pour les mémos sans titre** (idée Fabien, 23 juil. 2026 — décision : option B, affichage seul) : partout où un intitulé est attendu, replier sur les premiers mots du contenu — en particulier la **card du board** (owner + share + hub) : quand `title` est vide, le premier segment de l'extrait (`stripHTML(content)`, ~60 car.) prend le **style titre** (gras), SANS dupliquer le texte (le label remplace/style le début de l'extrait, pas une 2e ligne). Harmoniser au passage les placeholders existants (« (mémo) », « (sans titre) », « (mémo vide) »). La note rapide continue de remplir le contenu : **rien ne change en base ni à l'export**. Frontend pur 3 pages, helper partagé possible dans `_shared.js.html` (invariants 6/9). ⚠️ À vérifier dans le même lot : card totalement vide observée le 23 juil. en local (mémo sans titre ni contenu affichable ?) — reproduire, et corriger si bug d'extraction.
- ✅ **[MEMO-EDITOR-IMAGES]** — **LIVRÉ [V23.3.94]** (frontend pur 3 pages, export v23 inchangé, aucune route nouvelle) : section « 📷 Photos » dans la pop-in d'édition (helper partagé `renderMemoPhotos`), miniatures → visionneuse toutes images + EXIF, suppression `confirmPopin` + « ＋ Ajouter une photo » (routes/flux de la card), refresh section+card sans reload complet ; parité owner/share/hub `can_edit`, masquée si vide + lecture seule. Détail `REALISATION.md`.
- **[MEMO-EDITOR-IMAGES] Photos du mémo visibles dans la pop-in d'édition** (idée Fabien, 23 juil. 2026) : la pop-in « Éditer le mémo » n'affiche pas les images du mémo (gérées uniquement depuis la card) → ajouter une section **« 📷 Photos »** entre Contenu et Fichiers : miniatures (réutiliser `.thumb-wrap`/`.thumb-del`), clic → `runImageViewer` (toutes les images du mémo, badge EXIF côté owner), suppression avec confirm, bouton « + Ajouter une photo » branché sur le flux d'upload existant de la card (validation par signature serveur inchangée). Rafraîchir card ET pop-in après ajout/suppression. Parité invité `can_edit` là où la pop-in d'édition existe (share.html, hub.html), routes existantes uniquement (invariant 5). Frontend pur, export inchangé, invariants 6/8/9.
- ✅ **[VIEWER-FILMSTRIP]** — **LIVRÉ [V23.3.95]** (frontend pur `runImageViewer` partial, export v23 inchangé) : bande de vignettes défilable quand > 1 image (owner/share/hub/carte d'un coup), miniatures 52 px lazy, courante en évidence, clic = saut direct, auto-scroll de la bande, nav/clavier/compteur/zoom inchangés, 1 image = pas de bande. Détail `REALISATION.md`.
- **[VIEWER-FILMSTRIP] Bande de vignettes dans la visionneuse d'images** (idée Fabien, 23 juil. 2026) : quand la visionneuse (`runImageViewer`, partial partagé — owner/share/hub + calque photo carte) affiche **plus d'une image**, ajouter une **bande de vignettes horizontale défilable** sous l'image (au-dessus de la barre d'outils) : miniatures ~52 px (mêmes URLs, `loading="lazy"`), vignette courante mise en évidence (bordure accent), clic = saut direct à l'index, auto-scroll pour garder la courante visible, flèches/clavier/compteur « n / N » inchangés. Une seule image = pas de bande. Mobile : même bande, défilement tactile. S'aligne sur les styles `.iv-*` existants (exception justifiée invariant 9), frontend pur, export inchangé, invariants 6/8.
- ✅ **[IMAGE-THUMBS]** — **LIVRÉ [V23.4.96]** (Pillow, export v23 inchangé, aucune route nouvelle) : dérivées `t_`/`s_` dans `data/uploads/derived/` (originaux intacts), `?size=t|s` sur routes existantes (scope invité inchangé), génération upload + backfill daemon + filet à la volée, purge cascade, fallback original (gif/petite/corrompu). Gain : photo 9,2 Mo → vignette 20 Ko. Détail `REALISATION.md`, `CLAUDE.md` mis à jour.
- **[IMAGE-THUMBS] Images dérivées serveur (vignettes + taille écran)** (décision Fabien, 23 juil. 2026 — lot backend V23.4.x, spec complète `docs/specs/IMAGE-THUMBS-derived-images.md`) : les images sont servies en original plein pot partout (8-9 Mo/photo → chargements très lents mobile, vécu au festival). → dérivées Pillow `t` ~400 px (cards, filmstrip, carte) et `s` ~1600 px (visionneuse) dans `data/uploads/derived/`, originaux INTACTS (⬇ = brut), génération à l'upload + backfill daemon (pattern `image_meta`), purge en cascade, `?size=t|s` sur les routes image EXISTANTES (invariant 5), rien à l'export (donnée dérivée, pas de bump). Nouvelle dépendance Pillow (décision explicite).

## Dette technique / architecture

- **[SHARED-HELPERS] Mutualiser les helpers front owner/invité** (voir [docs/adr/ADR-001](docs/adr/ADR-001-mutualisation-helpers-front.md), **accepté — Option C**) : ~31 fonctions JS dupliquées entre `index.html` et `share.html` (cause : invariant 6). Extraire les helpers purs/sans état dans un partial unique `templates/partials/_shared.js.html` inclus au rendu (`{% include %}`) dans les deux templates ; mettre à jour l'invariant 6 (distinguer *source* et *sortie livrée*). 2e passe : factoriser la logique de rendu carte (`draw`/`renderSubbar`/`memoRow`). Le backend (`app.py`) est déjà factorisé (owner et invité partagent `_perform_memo_update`, etc.) — la dette est frontend only.

## Confort / UX (rendre l'app agréable)

Gains rapides à fort effet :

- **[UNDO-DELETE] « Annuler » après suppression** : quand un mémo part à la corbeille, toast « Mémo supprimé — Annuler » pendant ~5 s. Réutilise la suppression douce (`deleted_at`) + la route de restauration existante → annulation quasi gratuite. Le geste qui rassure le plus à l'usage. Frontend + un appel restore.
- **[SAVE-INDICATOR] Indicateur « enregistré »** : petit ✓ discret pendant ~1 s à chaque `patch` de mémo (titre, date, priorité) — aujourd'hui les sauvegardes sont silencieuses. Frontend pur.
- **[EMPTY-STATES] États vides soignés** : projet sans mémo, carte sans point, recherche sans résultat → message amical + bouton d'action (« Ajouter le premier mémo »). Beaucoup de polish pour peu de code, frontend pur.
- **[SHORTCUT-HELP] Aide raccourcis (`?`)** : overlay listant les raccourcis (`/`, à venir Cmd/K, 1-9…) + focus `/` plus visible. Discoverability quasi nulle aujourd'hui. Frontend pur.
- ✅ **[CARD-PREVIEW]** — **LIVRÉ [V23.6.98]** (frontend pur 3 pages, export v23 inchangé, zéro route) : extrait 2 lignes sous le titre (cas sans-titre déjà géré par [MEMO-TITLE-FALLBACK]) + rangée de pictos `☑ n/m · 📷 n · 📎 n · 💬 n` (helper partagé `memoPictosRow`, masqués à zéro, anti-doublon = anciens badges 📎/💬/☑ absorbés ; share = 📷/📎 seuls car sous-tâches/commentaires inline). Détail `REALISATION.md`.
- ✅ **[TAG-NAV]** — **LIVRÉ [V23.7.101]** (frontend pur `index.html`, owner-only, export v23 inchangé, zéro route) : section sidebar « 🏷 Étiquettes » (tags liens v7 + projets v8, compteurs, tri usage puis alpha, repli persisté, masquée en rail/si vide), vue dédiée `state.view='tag'` (Liens en cards + Projets en lignes + bouton 🔍 recherche globale), ajout rapide (D&D lien→étiquette + chip « + » datalist / « × »). `goTag` → vue dédiée. Détail `REALISATION.md`.
- ✅ **[WEB-CAPTURE]** — **LIVRÉ [V23.8.102]** (frontend pur `index.html`, owner-only, export v23 inchangé, zéro route) : `handleCapture()` lit `?capture=link|memo&url=&title=&text=` puis nettoie l'URL (`replaceState`), pré-remplit la pop-in lien / le composeur Inbox (validation manuelle, anti-XSS texte brut, bornes, param inconnu ignoré) ; onglet Paramètres « 🔖 Capture » = 2 bookmarklets draggable générés avec l'origin + copie. Détail `REALISATION.md`.
- **[OFFLINE] Mode hors ligne voyage** (besoin Fabien 23 juil. 2026 — Japon début novembre, réseau intermittent) : service worker **network-first versionné** `APP_VERSION` (cache purgé à chaque déploiement, skipWaiting déclenché par [UPDATE-PROMPT] — le SW devient gérable), lecture offline complète (dernière synchro + bandeau 📴 horodaté), photos/tuiles vues en cache LRU borné, **notes rapides hors ligne** (file localStorage → POST Inbox idempotent à la reconnexion, ⏳ en attente), édition/vote/commentaires offline HORS périmètre (conflits), jamais de cache sur /share/* ni sur les redirects Authelia. Owner d'abord. **Spec prête : `docs/specs/OFFLINE-travel-mode.md`** — lot V23.11.x, après [PWA] ; test mode avion réel par Fabien bien avant le départ.
- **[PWA] Dashboard installable + Share Target Android** (validé Fabien 23 juil. 2026 — suite logique de [WEB-CAPTURE] : le partage natif Android = le bookmarklet du mobile) : `manifest.webmanifest` owner (standalone, thème sombre, icônes PNG committées depuis favicon.svg) + `share_target` GET → réutilise le lecteur `?capture=` (forme sans `capture=` à reconnaître) ; **manifest invité dynamique** `GET /share/<token>/manifest.webmanifest` (start_url = l'accès du token, sonde bypass, rien d'autre exposé — invariant 5) ; **SANS service worker phase 1** (choix anti-cache-fantôme) ; iOS = installable mais pas de share target (limite plateforme). Export inchangé. Inclut **[UPDATE-PROMPT]** (idée Fabien) : version embarquée au rendu + comparaison via les polls existants → toast « ⬆ Nouvelle version — Recharger » (respecte uiBusy, 3 pages) — indispensable en PWA standalone (pas de bouton recharger) et futur déclencheur skipWaiting si SW en phase 2. **Spec prête : `docs/specs/PWA-installable-share-target.md`** — lot V23.10.x, après [VOICE-MESSAGES].
- ✅ **[VOICE-MESSAGES]** — **LIVRÉ [V23.9.103]** (frontend + 1 retouche backend, export v23 inchangé, zéro route) : vocal = PJ v22 + marqueur `[audio:<fn>]` dans le texte du commentaire → bulle vocale ; helpers partagés `recordVoice`/`renderVoiceBody`/`voiceAttachButton`/`voiceCommentButton` (3 pages) ; signatures webm/mp4 ajoutées à `_attach_preview` ; owner + invité `can_edit` ; suppression commentaire ≠ suppression PJ. Détail `REALISATION.md`.
- **[VOICE-MESSAGES] Mémos vocaux + commentaires vocaux façon WhatsApp** (idée Fabien 23 juil. 2026, architecture Fabien : **le vocal = une PJ v22 ordinaire + un marqueur `[audio:<filename>]` dans le texte du commentaire** → rendu en bulle vocale, **ZÉRO bump d'export** — le texte voyage en v14, le fichier en v22) : 🎤 MediaRecorder natif (webm/opus Chrome, m4a AAC iOS — étendre les signatures audio du validateur preview v22), bouton dans la pop-in mémo ET à côté du champ commentaire, owner + invités can_edit, limite 5 min, dégradation propre. Suppression commentaire = la PJ survit (non destructif). **Spec prête : `docs/specs/VOICE-MESSAGES-audio-memo-comments.md`** — lot V23.9.x, après [WEB-CAPTURE].
- **[WEB-CAPTURE] Capturer une page/sélection depuis le navigateur** (idée Fabien 23 juil. 2026, façon web clipper SiYuan/Trilium — phase 1 en BOOKMARKLETS, zéro extension) : 2 favoris « magiques » générés dans Paramètres → onglet « 🔖 Capture » — « ➕ Capturer la page » (→ pop-in lien pré-remplie url/titre) et « 📝 Note depuis la page » (sélection → mémo Inbox pré-rempli + source) via query params `?capture=link|memo` lus par la page owner (nettoyés, anti-XSS, bornés — derrière Authelia, rien de public). Frontend pur, export inchangé. **Spec prête : `docs/specs/WEB-CAPTURE-bookmarklet.md`** — lot V23.8.x, après [TAG-NAV].
- **[BROWSER-EXTENSION] Extension Chrome (phase 2, non planifiée)** : MV3 en mode développeur — badge d'activité 🔔 sur l'icône, popup mini-lanceur des services, captures en un clic. Sous-dossier `extension/`. À cadrer après validation d'usage des bookmarklets [WEB-CAPTURE].
- **[TAG-NAV] Section « 🏷 Étiquettes » dans la sidebar + vue dédiée par tag** (idée Fabien 23 juil. 2026, inspirée du panneau Tags de SiYuan — concrétise l'emplacement réservé par [SIDEBAR-EVERNOTE]) : section sidebar repliable listant les tags des liens (v7) + projets (v8) avec compteurs, tri par usage ; clic = **vue dédiée** « 🏷 #tag » (liens en cards + projets en lignes, bouton bascule vers la recherche globale). Owner only V1, agrégation front, zéro route, **export inchangé**. Inclut aussi l'**ajout rapide de tags aux liens** (décision 23 juil.) : D&D lien→étiquette sidebar + mini-chip « + » sur la card (datalist, ×-retrait), zéro route nouvelle. **Spec prête : `docs/specs/TAG-NAV-sidebar-tags.md`** — lot V23.7.x. V2 possibles : tags mémos (bump v24), parité invité, renommage/fusion.
- **[CARD-PREVIEW] Aperçu du contenu sur les cards compactes** (idée Fabien, 23 juil. 2026, maquette variante B validée) : sous le titre, extrait 2 lignes + rangée de pictos-compteurs ☑ n/m · 📷 n · 📎 n · 💬 n (signature du mémo, info passive — complète [LESS-BUTTONS] dont les cards ne montrent plus que le titre). Anti-doublon : absorbe les badges 📎/💬 actuels en conservant leurs clics ; cas sans-titre = l'extrait reprend APRÈS la partie promue en titre ([MEMO-TITLE-FALLBACK]). **Spec prête : `docs/specs/CARD-PREVIEW-excerpt-pictos.md`** — lot V23.6.x, 3 pages, frontend pur, export inchangé.
- ✅ **[LESS-BUTTONS]** — **LIVRÉ [V23.5.97]** (frontend pur 3 pages, export v23 inchangé, zéro route/schéma) : card réduite à la consultation (sous-tâches + commentaires) + barre compacte ✓/✎/⋯ ; contrôles d'édition déplacés dans la pop-in (owner `renderMeMeta` = projet/échéance/priorité/emoji/couleur/récurrence/hors-vote ; share `renderQMeta` = échéance/priorité/position ; hub déjà conforme) ; menu ⋯ card = Déplacer/Partager/Dupliquer/Supprimer (owner) et Déplacer/Supprimer (share). Règle d'audit card→pop-in respectée. Détail `REALISATION.md`.
- **[DRAFT-SAVE] Sauvegarde en brouillon à la sortie d'une édition** (idée dash prod) : si on quitte une édition non enregistrée, garder un **brouillon** récupérable au retour. Le geste qui rassure, comme [UNDO-DELETE] ; recoupe le fix « la saisie invité ne s'efface plus ».
- **[PHOTO-IMPORT-EMPTY] Import direct dans un projet sans photo** (idée dash prod) : cliquer « photo » dans un projet **sans aucune photo** propose directement l'**import** au lieu d'un état vide. Recoupe [EMPTY-STATES] et [PHOTO-TEMPLATE-MEMO]. Frontend.

Moins de clics :

- **[CMD-K] Palette de commandes (Cmd/Ctrl+K)** : sauter à n'importe quel projet/mémo/lien ou créer un mémo, au clavier. Le plus gros saut de confort pour un usage quotidien intensif. Frontend, effort moyen, autonome.
- **[INLINE-TITLE] Édition inline du titre** d'un mémo sur la carte (double-clic) sans ouvrir la pop-in.
- **[MOVE-MENU] « Déplacer vers… »** dans le menu au survol d'un mémo (en plus de dupliquer), pour éviter le drag & drop.
- **[COMMENT-QUICKVIEW] Accès rapide à la conversation depuis un mémo** (idée dash prod) : cliquer un mémo donne un accès direct au **fil de commentaires** sans dérouler tout le détail.
- **[COMMENT-OPEN] Ouvrir la conversation depuis un commentaire** (idée dash prod) : depuis un commentaire (badge 💬 / cloche 🔔), ouvrir directement le **fil complet** du mémo.
- **[COMMENT-PIN] Épingle « derniers messages »** (idée dash prod) : indicateur/épingle pour repérer les **derniers commentaires ajoutés** (non lus), au-delà du compteur actuel.

Petites touches de plaisir :

- **[VIEW-TRANSITIONS] Transitions entre vues** (Liens ↔ Mémos ↔ Plan) en léger fondu GSAP — cohérent avec les animations cards/tuiles existantes (invariant 8 : jamais sur un `<dialog>`).
- **[PROJECT-DONE] Micro-célébration** quand tous les mémos d'un projet passent à « terminé » (petite animation discrète, pas de confetti envahissant).
- **[RELATIVE-DATES] Dates relatives partout** (« dans 3 j », « il y a 2 j ») en complément de la date — déjà partiel via `dueInfo`, à généraliser.

## Moyens

- **[MENTION-CHECKLIST] Pinguer quelqu'un depuis une liste à cocher** (idée dash prod) : étendre les mentions `@` (déjà dans le corps Quill et les commentaires) aux **items de liste à cocher** d'un mémo, avec ajout aux assignés comme ailleurs. Réutilise l'infra mentions ; effort moyen (blot Quill `list` + menu d'autocomplétion sur les items).
- **[ONE-LINK-MULTI] Un seul lien pour plusieurs projets partagés** (idée dash prod) : un lien unique donnant accès à deux projets (ou plus) plutôt qu'un lien par projet. À cadrer avec l'invariant 5 (un token = un scope) : nécessite un token multi-cibles ou un regroupement de partages côté `shares`. Effort moyen, touche le backend de partage.
- **[IMAGE-LAZY-LOAD] Images chargées à la lecture, qualité moindre** (idée dash prod) : lazy load des images + **miniatures dégradées** générées côté serveur pour soulager le Zimaboard (aujourd'hui les fichiers sont servis pleine taille). Recoupe [IMAGE-CAROUSEL] et la note perf de [PHOTO-TEMPLATE-MEMO] ; attention au coût de génération de thumbnails (Pillow lourd sur le Zimaboard) — à cadrer.
- **[PROJECT-TABS] Projet avec sous-projets en onglets** (idée dash prod) : afficher les sous-projets (et récursivement les leurs) en **onglets** dans le board d'un projet, en plus de l'arbre sidebar. Réutilise `projects.parent_id`. Effort élevé (refonte du board), frontend ; aucun changement de schéma/export.
- [x] **[REACTION-PALETTE]** Palette de réactions configurable → **✅ FAIT [V21.0.57]** (greffée sur [COMMENT-REACTIONS] v21 → commit groupé [V21.0.56 + V21.0.57], PAS de bump v22). Whitelist data-driven `_reaction_palette(db)` (config owner `app_state.reaction_emojis`, seed = 6 base, ajout/retrait) ; validation stricte d'un **seul grapheme emoji** `_clean_reaction_emoji` (Python pur, plages Unicode + accessoires ZWJ/VS/teinte/keycap/régionaux + garde ≤ 10 cp, zéro lib/CDN — invariant 6 ; texte/HTML/2-emojis → 400) ; onglet « 😊 Réactions » dans Paramètres (gabarit priorités, chips ✕-retirables + ＋ Ajouter, sauvegarde auto) ; palette exposée (`/api/settings`, `share_data`, hub `/data`) et **exportée si custom** (top-level, compat v20/v21 sans palette = base par défaut) ; réactions à l'import validées en FORMAT (préserve les emojis retirés). Owner-only config, invités consomment. Détail : `REALISATION.md` [V21.0.57] + `CLAUDE.md` invariant 1 (v21). Invariants 1/2/5/6/8/9. _✅ Livré en prod — tag V21.0.57 (10 juil. 2026)._
- **[NAV & MOBILE] cluster d'idées navigation/repères (Fabien, 10 juil. — à maquetter avant brief, lots frontend séparés)** :
  - **[HEADER-ADD-CONTEXT]** Le « + Ajouter » global est trop présent en mobile (page = 2 blocs empilés Liens / Mémos ; « Ajouter » n'a pas le même sens selon le bloc, et les mémos ont déjà leur champ rapide, les liens leur « + » par catégorie). → rendre l'ajout **contextuel** (un « + » discret dans l'en-tête de chaque bloc) plutôt qu'un gros bouton global. Reco = contextuel (vs FAB flottant). Frontend pur (CSS/markup mobile), export inchangé.
  - **[FAVORITES]** (absorbe l'ancien OWNER-NAV-QUICK) Favoris épinglables : étoile ☆ sur un **projet/sous-projet OU une vue** (Plan, Agenda, Carte, Mémos) → une section **« ⭐ Favoris » tout en haut de la colonne de gauche, AU-DESSUS de « Partages »** (desktop) et **AVANT « Partages »** (mobile). **Décisions Fabien figées** : stockage EN BASE (synchronisé desktop ↔ mobile) ; **NON exporté / NON importé** (confort de navigation perso, comme les voix de vote → aucun bump d'export, l'import ignore). Résout le problème de scroll owner vers Plan (on l'épingle). Implémentation : table/colonne additive (favoris = liste de refs {type: project|view, id}), routes toggle owner-only, section rendue par le partial pour desk+mobile cohérents. Owner-only V1 (invités hors périmètre). ⚠️ soin : une vue épinglée pointe une vue, un projet épinglé pointe un id projet — modèle unifié `{kind, ref}`. **V1 briefé (10 juil.) = projets/sous-projets uniquement** (vues en V2) ; table dédiée `favorites`, section sidebar façon Partages, ★ dans la pop-in projet. **Maquette validée, EN COURS chez CC.**
  - **[HOME-RESET]** Cliquer le titre « Dashboard » réinitialise la vue (dé-sélection projet/filtre → écran initial), réflexe universel logo=accueil. Quick win, quelques lignes, frontend pur.
  - Séquencement proposé : [HOME-RESET] = quick win isolé ; [HEADER-ADD-CONTEXT] et [FAVORITES] chacun avec sa maquette `show_widget` avant brief. Tous APRÈS [COMMENT-REACTIONS] V21.
- **[POMODORO] Pomodoro / chronomètre avec alerte de fin** (idée Fabien) : minuteur type Pomodoro (ou chrono libre), rattaché à la vue Agenda, avec **alerte quand le temps est écoulé**. Frontend pur, **aucun changement schéma/API/export** (état du minuteur = éphémère, en mémoire/localStorage ; rien en base).
  - **Alerte double, chacune (dés)activable indépendamment via checkbox** dans l'UI du minuteur :
    - **Sonore** : Web Audio API. **Bip généré à la volée** (`OscillatorGain`) ou petit WAV **inliné en base64** — **jamais** d'URL/CDN externe au runtime (invariant 6 : sortie HTML autonome, pas de dépendance runtime). Un bip synthétisé = zéro asset à héberger.
    - **Haptique** : `navigator.vibrate([pattern court, ex. 3 vibrations rapides])`.
  - **Contraintes à respecter** :
    - **Interaction utilisateur requise** : le son/vibration ne peuvent partir qu'après un geste (le clic « Démarrer » du chrono le garantit — l'AudioContext est débloqué à ce moment).
    - **Dégradation propre, jamais de crash** : `navigator.vibrate` **absent sur iOS/Safari** et Web Audio parfois restreint → guarder par `typeof`/`try-catch`, no-op silencieux si non supporté (ne pas supposer l'API présente).
    - **Onglet en arrière-plan** : les timers throttlés en background — prévoir un recalage sur l'heure réelle (comparer à un timestamp de fin plutôt que décrémenter un compteur), sinon l'alerte dérive.
  - **Trois modes dans le même widget « ⏱ »** (chips gabarit `.prio-btn`, invariant 9) : **🍅 Pomodoro** (cycles décompte travail/pause configurables, alerte à chaque fin de cycle), **⏳ Minuteur libre** (décompte d'une durée choisie + alerte), **⏱ Chronomètre** (compte CROISSANT, départ/pause/remise à zéro, éventuellement tours — pas d'alerte de fin par nature). Même mécanique d'horloge, seuls le sens du compteur et l'alerte changent.
  - **Source de vérité = timestamps absolus** (précision du point « arrière-plan » ci-dessus) : `end_at` pour pomodoro/minuteur, `start_at` pour le chrono — chaque tick RECALCULE depuis `Date.now()`, jamais de « −1 par seconde ». Recalage explicite sur `visibilitychange`/focus au retour d'arrière-plan.
  - **Web Worker = option de confort desktop** pour la ponctualité du bip en onglet caché (les timers d'un worker sont moins bridés que ceux de la page). ⚠️ Pas magique : téléphone verrouillé / page gelée (iOS) = worker suspendu aussi — **limite honnête assumée** : l'alerte part au réveil de la page (et le temps affiché est exact grâce au timestamp). Dégradation propre si l'API manque.
  - **Parité invités : OUI, gratuite** — widget dans le partial `_shared.js.html` (ADR-001) → les 3 pages (owner/share/hub) d'un coup. État 100 % local au navigateur de chacun (localStorage), zéro synchro, zéro route, zéro donnée partagée = aucune surface de sécurité. Le time-tracking rattaché aux mémos (écriture en base, scopes) = autre lot, autre spec, si envie un jour.
  - Effort moyen, autonome, **lot tampon idéal** entre deux gros morceaux de la vague vote. Recoupe [AGENDA] (emplacement UI) ; réutilise le pattern GSAP/anim discret (invariant 8, jamais sur un `<dialog>`) pour un flash visuel de fin en complément. Vanilla JS sans build, comme tout le front.

- **[PHOTO-TEMPLATE-MEMO] Mémo « photo » : carrousel + barre d'options + frise en deux niveaux** — maquette annotée : [`docs/specs/PHOTO-TEMPLATE-MEMO-mockup.svg`](docs/specs/PHOTO-TEMPLATE-MEMO-mockup.svg). Affichage dédié des photos d'un projet (complète [PHOTO-MAP] côté UI ; même source de données `image_meta`, **aucune écriture sur les mémos**).
  - **Carrousel** (repère 1) : image courante, flèches ‹ ›, compteur n/total, badge lieu + date de prise de vue (EXIF).
  - **Plein écran** (repère 2) : ouvre la visionneuse existante `runImageViewer` (zoom/pan/rotation déjà faits).
  - **Barre d'options** (repère 3) : plein écran · télécharger · rotation ↺↻ · zoom −＋ · **📍 localiser cette photo sur la carte** · supprimer (gaté `can_edit`). Réutilise la barre de `runImageViewer`.
  - **Bouton « Carte »** (repère 4) : ouvre la carte du projet avec le calque photos ([PHOTO-MAP]), filtré projet/groupe.
  - **Vignettes** (repère 5) : toutes les photos, la courante surlignée (sync avec la frise).
  - **Frise par dates** (repère 6) : un point par jour, **le chiffre = nombre de photos ce jour-là** ; clic = filtrer le carrousel sur ce jour.
  - **« + » → frise horaire** (repère 7) : si **plusieurs photos le même jour**, le « + » déplie une **2ᵉ frise plus large par heures** (la timeline « en deux » : jours → heures).
  - **Données** : tout vient de `image_meta` (lat/lng/label/taken_at), persistée à l'upload — voir [PHOTO-MAP]. **Aucun changement d'export.** Invariants 5/6/8 (réutilise `runImageViewer`/`runMapDialog`, helpers dans le partial, pas de GSAP sur le `<dialog>`).

- **[IMAGE-TRASH] Corbeille des images (suppression douce 30 j, owner + invités, notification)** : aujourd'hui supprimer une image = **hard delete** immédiat (`_delete_image_files` + retrait de `memos.images`), owner comme invité. But : transposer la corbeille des mémos (invariant 7) aux images. Décisions actées : (1) **toutes** les suppressions passent par la corbeille (owner ET invités) ; (2) pendant les 30 j, l'owner **voit** l'image, marquée « supprimée par [invité] », avec **Restaurer / Supprimer définitivement** ; (3) l'owner est **notifié** quand c'est un invité ; (4) **purge auto à 30 j**.
  - **Modèle** : nouvelle table `image_trash` (`memo_id`, `memo_uid`, `filename`, `deleted_by` = nom owner ou invité, `deleted_at`, `share_id` nullable). À la suppression : retirer le filename de `memos.images` (→ disparaît des vues owner + invité, y compris `share_data`) **mais garder le fichier** dans `data/uploads/` jusqu'à la purge. Restaurer = remettre le filename dans `memos.images` + supprimer la ligne. Supprimer définitivement / purge = `_delete_image_files` + supprimer la ligne.
  - **Routes** : `DELETE /api/memos/<id>/images/<name>` (owner) et `DELETE /share/<t>/memo/<id>/images/<name>` (invité, **chemin scopé existant — invariant 5, pas de nouvelle route publique**) deviennent **soft**. Nouvelles routes owner : lister / restaurer / purger (ex. `GET /api/image-trash`, `POST /api/image-trash/<id>/restore`, `DELETE /api/image-trash/<id>`).
  - **Notification owner** : réutiliser le fil 🔔 Activité / la section « modifications par invité » de 🔗 Partages → entrée « 🗑 [invité] a supprimé une image de [mémo] » avec Restaurer / Confirmer.
  - **UI owner** : surface des images supprimées (dans la vue 🗑 Corbeille existante, onglet « Images », ou section dédiée) : miniature + « supprimée par X · date » + Restaurer / Supprimer définitivement.
  - **Purge auto** : nouveau délai **30 j** (`IMAGE_TRASH_DAYS=30`, distinct de `BACKUP_KEEP_DAYS=7`), branché dans la boucle quotidienne existante à côté de `_purge_trash`.
  - **Invariants** : 5 (invité via route scopée existante), 7 (philosophie soft-delete), **export inchangé** — les images en corbeille sont exclues de l'export comme les mémos en corbeille (filename déjà retiré de `memos.images`, table `image_trash` non exportée → pas de bump de version ; limite assumée : une image en corbeille n'est pas récupérable après un cycle export/import, cohérent avec la corbeille mémos).

- **[IMAGE-CAROUSEL] Refondre l'affichage des images des mémos (carrousel + options)** : aujourd'hui = miniatures + visionneuse simple. But : un **carrousel** pour naviguer entre les images d'un mémo (← →, indicateur n/total, plein écran), avec une **barre d'options** : **télécharger**, **rotation** (↺ ↻), zoom, et **supprimer** (propriétaire). Réutilise l'infra existante : les fichiers sont déjà servis par `/uploads/<name>` (owner) et `/share/<token>/image/<name>` (invité) → le **téléchargement** = juste un lien `download` vers ces routes (invités inclus, dans le scope du partage). Décision à trancher pour la **rotation** : (a) **affichage seul** (CSS `transform`, non sauvegardé — simple, rien côté serveur) ou (b) **persistante** (réécrit le fichier via Pillow côté serveur + re-valide la signature binaire — plus lourd, modifie `data/uploads/`). Recommandation : commencer par (a). **Parité invité** : la visionneuse/carrousel doit marcher aussi côté `share.html` (lecture + téléchargement scopés) ; si le composant est identique owner/invité, le mettre dans `templates/partials/_shared.js.html` (ADR-001). **Invariants** : aucun changement du format d'export (toujours noms de fichiers seuls, v6+), validation par signature binaire des uploads inchangée (invariant 5), pas de GSAP sur un `<dialog>` (invariant 8).

- **[TAG-NAV] Section « Étiquettes » dans la sidebar (navigation par tag)** : les tags existent déjà (sur liens, mémos et projets, normalisés minuscules sans `#`, recherche scopée `l#`/`m#`/`p#`) mais ne sont pas navigables depuis la sidebar. Ajouter une section **Étiquettes** listant tous les tags présents (dédupliqués, triés, avec compteur), chacun cliquable pour **filtrer/parcourir** tout ce qui le porte (réutilise la recherche scopée existante). Optionnel : repli/dépli, et petites pastilles de couleur. **Frontend uniquement** (agrège `state.links`/`state.memos`/`state.projects`), aucun changement d'export. Brique d'Evernote ([SIDEBAR-EVERNOTE]) — l'utilisateur aime ce modèle « par étiquette ».

- [x] **[ATTACHMENTS] Pièces jointes (pdf, texte, image, audio) téléchargeables, invités inclus** → **✅ FAIT [V22.0.69] (BUMP EXPORT v22)** : table additive `attachments`, upload owner + invités approuvés `can_edit` (300 Mo, `MAX_CONTENT_LENGTH` 320 Mo, gunicorn `--timeout 300`), validation par **signature binaire** (`_attach_preview`), service inline/download anti-XSS (`_serve_attachment_row`), routes owner `/api/memos/<id>/attachments` + `/api/attachments/<id>` et invité `/share/<token>/memo/<id>/attachments` + `/share/<token>/attachment/<id>` (scope revalidé, invariant 5), export v22 additif = **noms de fichiers seuls** (binaires dans `data/uploads/`, jamais dans le JSON), import non destructif `_import_memo_attachments`. Détail : `REALISATION.md` [V22.0.69]. Idée d'origine ci-dessous.
  - généraliser l'upload d'images existant (`_save_uploaded_image`, `UPLOAD_DIR`, validation par **signature binaire** — invariant 5) à d'autres types. Réutiliser au maximum l'infra en place (table images / `data/uploads/`, routes `/uploads/<name>` et `/share/<token>/image/<name>`, upload invité `/share/<token>/memo/<id>/images`).
  - **Types & validation** : PDF (signature `%PDF-`, facile), images (déjà fait). Audio (signatures mp3/`ID3`, m4a/`ftyp`, ogg/`OggS`, wav/`RIFF`…`WAVE`) — plus de cas + fichiers lourds, attention à la taille. **Texte** (`.txt`) : pas de signature fiable → valider par extension + plafond de taille + servir en **téléchargement** (`Content-Disposition: attachment`), jamais en inline (anti-XSS). Garder une **liste blanche d'extensions** et une **taille max** par type.
  - **Téléchargement** : route owner + route publique `/share/...` renvoyant le fichier en pièce jointe ; les invités **approuvés** peuvent télécharger (pas seulement voir), dans le strict périmètre du partage (invariant 5). Ne jamais servir un type exécutable/HTML inline.
  - **Modèle & export** : soit étendre la table images en table `attachments` générique (colonne `kind`/`mime`), soit une table dédiée. Export/import **vN additif** : **noms de fichiers seulement** (comme les images v6), compat ascendante, upsert non destructif. Les fichiers eux-mêmes restent dans le volume `data/uploads/` (jamais committés, jamais dans le JSON).
  - **Note** : recoupe l'item « Audios sur les mémos » plus bas — à fusionner si on fait l'audio ici. Découpage conseillé : livrer d'abord **PDF + image** (collent à l'infra actuelle), puis **audio**, puis **texte**.
  - [x] **[ATTACHMENTS-QUICKVIEW] Aperçu rapide depuis la card** → **✅ FAIT [V22.0.70]** : badge 📎 n cliquable → pop-in `attachQuickView` (partial, `<dialog>` à la volée, réutilise `renderAttachments`), 3 pages. `REALISATION.md`.
  - [x] **[ATTACHMENTS-COMMENT] Auto-commentaire ajout/suppression** → **✅ FAIT [V22.0.71]** : `_attach_log_comment` insère un commentaire normal (compte 💬, déclenche 🔔 si invité) sur les 4 sites owner/invité. `REALISATION.md`.
  - [x] **[ATTACHMENTS-BTN-STYLE] Boutons au style de l'app (monochrome)** → **✅ FAIT [V22.0.72]** : `attachRow` passe de `.prio-btn`+emojis à `.iv-btn`/`.iv-del` (⤢ / ⤓ / ✕), overrides ad-hoc retirés. `REALISATION.md`.
  - [x] **[PHOTO-BATCH-DOWNLOAD] Téléchargement d'un lot en `.zip` (mémo + dossier)** → **✅ FAIT [V22.0.73]** : pop-in de choix (photos seules / toutes pièces jointes ; « inclure les sous-dossiers » pour un dossier), zip streamé sur fichier temporaire (`ZIP_STORED`, jamais en RAM), routes owner + invités (`/share/*`, scope revalidé — invariant 5), arbre `SousDossier/TitreMémo/fichier` assaini + RFC 5987, filtre « Photos » = union `attachments` image/* + `memos.images`. Boutons monochromes `.iv-btn ⤓` (mémo, 3 pages via `renderAttachments`) + « ⤓ Télécharger » (dossier : board owner / tuile share / en-tête hub). Détail : `REALISATION.md` [V22.0.73]. Aucun changement d'export (rejoint v22). Groupé au commit [ATTACHMENTS].

- **Bookmarklet "ajouter à mon dash"** : un lien à glisser dans la barre de favoris qui ouvre `dash/?add=<url>&title=<titre>` pré-rempli — capture d'un lien depuis n'importe quelle page.
- **Historique de disponibilité** : stocker les résultats du status check (table `status_history`) et afficher un mini sparkline uptime sur chaque card. Le Zimaboard devient aussi un mini-monitoring.
- **Épingler des liens** : section "Favoris" en haut, toujours visible quelles que soient les catégories.
- **[NOTIFY-EMAIL] Notifications d'activité par e-mail** : maintenant que la **plomberie SMTP existe** (`.env` + `_smtp_config()` + envoi de [HUB-EMAIL-INVITE] V19.8.14), on peut prévenir les gens par mail sur les événements clés :
  - **Mention `@` ou assignation** d'une personne (mémo, commentaire) → mail à la personne mentionnée/assignée.
  - **Nouveau commentaire** sur un mémo → mail aux assignés du mémo + (si commentaire d'un invité) au propriétaire.
  - **Création d'un sous-projet / d'un mémo** dans un partage → mail au propriétaire (et/ou aux membres concernés).
  - (extensible : modification d'un partage, échéance proche, etc.)
  - **Résolution des destinataires** : n'envoyer qu'aux personnes ayant un **e-mail connu** (invités approuvés via `share_guests` / `guest_hubs`, propriétaire via Paramètres) — les assignés en **texte libre sans e-mail** sont ignorés. **Jamais à un destinataire arbitraire** (réutilise la règle « destinataire forcé » de [HUB-EMAIL-INVITE], invariant 5).
  - **Anti-spam** : ne pas notifier l'**auteur** de sa propre action ; **throttle / regroupement** (immédiat vs **digest** quotidien, par personne) ; idéalement des **préférences de notif** par invité/owner (quels événements, immédiat/digest, opt-out) — table additive `notify_prefs` ou dans `app_state`, **export additif vN** si on persiste les préférences.
  - **Liens dans le mail** : pointer vers le **hub** (`/share/hub/<token>`) ou le `/share/<token>` concerné, pas une ressource hors périmètre.
  - Réutilise le fil **🔔 Activité** existant comme source d'événements. Secret SMTP hors base/export/front/logs (déjà le cas). Lot **feature** (probablement V19.9/V20) ; à scoper (immédiat vs digest, préférences) avant build.
- **[TABLES] Tableaux dans les mémos** (idée Fabien) : pouvoir insérer des **tableaux** dans le contenu d'un mémo. Quill 2 n'a pas de module table simple natif → options à trancher : (a) module table Quill **auto-hébergé** dans `static/` (invariant 6, pas de CDN), (b) `<table>` HTML éditable maison, ou (c) un **type de mémo « tableau »** (grille structurée). Le contenu reste du **HTML dans `content`** (compat ascendante, pas de bump d'export si on reste en HTML). Attention au rendu **card** (clamp 3 lignes) et au **mobile** (scroll horizontal). À scoper avant build.
- **Thèmes** : clair/sombre faits ; reste une palette **AMOLED noir** (et éventuellement le suivi auto du thème système via `prefers-color-scheme`).
- **Vue graphe / constellation des tags** : un force-directed graph (lib `force-graph` de vasturiano, 2D canvas, à auto-héberger dans `static/` — invariant 6) montrant les liens ↔ mémos ↔ projets reliés par leurs `#tags` communs (et éventuellement les assignés). Clic sur un nœud = ouvrir/filtrer. À scoper comme **explorateur de relations** (surtout les tags, qui sont du many-to-many), pas comme un dump générique du graphe — l'arbre de projets se lit déjà en arbre. Voir discussion : utile si ciblé sur les tags.

## Plus ambitieux

- **PWA** (redemandé sur le dash prod, mémo « Compliqué ») : manifest + service worker → installable sur mobile, icône sur l'écran d'accueil, cache hors-ligne de la dernière vue. **Important (demande Fabien)** : gérer le hors-ligne en écriture — file d'attente locale (outbox IndexedDB) des modifications faites sans réseau, synchronisées automatiquement au retour de la connexion (cocher des courses dans un magasin sans 4G).
- **Audios sur les mémos** : bouton 🎤 (MediaRecorder), upload validé par signature comme les images, lecteur sur la card. Sans transcription (Zimaboard trop léger pour Whisper) — pièce jointe vocale simple.
- **Widgets de services** : pour certains services connus, afficher une info en plus du statut (espace disque du NAS, nombre de téléchargements en cours, capteurs Home Assistant via leurs APIs).
- **Multi-profils** : page d'accueil différente par navigateur/contexte (perso / boulot) via un paramètre `?profile=`.
- **Espace multi-utilisateur (via Authelia)** : de vrais comptes, chacun son dashboard, **sans casser l'invariant 5 (pas d'auth dans l'app)** — l'app lirait le header `Remote-User` qu'Authelia transmet déjà pour scoper les données. Implique de scoper **chaque** table par `owner_id` (liens, catégories, mémos, projets, priorités, shares…), de porter le scope dans l'export/import, et de le faire respecter partout. Gros chantier qui change la nature du produit (de « mon dashboard partagé » à « N dashboards ») — à ne lancer que s'il y a réellement plusieurs utilisateurs indépendants à servir ; sinon le système de partage/invités couvre déjà la collaboration.
- **Notifications push (PWA)** : en complément des notifs e-mail. Léger pour le Zimaboard (POST HTTPS vers le service push d'Apple/Google via `pywebpush`/VAPID, pas de connexion persistante). ⚠️ sur iPhone, push uniquement si le PWA est installé sur l'écran d'accueil (iOS 16.4+), et possible restriction UE (DMA) — à tester sur l'appareil avant de s'y fier ; les notifs e-mail restent le repli universel.
- **Backup automatique planifié** : un cron dans le conteneur qui poste l'export JSON quotidien dans un dossier `data/backups/` avec rotation (7 jours), pour ne plus dépendre de l'export manuel.

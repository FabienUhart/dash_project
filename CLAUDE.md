# === LANGUE ===

**Toujours répondre en français**, quelle que soit la langue des skills, prompts, plugins ou specs (AIDD et autres sont en anglais — ça ne change rien : la conversation avec l'utilisateur reste en français). Le code, les noms de variables et les messages de commit techniques peuvent rester en anglais si c'est la convention du projet, mais **toute explication adressée à l'utilisateur est en français**. Cela vaut **tout le temps** : récapitulatifs de fin de réalisation, messages d'avancement, plans, questions de clarification — **tout le dialogue avec l'utilisateur est en français**, sans exception.

# === MEMORY BANK ===

Une banque de mémoire vit dans `.claude/memory/`. Avant toute réponse importante ou modification de code :
1. Lire `.claude/memory/MEMORY.md` (résumé, stack réelle, état du projet, pointeurs).
2. Lire `.claude/memory/patterns.md` si pertinent (conventions transverses).
3. Consulter les ADR dans `docs/adr/`.

Ce `CLAUDE.md` reste la **source canonique** (invariants, format d'export, historique). La Memory Bank l'**indexe**, elle ne la duplique pas. Après une grosse réalisation ou décision d'archi, proposer une mise à jour de la Memory Bank ET de ce fichier, sans recopier le contenu de l'un dans l'autre.

# CLAUDE.md

Contexte pour Claude Code. À lire avant toute modification ou commit.

## Le projet en une phrase

Dashboard perso auto-hébergé sur un Zimaboard (page d'accueil navigateur) : liens vers services self-hosted avec catégories, mémos post-it, statuts online/offline, recherche, horloge/météo.

## Architecture

- **`app.py`** : tout le backend. Flask + SQLite, pas d'ORM, pas de blueprint. La migration de schéma est dans `init_db()` (ALTER TABLE additifs + backfill, jamais destructif — ne JAMAIS dropper de colonne ou de données).
- **`templates/index.html`** : tout le frontend en un seul fichier (CSS + HTML + JS vanilla, pas de framework, pas de build). Layout "3 zones" : sidebar catégories / cards / colonne mémos.
- **`data/dashboard.db`** : SQLite, monté en volume Docker. Contient les vraies données de l'utilisateur — ne jamais la modifier ou supprimer dans un commit.
- **`data/uploads/`** : images des mémos (noms `uuid4().hex.ext`, validés par regex côté serveur). Même volume Docker, même règle : ne jamais y toucher dans un commit. Le JSON d'export ne contient que les noms de fichiers.
- **`data/uploads/derived/`** : images DÉRIVÉES [IMAGE-THUMBS] générées par **Pillow** — `t_<nom>.jpg` (vignette ~400 px : cards, bande de vignettes, calque photo carte, section 📷) et `s_<nom>.jpg` (taille écran ~1600 px : image de la visionneuse), JPEG q82, orientation EXIF appliquée. **Originaux jamais modifiés** ; le bouton ⬇ télécharge toujours le brut. **Donnée dérivée re-calculable** depuis les originaux → **jamais exportée** (l'export ne porte que les noms d'images originales, compat v23 inchangée), **jamais à toucher dans un commit** (comme `data/uploads/`). Servies via **`?size=t|s`** sur les routes image EXISTANTES (`/uploads/<nom>`, `/share/<token>/image/<nom>` — scope invité inchangé, invariant 5) ; sans param, valeur inconnue, GIF, image plus petite que la cible, ou échec Pillow → **original servi** (jamais de 404). Génération à l'upload (`_save_uploaded_image`) + backfill daemon idempotent (`_backfill_derived`) ; purge en cascade avec l'original (`_delete_derived` via `_delete_image_files` + les 2 suppressions per-image).
- Déploiement : `docker compose up -d --build` (gunicorn, port 8099, derrière Caddy + Authelia en prod).

## Invariants à respecter

1. **Compat ascendante des sauvegardes** : `/api/import` doit toujours accepter les exports v1 (liens seuls), v2 (+ catégories/mémos), v3 (+ uid/dates), v4 (+ done/due_date/priority/subtasks sur les mémos, color sur les catégories), v5 (+ projets de mémos, rattachés par nom), v6 (+ images sur les mémos, noms de fichiers seulement), v7 (+ tags sur les liens, normalisés minuscules sans #), v8 (+ tags sur les projets), v9 (+ `recurrence` sur les mémos et liste `history` des tâches effectuées, dédupliquée par (memo_uid, done_at)), v10 (+ liste `priorities` ; `memos.priority` est remappé par NOM de priorité à l'import, jamais par id brut) v11 (+ `emoji` sur catégories, projets et mémos), v12 (+ `parent` sur les projets, résolu par NOM en deux passes à l'import, jamais par id), v13 (+ `location` JSON {lat, lng, label} sur mémos et projets, validée par `_clean_location()` — coordonnées bornées, jamais de HTML) v14 (+ `title` et `assignees` (liste de noms libres, dédupliquée, nettoyée par `_assignees_json()`) sur les mémos, `description` sur les projets, liste `comments` à la racine — rattachés par `memo_uid`, dédupliqués par (memo_uid, created_at, author) ; un mémo peut avoir un titre SANS contenu : la validation est « titre OU contenu », et la dédup sans uid utilise le titre quand le contenu est vide) et v15 (+ `priority` (0-3) et `parent_created_at` sur les `comments` — le `parent_id` est résolu à l'import en rattachant le commentaire dont le (memo_uid, created_at) correspond, donc les réponses survivent ; les mémos en corbeille — `deleted_at` non vide — sont **exclus** de l'export ; le statut « vu » (`comment_seen`) n'est PAS exporté) et v16 (+ `marker_color` sur les mémos ET les projets — couleur perso du point sur la carte, validée par `_clean_hex_color()` → `#rrggbb` strict ou `''`, jamais de HTML ; `''` = pas d'override, on retombe sur priorité/projet/défaut, ce qui garantit la compat ascendante : tous les exports v1→v15 sans le champ rendent à l'identique ; côté partage, l'override est exposé sous la clef `point_color` pour ne pas le confondre avec le `marker_color` global = couleur par défaut de la carte) et v17 (+ `map_groups` sur les mémos — liste JSON d'étiquettes de groupe de carte, nettoyée/dédupliquée par `_map_groups_json()` comme `assignees`, max 30, jamais de HTML ; absent = `[]`, garantit la compat ascendante v1→v16 ; pas de table séparée : un groupe est « par projet » car on ne le voit que parmi les points du projet courant sur la carte) et v18 (+ `due_time` sur les mémos — heure optionnelle `"HH:mm"` 24 h ou `''`, validée par `_clean_due_time()` (regex stricte → `''` sinon, jamais de HTML) ; dépend de `due_date` : une heure sans date n'existe pas, et vider la date vide l'heure ; absent = `''`, garantit la compat ascendante v1→v17 ; upsert non destructif comme `marker_color` — newer-wins n'écrase jamais une heure présente par une absente/plus ancienne ; émis automatiquement via `_memo_dict`) et v19 (+ `created_by` sur les mémos — identité du créateur : `''` = le propriétaire (résolu à l'affichage via `_owner_name(db)` → `created_by_display`, survit au renommage owner, comme l'auteur `moi` des commentaires), chaîne non vide = invité « Nom <email> » posée à la création via `/share/<token>/memos` ; absent à l'import = `''`, garantit la compat ascendante v1→v18 ; upsert non destructif (newer-wins n'écrase jamais un `created_by` non vide par un vide/plus ancien) ; `created_by_display` est runtime-only, **jamais exporté** — seul `created_by` brut l'est ; **owner-only** : non exposé aux invités dans `_share_memo_dict` qui n'expose que `created_at`) et v20 (+ `is_trip` sur les projets — flag « voyage » héritable [MAP-TIMELINE] : `NULL` = hérite du plus proche ancêtre tranché, `1` = voyage, `0` = non, défaut résolu = pas voyage via `_resolve_trip()` ; exporté **BRUT** (null/0/1, jamais résolu) ; absent à l'import (v1→v19) = `NULL` = hérite → rendu identique, compat ascendante garantie ; **upsert non destructif** : un `0/1` local n'est JAMAIS écrasé — ni par un absent/NULL, ni par une autre valeur, seul un existant `NULL` s'enrichit ; côté invités, `share_data`/hub `/data` exposent uniquement le booléen **`trip` RÉSOLU par le serveur** (l'arbre du partage peut être tronqué au scope), **lecture seule** — le flag n'est PAS éditable via `/share`, même `can_edit`) et v21 (+ `reactions` sur les `comments` — réactions emoji [COMMENT-REACTIONS] : liste JSON `[{emoji, voter, created_at}]` par commentaire, table additive `comment_reactions (comment_id, emoji, voter, created_at, UNIQUE(comment_id, emoji, voter))`, `CREATE TABLE IF NOT EXISTS` — jamais destructive ; **palette CONFIGURABLE** [REACTION-PALETTE] : whitelist serveur data-driven `_reaction_palette(db)` (config owner dans `app_state.reaction_emojis`, seed = 6 base `REACTION_EMOJIS = 👍 👎 ❤️ 😂 😮 🎉`, l'owner **ajoute/retire**), la réaction est validée contre cette palette (tout autre → 400) ; **ajout d'emoji validé STRICTEMENT** par `_clean_reaction_emoji` (un seul grapheme emoji, Python pur — plages Unicode + accessoires ZWJ/VS/teinte/keycap/régionaux + garde ≤ 10 cp, zéro lib/CDN — invariant 6 ; rejette texte/HTML/2-emojis → 400) ; palette **exportée avec les réglages** (top-level `reaction_emojis`, émise seulement si custom → compat v20/v21 sans palette = base par défaut, aucun crash ; import restauré si aucune config locale, non destructif) ; **votant** = pattern `created_by` — `voter=''` = propriétaire (résolu à l'affichage via `_owner_name` → `voters`/`mine`, survit au renommage, comme `created_by`), non vide = invité « Nom <email> » ; **toggle** par (commentaire, emoji, votant) via `UNIQUE(...)` ; exporté **BRUT** (`voter` verbatim, jamais résolu), absent à l'import (v1→v20) = aucune réaction = rendu identique, compat ascendante garantie ; import **additif non destructif** : `INSERT OR IGNORE` sur la clé unique (ré-import v21 = 0 doublon), commentaire résolu par (memo_uid, created_at, author), emoji hors palette ignoré ; **purge en cascade** (suppression commentaire + purge définitive mémo suppriment les réactions AVANT les commentaires) ; côté invités, réagir exige un invité **approuvé** mais **PAS `can_edit`** (réagir ≠ éditer) via `POST /share/<token>/comment/<id>/react` (scope revalidé, invariant 5), `voters` expose le **nom seul** (e-mail owner-only) ; les réactions **ne comptent pas** dans le badge d'activité 🔔 ; `comment_seen` reste non exporté) et v22 (+ `attachments` sur les mémos — pièces jointes de TOUT type [ATTACHMENTS] : liste JSON `[{filename, orig_name, mime, size, preview, created_at, created_by}]` par mémo, table additive `attachments (id, memo_id, memo_uid, filename, orig_name, mime, size, preview, created_at, created_by, share_id)`, `CREATE TABLE IF NOT EXISTS` — distincte de `memos.images` (photos EXIF/carte, inchangées) ; **binaire jamais dans le JSON**, noms de fichiers seuls (comme les images) — les fichiers vivent dans `data/uploads/` ; **`preview`** = type « aperçu sûr » (image/pdf/audio validé par SIGNATURE binaire) → aperçu inline possible, **tout le reste = téléchargement forcé** (`Content-Disposition: attachment`, jamais inline — anti-XSS, aucun HTML/SVG/script rendu) ; **`created_by`** = pattern `created_by` v19 (`''` = owner, sinon « Nom <email> » pour un upload invité) ; export **BRUT** (voter/filenames verbatim), absent à l'import (v1→v21) = aucune pièce jointe = rendu identique, compat ascendante garantie ; import **additif non destructif** : n'ajoute que les fichiers dont le **binaire EXISTE** sur le volume (tolérant comme les références d'image orphelines) et pas déjà rattachés (dédup par (memo_id, filename)) — ré-import = 0 doublon ; **purge en cascade** (`_purge_memo_row` supprime fichiers + lignes) ; taille max **300 Mo/fichier** (`MAX_CONTENT_LENGTH` = 320 Mo, gunicorn `--timeout 300`) ; côté invités, upload sous **`/share/<token>/memo/<id>/attachments`** (bypass Authelia, invariant 5 — invité **approuvé + `can_edit`** requis, scope revalidé) et download `/share/<token>/attachment/<id>` (tout invité ayant accès, scope revalidé) ; owner `/api/memos/<id>/attachments` + `/api/attachments/<id>` (derrière Authelia)) et v23 (+ `attachments` sur les **projets** [FOLDER-ATTACHMENTS] — une pièce jointe peut viser un DOSSIER au lieu d'un mémo : colonne additive `attachments.project_id` (nullable ; rempli = attache de dossier avec `memo_id = 0` puisqu'aucun mémo n'a l'id 0 → invisible des requêtes mémo `WHERE memo_id = ?` ; NULL = attache de mémo v22 inchangée), même stockage `data/uploads/` (noms de fichiers seuls dans le JSON, jamais le binaire), même validation par signature et même service inline/download anti-XSS que les mémos ; **export niché par projet** (clef `attachments` dans chaque entrée de `projects`, rattachée par NOM à l'import comme le reste d'un projet), absent à l'import (v1→v22) = aucune pièce jointe de dossier = rendu identique, compat ascendante garantie ; import **additif non destructif** (`_import_project_attachments` : binaire présent + dédup `(project_id, filename)`, ré-import = 0 doublon) ; **purge en cascade** à la suppression du dossier (`delete_project` supprime fichiers + lignes) ; routes owner `GET/POST /api/projects/<id>/attachments` (derrière Authelia) et invité `POST /share/<token>/project/<id>/attachments` (bypass `/share/*`, **partage de projet uniquement**, dossier dans le sous-arbre autorisé, invité **approuvé + `can_edit`**, scope revalidé — invariant 5) ; download/delete réutilisent `GET`/`DELETE /api/attachments/<id>` (owner) et `/share/<token>/attachment/<id>` (invité, scope élargi par `_attach_in_share_scope` = mémo dans le scope OU dossier descendant d'un partage de projet). **[FILES-VIEW]** ajoute des vues de recherche (agrégation `_collect_files` : fichiers de dossier + de mémo + images legacy avec **fil d'Ariane cliquable** `source`) — routes **lecture** owner `GET /api/projects/<id>/files?subtree=1` + global `GET /api/files`, invité `GET /share/<token>/project/<id>/files` (borné au sous-arbre du token) — **données dérivées, RIEN de nouveau dans l'export**) et **v24** (+ `due_end` sur les mémos [MEMO-DATE-RANGE] — **date de FIN** de plage `YYYY-MM-DD` ou `''` : hôtels multi-nuits, événements multi-jours. **PREMIER bump depuis v23** : `due_end` est une **donnée utilisateur réelle EXPORTÉE** (≠ `end_time`/voix qui restent non exportés). Validée `_clean_due_end()` (regex stricte → `''`, jamais de HTML) + garde-fous : exige `due_date`, `due_end ≥ due_date`, **exclusive de la récurrence** (400 explicite côté écriture ; effacer la date efface la fin). Absente à l'import (v1→v23) = `''` = mémo mono-jour → **rendu identique, compat ascendante garantie** (invariant 2). Import **tolérant** (une fin invalide — sans date, < début, ou avec récurrence — est ignorée, jamais de 400) et **non destructif** (une fin présente n'est pas écrasée par une absente). Émise via `_memo_dict` ; exposée aux invités (`_share_memo_dict`), éditable selon la matrice de rôles ([GUEST-ROLES]). UI : champ « → fin » dans la pop-in (3 pages, visible si date posée et pas de récurrence), badge « 3 → 6 nov » sur cards/plan/agenda, « En retard » basé sur la **fin effective** (un séjour en cours n'est pas en retard), agenda/frise voyage : le mémo couvre **chaque jour** de sa plage (point carte UNIQUE). `APP_VERSION="24"`) et **v25** (+ `uid`/`parent_uid` sur les projets et `project_uid` sur les mémos [PROJECT-NAME-PER-FOLDER] — **unicité des noms de dossiers PAR DOSSIER PARENT** (plus globale) : deux dossiers de même nom ne peuvent être **frères** (même `parent_id`, racine incluse, `COALESCE(parent_id,0)` — les NULL SQLite sont distincts), mais « restaurants » sous « Japon » ET sous « Corée » est désormais possible. Le nom n'étant plus une clef fiable, chaque projet porte un **`uid` stable** (invariant 3, backfillé par `init_db()`, jamais régénéré) via un **rebuild de la table `projects`** (SQLite ne retire pas un UNIQUE : rename → create sans UNIQUE(name) → port en préservant les `id` → `CREATE UNIQUE INDEX ux_projects_name_parent`, idempotent). **Résolution import UID-D'ABORD** puis par **(nom, parent résolu)** — plus jamais par nom à plat : création **topologique** (parent résolu AVANT l'insert → aucune collision « tout à la racine ») ; les mémos rattachés par `project_uid` puis `project` (nom). Absents à l'import (v1→v24) = résolution (nom, parent) : les vieux exports avaient des noms globalement uniques → chemin toujours résolvable → **rendu identique, compat ascendante garantie** (invariant 2). Upsert par uid **non destructif** : le nom LOCAL n'est **jamais écrasé** (les projets n'ont pas d'`updated_at` → « newer-wins » indécidable, on ne détruit pas un renommage local) ; ré-import d'un export v25 = **0 ajout, 0 doublon**. `name`/`parent` (nom) **restent émis** (lisibilité + compat). **Collision au DÉPLACEMENT (D4)** owner comme invité → **409** « un dossier de ce nom existe déjà à cet endroit » (jamais de 500), garde unique `_sibling_name_taken`. [EXPORT-SUBTREE] doublé de `proj_uids`/`project_uid` ; « Importer ici » évalue « racine nouvelle » par uid-puis-chemin. Provisioning d'espace invité et suffixe de dédup **re-scopés au parent**. `APP_VERSION="25"`) et **v26** (+ `created_by` sur les **projets** [GUEST-HOME] — identité du créateur d'un dossier : `''` = le propriétaire, chaîne non vide = invité « Nom <email> » (dossier perso). **Donnée utilisateur réelle désormais EXPORTÉE** : sans elle, après restauration sur base neuve, la dédup GLOBALE par e-mail et l'élévation par propriété (`_owns_guest_space_folder`) seraient perdues → une ré-approbation créerait un espace **dupliqué**. Import : restaure `created_by` (résolution **uid-d'abord** comme v25) — **création** (INSERT direct) et **enrichissement non destructif** (n'écrit que si l'existant est vide, jamais d'écrasement). Absent à l'import (v1→v25) = `''` → **rendu identique, compat ascendante garantie** (invariant 2). Les partages/`share_guests` restent NON exportés (secrets) : après restauration, une ré-approbation **raccroche** l'espace existant via la dédup `created_by`. `APP_VERSION="26"`) et **v27** (+ liste top-level `memo_links` — **liens symétriques entre mémos** [MEMO-LINKS] : table additive `memo_links (src_memo_id, dst_memo_id, created_at, created_by)`, `CREATE TABLE IF NOT EXISTS` — jamais destructive ; **une seule ligne par paire NON ORDONNÉE** (index unique sur `CASE WHEN src<dst…`, déterministe) : le lien est **symétrique en effet** (visible/suivable des deux côtés) mais **orienté en mémoire** (`src` = le mémo depuis lequel il a été posé, pilote le style des chips) ; auto-lien **400**, plafond **20 liens/mémo** (400 explicite), `created_by` = pattern v19 (`''` = owner, sinon « Nom <email> ») ; mémo en **corbeille** → ses liens sont **masqués partout mais conservés** (restauration = ils reviennent), **purge définitive** → liens supprimés en cascade (`_purge_memo_row` + `_purge_trash`). Exporté **par UID** (`[{src_uid, dst_uid, created_at, created_by}]`, jamais d'id) et borné aux mémos exportés (sous-arbre [EXPORT-SUBTREE] inclus) ; import **uid-d'abord TOLÉRANT** (uid inconnu → lien ignoré, jamais de 400), **dédup par paire non ordonnée** (ré-import v27 = 0 doublon), **additif non destructif** (un lien local n'est jamais retiré). Absent à l'import (v1→v26) = aucun lien = **rendu identique, compat ascendante garantie** (invariant 2). Le champ `links` des payloads mémo est **runtime-only, JAMAIS exporté** (comme `created_by_display`) et **borné au scope du lecteur** — un lien vers un mémo hors scope d'un invité est **omis** (jamais de titre-fantôme, invariant 5). `APP_VERSION="27"`). Toute évolution du format incrémente `version` dans l'export et reste importable.
2. **L'import n'est jamais destructif** : il ajoute, met à jour (uid identique + `updated_at` plus récent) ou enrichit les champs vides (match nom+URLs sans uid). Il ne supprime ni n'écrase jamais un champ rempli avec une donnée plus ancienne.
3. **`uid`** : UUID stable qui suit chaque lien/mémo à travers exports/imports. Généré à la création et backfillé par `init_db()`. Ne jamais le régénérer pour une ligne existante.
4. **Les URLs locales** (`192.168.1.x`) servent au check de statut ET à la récupération des favicons côté serveur (contournement d'Authelia). Le cache favicon (`_favicon_cache`, en mémoire) doit être invalidé quand les URLs d'un lien changent.
5. **Pas d'auth dans l'app** : la sécurité est assurée par le reverse proxy. Ne pas ajouter de login. **Exception volontaire** : les routes publiques invité vivent **toutes sous le préfixe `/share/...`** — le partage `/share/<token>/...` ET le hub `/share/hub/<hub_token>/...` (hub agrégé ; **déplacé de `/g/` vers `/share/hub/` en V19.8 précisément pour réutiliser le bypass `/share/*` existant**, sans toucher à Caddy). Elles sont publiques via la règle bypass Authelia `/share/*` (déjà en place) et protégées uniquement par le jeton (+ code PIN) — elles ne doivent JAMAIS exposer autre chose que la ressource partagée (mémo ou projet ciblé, ses sous-tâches et ses images). Toute écriture via `/share/` exige en plus un invité **approuvé** (header `X-Guest-Token`, approbation obtenue par le code PIN du lien). **Toute nouvelle route publique invité DOIT vivre sous `/share/...`** (pour être couverte par le bypass `/share/*` sans toucher au reverse proxy) et rester dans ce périmètre — **ne JAMAIS créer un préfixe de premier niveau** (ex. l'ancien `/g/`), ce qui obligerait à éditer la config Caddy du Zimaboard (HORS de ce repo) et a déjà piégé `/g/*` à sa sortie. **Vérification après tout ajout de route publique** : une requête **non authentifiée** (`fetch(path, {credentials:'omit', redirect:'manual'})`) doit **atteindre l'app** (réponse `basic`, ex. 404) et **pas** être redirigée vers Authelia (`opaqueredirect`/302). Les uploads d'images (propriétaire comme invité) passent par `_save_uploaded_image()` qui vérifie la **signature binaire** du fichier — ne jamais accepter un upload sans cette vérification.
6. **Sortie HTML auto-contenue, sans build** : la **page livrée** reste un HTML autonome (CSS + JS inlinés, pas d'étape de build, pas de dépendance runtime externe). Au niveau **source**, les **helpers purs et identiques** owner/invité vivent dans **`templates/partials/_shared.js.html`** (un seul `<script>`), inclus dans `index.html` ET `share.html` via `{% include 'partials/_shared.js.html' %}` au rendu Jinja — donc inlinés dans la sortie, sans asset servi à part (pas de `/share/assets`, invariant 5 non impacté), réf. **ADR-001 (Option C)** dans `docs/adr/`. **Règle** : n'y mettre que des fonctions **sans état, sans dépendance à `state`/`DATA`, et au corps strictement identique entre les deux pages** (vérifier par diff avant d'extraire) ; jamais de logique élargissant le périmètre invité. Le rendu lié aux données (`renderSidebar`, carte, vue Plan…) reste, lui, dupliqué pour l'instant (chantier ADR-001 items 4/6). Hors de ce partial : pas de séparation CSS/JS du code maison. Dépendances front autorisées uniquement si **auto-hébergées dans `static/`** (actuellement : Quill 2 pour l'édition riche des mémos — dégrade en textarea si le fichier manque ; `gsap.min.js` pour les animations — dégrade proprement si absent ; `leaflet.js`/`leaflet.css` ; **Leaflet.markercluster** (`leaflet.markercluster.js` + `MarkerCluster.css` + `MarkerCluster.Default.css`, vendorisé 1.5.3, [PHOTO-CLUSTER] — clustering du calque photo de la carte, dégrade en marqueurs simples si absent) ; **quill-table-better** (`quill-table-better.js` + `quill-table-better.css`, vendorisé 1.2.3 MIT, [MEMO-TABLES] — tableaux dans l'éditeur Quill des mémos, activé par le helper partagé `applyTableBetter()` du partial ; absent = no-op, éditeur sans bouton ⊞ et tableaux existants toujours rendus par Quill 2 core) ; `favicon.svg`). Pour les invités, ces fichiers sont servis par la route publique `/share/assets/<nom>` (liste blanche `SHARE_ASSETS`) car `/static/` est derrière Authelia. Pas de CDN au runtime, sauf les appels existants : open-meteo, icons.duckduckgo.com (fallback favicon), nominatim.openstreetmap.org (géocodage à la sauvegarde uniquement, échec silencieux), tile.openstreetmap.org (tuiles de la carte Leaflet, auto-hébergée dans `static/` : `leaflet.js` + `leaflet.css`, marqueurs vectoriels `circleMarker` donc aucune image Leaflet requise) et **api.frankfurter.app** (taux de change BCE du convertisseur ¥€ [FX-CONVERTER] — **appel SERVEUR uniquement, 1 fetch/jour maximum**, cache `app_state.fx_cache`, échec silencieux → dernier cache ; jamais depuis le navigateur).
7. **Suppression douce** : supprimer un mémo (UI propriétaire OU invité) ne fait JAMAIS de `DELETE` SQL — ça pose `memos.deleted_at` (corbeille). Les mémos en corbeille sont exclus de `/api/memos`, du scope partage et de l'export. La purge définitive (fichiers images + shares + commentaires) n'arrive QUE via la corbeille (routes `/api/trash`) ou la purge auto `_purge_trash()` après `BACKUP_KEEP_DAYS` jours. La restauration owner-only est `/api/trash/<id>/restore` (≠ `/api/memos/<id>/restore` qui restaure une *révision*).
8. **Animations** : ne JAMAIS animer un élément `<dialog>` lui-même via GSAP (`y`/`scale`/`autoAlpha`) — poser un transform GSAP sur un dialog le sort du top-layer Chromium et casse le centrage + le `::backdrop`. L'entrée des pop-ins est gérée en CSS (`@keyframes dlg-in`/`dlg-bd` sur `dialog[open]`, sous `prefers-reduced-motion`). GSAP n'anime que des éléments hors top-layer (cards, sidebar, tuiles) ou des enfants d'un dialog, jamais le dialog.
9. **Cohérence visuelle — réutiliser les classes existantes, pas de styles bespoke** : toute nouvelle UI (boutons, chips, pastilles, pop-ins) doit **réutiliser les classes/tokens du système existant** plutôt que d'inventer ses propres styles. Référence = les **cards** (« Voyage Japon ») : boutons d'action ronds via le style `.task .task-actions button` (rayon `9px`, `var(--panel-2)`/`var(--border)`, hover `var(--panel)`/`var(--muted)`, **action principale en accent** comme `.texp`/`.tedit`) ; petits chips/filtres via `.prio-btn` (sélectionné = `background:var(--accent);color:#10141a`) ou `.mfilter` ; pastilles texte via `.badge` ; suppression en danger (`var(--red)`, cf. `.thumb-del`). Couleurs **toujours** via les variables CSS (`--accent`, `--panel`, `--border`, `--muted`, `--red`…), jamais en dur. Si un composant a besoin d'un style à part (ex. `.iv-btn`, overlays sur photo), il **s'aligne** sur ces tokens et reste une exception justifiée — pas un nouveau langage visuel. But : une seule source de vérité du look, owner ET invité (composants partagés dans le partial — réf. ADR-001). **Boutons → style « Luciole »** (`docs/design/LUCIOLE-boutons.md`, baptisé le 30 juil. 2026) : **jamais d'aplat accent plein sur le chrome** (la couleur pleine est réservée aux DONNÉES), **le mouvement n'appartient qu'au geste** (lueur/lift au survol seulement, zéro animation permanente), **un état persistant se dit par la MATIÈRE et le LIBELLÉ** (bordure/fond/creux + « Détails » ⇄ « Réduire »), clair et sombre chacun juste dans son monde. Toute nouvelle UI de boutons s'y réfère ; dire « bouton Luciole » dans un brief = ce doc (helpers `mabGlowButton`/`lucioleize` dans `templates/partials/_shared.js.html`, CSS par page). Le danger reste **rouge** (`button.danger`), jamais en Luciole.

## Versionnage (SemVer projet `VX.Y.Z`)

Suivi des versions en `VX.Y.Z` (ex. `V19.1.123`) :

- **X** = version du **format d'export** (= invariant 1, `APP_VERSION` / `version` de l'export). Ne bouge **que** lors d'un changement de format de données. C'est le seul numéro qui fait foi pour la compat des sauvegardes. Actuellement **27**.
- **Y** = **mineure** : incrémentée à chaque nouvelle fonctionnalité ou lot cohérent, **même si le format d'export ne change pas** (beaucoup de features sont « export inchangé »).
- **Z** = **build / identifiant de réalisation** : **compteur continu global**, incrémenté à chaque entrée de `REALISATION.md` et **jamais remis à 0**.

Règles d'usage :
1. **`IDEAS.md` (planification)** : chaque tâche/concept est ciblé à cette granularité, ex. « Feature A → prévu pour **V19.1** ».
2. **`REALISATION.md` (journal)** : chaque accomplissement est consigné avec son tag `[VX.Y.Z]` au moment de sa complétion/livraison, ex. `- [V19.1.123] Implémentation du module X.` C'est la **source de vérité du compteur Z** (prendre le dernier Z + 1).
3. `APP_VERSION` et le footer suivent **X** (le format d'export). Un changement de format = bump de X **et** d'une entrée d'invariant 1.

## Comment tester avant de commit

```bash
# syntaxe
python3 -m py_compile app.py

# lancer sur une COPIE de la base (jamais sur data/dashboard.db directement)
cp data/dashboard.db /tmp/test.db
DB_PATH=/tmp/test.db flask --app app run -p 8099

# scénarios critiques à vérifier :
# 1. la migration passe sur une base existante (l'app démarre, GET /api/links OK)
# 2. ré-import d'un export complet → 0 ajout, tout en "skipped"
# 3. import v1 sans uid → pas de doublon, champs vides enrichis
# 4. CRUD catégories/mémos + reorder
```

`backup*.json` est gitignoré (données perso) — ne jamais le committer.

### Scénarios de test spécifiques v19 (created_by)

- import v18 (sans `created_by`) → pas de doublon, `created_by` reste `''` = propriétaire ;
- mémo créé par owner → en-tête affiche le propriétaire (`created_by_display` résolu) ;
- mémo créé via partage invité (`POST /share/<token>/memos`) → en-tête owner affiche « Nom <email> » ;
- `GET /api/export` → `version: 19` + `created_by` présent sur chaque mémo (jamais `created_by_display`).

## Process de fin de réalisation

Après **chaque réalisation** (un lot livré et testé — pas à chaque tour de conversation), Claude Code doit, dans l'ordre :

1. **Tester** : `python3 -m py_compile app.py` + les scénarios critiques (voir « Comment tester »), toujours sur une **copie** de la base (`cp data/dashboard.db /tmp/test.db`), jamais sur `data/dashboard.db`.
2. **Journaliser** : ajouter l'entrée taggée `[VX.Y.Z]` dans `REALISATION.md` (Z = dernier + 1) et basculer l'item correspondant d'`IDEAS.md` en « Fait » (cf. § Versionnage).
3. **Rebuild local** : lancer **`docker compose up -d --build`** pour que la nouvelle version tourne sur `http://localhost:8099/` et soit immédiatement testable (par Fabien et par l'agent Cowork). Le rebuild local utilise la vraie base (`./data`) : la migration doit donc rester additive et non destructive (invariant 1).
4. **Ne pas déployer en prod automatiquement** : le déploiement sur le Zimaboard reste **manuel** (`git pull` + `docker compose up -d --build` côté serveur). Ne **pas** committer ni pousser sans demande explicite de Fabien.

(Option « dure » équivalente : un hook `Stop` dans `.claude/settings.local.json` qui lance le rebuild automatiquement — voir le script fourni hors dépôt. Le présent process reste la source de vérité même si le hook n'est pas activé.)

## Historique des évolutions majeures

- **v1 (origine)** : liste de liens à plat, 2 URLs par lien, statut, mémo par lien, export/import basique.
- **Juin 2026 — refonte "3 zones"** : catégories (table + sidebar + drag & drop), mémos indépendants (table `memos`, colonne post-its), recherche instantanée (`/`), favicons via serveur, horloge + météo, panneau Paramètres (export/import/gestion catégories), responsive mobile, import anti-doublons puis import avec `uid` + dates (mise à jour des éléments connus au lieu de dupliquer).
- **Juin 2026 — vue Mémos façon Planify** : page dédiée (sidebar → Mémos) avec tuiles de filtres, sections par échéance (En retard / Aujourd'hui / À venir / Sans date / Terminés repliable), cases à cocher, priorités P1-P3, sous-tâches JSON, liens cliquables, couleurs de catégories (export v4), puis projets de mémos avec Inbox, couleurs et drag & drop depuis la sidebar (export v5), puis images sur les mémos (upload dans `data/uploads/`, miniatures + visionneuse, export v6), puis tags `#` reliant liens et mémos + pop-in de création projet/catégorie avec couleur (export v7), puis tags sur les projets + recherche scopée (sélecteur Tout/Liens/Mémos/Projets + préfixes `p#` `l#` `m#`) et remplacement de tous les confirm/alert natifs par des pop-ins (export v8), puis tâches récurrentes (cocher replanifie sur le rythme prévu, `_next_due()`) + historique des tâches effectuées (table `memo_history`, tuile 📜, purge manuelle) (export v9), puis priorités configurables (table `priorities` seedée P1 rouge/P2 jaune/P3 vert, couleurs dynamiques, gestion + pop-in dans Paramètres) (export v10), puis partage externe par lien à jeton (table `shares`, routes publiques `/share/<token>`, page `templates/share.html`, lecture seule ou modifiable, bypass Authelia requis — non inclus dans l'export), puis invités identifiés par e-mail validés par le propriétaire (table `share_guests`, header `X-Guest-Token`), attribution des modifications et versions restaurables (`memo_revisions` avant/après, fil 🔔 Activité, restauration owner-only via `/api/memos/<id>/restore`), puis attribution visible (badge 👤 sur les mémos touchés par un invité, champ `guest_editor` dans `/api/memos`) + rechargement auto du dashboard toutes les 15 s (sauf pop-in ouverte ou édition en cours), puis listes à cocher façon Evernote (Quill `list: check`, `li[data-list]` cliquables sur la card ET la page partagée, champ "+ Ajouter un article" pour les invités), droits invités complets (texte, date, priorité, photos JPG/PNG validées par **signature binaire** — `_looks_like_image()`, gif/webp réservés au propriétaire), puis code PIN à 4 chiffres par lien (`shares.pin`, généré/modifiable, code correct = invité approuvé d'office) et page 🔗 Partages (vue dédiée : liens + PIN éditables, invités, modifications groupées par invité avec avant/après dépliable — remplace la pop-in Activité), enfin double-clic/clic droit sur catégories-projets de la sidebar pour ouvrir la pop-in d'édition (avec bouton Supprimer), et emojis personnalisables sur catégories, projets et mémos (champ + palette dans la pop-in, input dans le détail du mémo, affichés sidebar/cards/board/page partagée) (export v11), puis projets imbriqués (`projects.parent_id` avec anti-cycle via `_resolve_parent()`, drag & drop projet→projet dans la sidebar, arbre indenté, la vue ET le partage d'un parent incluent récursivement les mémos des descendants — avertissement à la création du lien) (export v12).
- **Juin 2026 — parité page partagée + gouvernance + backup** : footer version (`APP_VERSION`, affiché "v12 · créé par Fabien U."), la page partagée d'un projet devient un mini-dashboard pour invités validés — sidebar hiérarchique (navigation, compteurs), tuiles de filtres scopées, création de mémos (sélecteur de projet) et de **sous-projets**, drag & drop mémo→projet et projet→projet, édition de projets par double-clic/clic droit (nom/emoji/couleur, renommages journalisés), éditeur **Quill** (servi via `/share/assets/` car `/static` est derrière Authelia), notes libres par défaut avec listes à cocher opt-in (bouton ☑ Liste) — le tout strictement cantonné au périmètre du partage (routes `PUT /share/<t>/memo|project`, scope vérifié serveur). Côté propriétaire : récap des invités sous le titre des projets/mémos partagés (chips ✏️/👁), et dans la page 🔗 Partages une section **INVITÉS par personne** (tous les accès d'un e-mail, hiérarchie complète dépliée, droits, dates, copie de lien, refus/retrait ciblé). Emojis et points de couleur affichés **côte à côte** (`comboIcon()`). Enfin **sauvegardes automatiques quotidiennes** (voir section Sauvegardes), horloges multi-fuseaux (heure de Paris auto hors de France + fuseau choisi en localStorage), et **géolocalisation** sur mémos et projets (capture GPS navigateur, badge 📍 → lien OpenStreetMap — pas de carte embarquée ni de géocodage externe, invités inclus via le partage) (export v13, APP_VERSION "13").
- **Juin 2026 — labels courts + liste des points carte** : `_geocode_search()` renvoie un label court (« nom, ville » via addressdetails Nominatim ; l'adresse complète reste dans la clef `full` pour le sélecteur, filtrée par `_clean_location()` au stockage), badges plafonnés en largeur (ellipsis, adresse complète au survol), et le dialog carte (dashboard ET page partagée) gagne un panneau latéral listant les points — clic = centrage + popup.
- **Juin 2026 — v14 : titres, descriptions, assignés, commentaires** : champ `title` sur les mémos (gras sur la card, input 🏷 dans le détail et dans les outils invités, mémo valide avec titre seul), `description` sur les projets (textarea dans la pop-in projet, affichée sous le titre du board — éditable au clic — et sur la page partagée, modifiable par les invités via leur pop-in projet), **assignés multiples** `memos.assignees` (saisie libre — on peut rattacher quelqu'un qui n'est PAS encore invité et l'affilier plus tard —, suggestions datalist = « Fabien » + invités approuvés + noms déjà utilisés, badges @nom sur les cards, inclus dans la recherche), et **commentaires signés** (table `memo_comments`, fil sous le détail du mémo, badge 💬 n cliquable, routes owner GET/POST `/api/memos/<id>/comments` + DELETE `/api/comments/<id>` owner-only, route invité POST `/share/<t>/memo/<id>/comments` — can_edit + invité approuvé + scope vérifié —, auteur invité affiché en violet 👤, commentaires invités comptés dans le badge d'activité 🔔, inclus dans `share_data` et dans l'export/import v14). `share_data` expose aussi `members` (propriétaire + invités approuvés du lien) pour les suggestions d'assignés côté invité (export v14, APP_VERSION "14").
- **Juin 2026 — refonte visuelle + corbeille + QR + commentaires v2** (export v15, APP_VERSION "15") :
  - **Refonte visuelle** (tokens CSS resserrés, glass header, cards avec hover, focus-rings) et **GSAP** (`static/gsap.min.js`, self-hosté, whitelisté `/share/assets/`) : stagger des cards/tâches, count-up des tuiles, intro header/sidebar. ⚠️ l'entrée des **pop-ins est en CSS** (`@keyframes dlg-in`/`dlg-bd`), pas GSAP — voir invariant 8.
  - **Mentions `@`** : blot Quill `mention` (`<span class="mention" data-mention>`) + menu d'autocomplétion dans l'éditeur ET dans le champ commentaire (`attachInputMention`). Sélection = insère `@Nom` + ajoute aux assignés. Source = `assigneeSuggestions()` / `shareSuggestions()` (toi + invités + noms déjà utilisés, saisie libre). La pop-in « Modifier le mémo » de la page partagée a enfin un champ assignés général (`#q-people`) + un titre (`#q-title`).
  - **Signal d'accès** : un assigné/mention qui n'a pas accès au partage s'affiche en **ambre `⚠ @Nom`** (badge, puce, mention inline). Dashboard : accès = propriétaire ou invité approuvé d'un partage couvrant le projet du mémo (ou un ancêtre). Partage : accès = `DATA.members`.
  - **Corbeille** (invariant 7) : `memos.deleted_at`, vue 🗑 sidebar (Restaurer / Supprimer définitivement / Vider), purge auto 7 j (`_purge_trash`). Côté partage, l'invité approuvé peut supprimer (→ corbeille) et restaurer (tuile 🗑 ; `trash` dans `share_data`, routes `DELETE`/`POST .../restore` et `.../seen`).
  - **Ouvrir un projet à un invité existant** depuis 🔗 Partages : `POST /api/guests/grant` (réutilise un partage projet aux mêmes droits sinon en crée un, pré-approuve l'invité, renvoie lien+PIN).
  - **Favicon** `static/favicon.svg` (whitelisté partage). **Dupliquer un mémo** `POST /api/memos/<id>/duplicate` (copie tout sauf images, `done` remis à 0, titre + « (copie) »). Supprimer/Dupliquer déplacés dans un **menu ⋯** en haut de la pop-in mémo.
  - **QR code** : `GET /api/qr?data=` → SVG (lib `qrcode` ajoutée à `requirements.txt`, pure Python). Bouton ▦ sur chaque lien/accès → pop-in QR (fond blanc) + récap cible/droits/PIN/personnes.
  - **Carte** : `boardMapPoints()` utilise désormais le même filtrage que la liste (recherche + filtre + projet) → plus de point « fantôme » d'un mémo masqué.
  - **Commentaires v2** : mentions `@`, **réponses imbriquées** (`memo_comments.parent_id`, un seul niveau, `_valid_parent_comment` aplatit les réponses de réponses), **priorité** (`memo_comments.priority` 0-3, pastille rouge/ambre/vert), **accusés de lecture** (table `comment_seen`, marqué auto à l'ouverture — owner « Fabien », invité = nom/email ; endpoints `POST /api/memos/<id>/comments/seen` et `POST /share/<t>/memo/<id>/seen` ; affichage « 👁 vu par … » en excluant soi-même).
- **Juin 2026 — thème clair/sombre, sidebar repliable, pop-ins partout, fix formulaire invité** (UI/UX uniquement — **aucun changement du format d'export**, toujours v15) :
  - **Thème clair / sombre** : palette claire sous `:root[data-theme="light"]` (surcharge des tokens + des surfaces à rgba sombre en dur : header / sidebar / panneau mémos / cards), bouton 🌙/☀️ dans le header (`#theme-btn`), persisté `localStorage` clé `theme`, script inline dans le `<head>` pour l'appliquer **avant le rendu** (anti-FOUC). Présent sur `index.html` ET `share.html`. Sombre par défaut.
  - **Sidebar repliable** (réalisé via le skill `emil-design-eng`, branche `ui-modernization-gsap`) : 👁 `#sidebar-eye-btn` masque toute la sidebar (classe `sb-hidden`, largeur→0, icône → `#ic-eye-off`) ; ☰ `#sidebar-rail-btn` la replie en rail d'icônes 64px (classe `sb-rail`, `overflow:hidden` pour éviter le débordement des pastilles). Indépendants, persistés `localStorage` `sbHidden`/`sbRail`, animations GSAP backIn/backOut (sur le `nav`, jamais sur un `<dialog>` — invariant 8).
  - **Suppression projet dans un menu ⋯** : comme les mémos, le bouton Supprimer de la pop-in projet passe dans un menu `card-menu` (`#create-menu-btn` / `#create-menu`) ; le pied de page ne garde qu'Annuler / Enregistrer.
  - **Plus aucune boîte native** : tous les `alert()` restants de `share.html` remplacés par la pop-in `notify(msg, {error})` (markup `#notify-dialog` + fonction, miroir d'`index.html`). Règle : ne jamais utiliser `alert`/`confirm`/`prompt` natifs, toujours `notify()` / `confirmPopin()`.
  - **Fix formulaire d'accès invité** (`share.html`, `renderGuestBanner()`) : le refresh auto (`setInterval(load, 15000)`) reconstruisait le formulaire à chaque tick et **effaçait la saisie** (prénom/e-mail/code). Garde : si l'invité est anonyme et que `#guest-form` existe déjà, on **ne reconstruit pas**. Mauvais PIN → erreur **inline** (`#guest-error`) au lieu d'un `alert()`, **sans vider** prénom/e-mail (seul le code est remis à zéro + refocus). Validation à la touche Entrée.
- **Juin 2026 — passe responsive mobile** (UI/UX uniquement, `index.html` + `share.html`, **aucun changement d'export**, toujours v15) :
  - **Barre de projets/catégories collante** en mobile (`@media max-width:900px`) : la sidebar repliée en rangée horizontale devient `position: sticky` épinglée sous le header. Le header se replie sur plusieurs lignes (hauteur variable) → mesurée en JS dans `syncHeaderHeight()` qui pose `--header-h` (sur load, resize, ET à chaque `renderAll()`/`render()` car le bouton « + Ajouter » masqué hors vue Liens change la hauteur du header).
  - **Bouton « + Ajouter » du header masqué hors vue Liens** (`renderAll()` : `add-link` en `display:none` si `state.view !== 'links'`) — il ne crée que des liens, les vues Mémos/Projet ont leur propre barre d'ajout.
  - **Bouton ✎ Modifier** ajouté dans l'en-tête du projet (`renderBoard`, à côté de Partager) → `openCreateDialog('project', proj)`. Le double-clic/clic droit sur la sidebar reste, mais est inutilisable au tactile.
  - **Mémos en mobile** : badges (échéance, @assignés, projet, lieu) passés sur leur **propre ligne** sous le texte (`flex-wrap` sur `.task-row`, contenu `flex:1 1 100%`, badges `flex-basis:100%`) — sinon la colonne de texte s'effondre à 1 caractère/ligne. Idem commentaires (`.comment-row .c-body` pleine largeur — `!important` côté `share.html` car le `flex:1` y est en style inline) et vue Partages (`.guest-row`/`.rev-row`/`.share-row` en `flex-wrap`, `.grow` en `flex-basis:100%`).
  - **Actions de card révélées par largeur d'écran** (`@media max-width:900px { .card .actions, .task .task-actions { opacity:1 } }`) et **pas seulement** par `@media (pointer:coarse)` — le mode responsive DevTools rapporte un pointeur « fin » et le survol n'existe pas au doigt. **Dupliquer** (classe `.tdup`) masqué sur la card en mobile (trop facile à toucher ; reste dans le menu ⋯ de l'éditeur). Flèche d'expansion agrandie (`1.3rem`).
  - **Boutons ronds & barre Quill** : la règle tactile `@media (pointer:coarse) { button { min-height:44px } }` étirait les boutons ronds en **ovale** (`.task-check`, `.thumb-del`) et gonflait chaque bouton de la barre Quill (barre à 354px, icônes SVG à 38px). Fix : `min-height:0` + dimensions carrées explicites sur ces éléments. Barre d'outils Quill en mobile : **une ligne défilable** (`white-space:nowrap; overflow-x:auto` + `.ql-formats { display:inline-block }`) au lieu de s'enrouler.
  - ⚠️ **Gotcha cascade** : les media queries n'augmentent PAS la spécificité. Un override mobile placé **avant** la règle de base dans le fichier perd à spécificité égale. → Tous les overrides de mise en page mobile doivent être placés **après** les règles de base (bloc « Overrides mémos mobile » en fin de `<style>`), sinon ils sont silencieusement écrasés.
- **Juin 2026 — nom du propriétaire configurable + renommage des invités** (pas de changement du format d'export) :
  - **Nom du propriétaire** : stocké dans `app_state` (clé `owner_name`, défaut « Fabien », helper `_owner_name(db)`), réglable via Paramètres → **IDENTITÉ** (`GET`/`PUT /api/settings` `{owner_name}`). Remplace le « Fabien » codé en dur dans `_mark_comments_seen` (accusés de lecture) et `members` de `share_data` (suggestions d'assignés côté invité). Front : `state.ownerName` (chargé dans `loadAll`), utilisé par `assigneeSuggestions()` et l'exclusion de soi dans « 👁 vu par… ». L'auteur des commentaires propriétaire reste « moi » (signature self, volontairement indépendante).
  - **Renommage d'invité** (`POST /api/guests/rename` `{email, name}`) : met à jour `share_guests.name` pour **tous les accès d'un même e-mail**. Bouton ✎ dans l'en-tête de chaque invité de la vue 🔗 Partages (édition inline). **Non destructif et cosmétique** : ne touche ni e-mail, ni PIN (sur le lien), ni token, ni `guest_token`, ni statut → la connexion de l'invité est inchangée et sa session reste valide. Les attributions déjà enregistrées (assignés, commentaires) gardent l'ancien nom. ⚠️ le nom peut être réécrit si l'invité refait le formulaire de connexion avec un autre prénom.
- **Juin 2026 — mutualisation des helpers front** ([SHARED-HELPERS], ADR-001 Option C, frontend uniquement, **aucun changement schéma/API/export**) : fin de la duplication owner/invité pour les helpers **purs et identiques**. Nouveau `templates/partials/_shared.js.html` (un `<script>`) inclus par `index.html` et `share.html` via `{% include %}` au rendu (donc inliné, pas d'asset servi → invariant 5 intact, pas de build → esprit invariant 6 préservé). **10 fonctions extraites** (identiques vérifiées par diff, top-level, sans appel à un helper page-spécifique) : `usableProjColor`, `provenanceColor`, `setTheme`, `stagger`, `announce`, `syncHeaderHeight` + mentions (`registerMentionBlot`, `addMentionMatcher`, `attachInputMention`, `setupMentions`). **Exclus volontairement** : `el`/`notify`/`countUp`/`applyA11y` (corps **différents** entre les pages) et les helpers de focus carte `isFocused`/`isGroupFocused`/`isSubFocused`/`groupNames`/`hasUngrouped` (**closures** internes au rendu carte → ADR item 4). Invariant 6 réécrit en conséquence. Le partial s'exécute avant le `<script>` applicatif de chaque page (déclarations de fonctions → globales dispo). **v2 (ADR-001 items 4/6)** : `el()` unifié rétro-compatible (détecte la forme par le type du 2e arg, garde-fou `null→{}`) — une seule définition, zéro appel réécrit ; **pop-in carte factorisée** en un `runMapDialog(cfg)` (config = données/persistance/sauvegardes par page) remplaçant `openMapDialog`/`openShareMap` et tous leurs internes (focus, sous-filtre, groupes, provenance) ; **primitive d'arbre** `treeConnector(ancestorsLast, isLast)` partagée par les deux sidebars et vues Plan. Les deux `renderSidebar`/`renderTree` **restent séparés** (assemblages de sections différents owner/invité) — fusion complète volontairement hors périmètre (mauvais ratio risque/valeur).
- **Juin 2026 — parité page invité** ([GUEST-PARITY], `templates/share.html` uniquement, **aucun changement schéma/API/export**, strictement **scopé au partage** — invariant 5, toujours `DATA.projects`/`DATA.memos`) : la sidebar invité (`renderSidebar`/`renderTree`) passe en **arbre `tree`** (connecteurs `.sb-tree`, chevrons `.sb-proj-chev` repliables, état `shareSbProjOpen:<TOKEN>:<id>`, ellipsis des noms, crayon ✎ en absolu, masqué en mobile) ; ajout d'une **vue Plan** via toggle léger `GVIEW` ('board'/'plan') — tuile « 🌳 Plan » dans les filtres → `render()` affiche `#tree-board` (`renderShareTreeView`, racine = `DATA.root_id`, mémos en feuilles, connecteurs, chevrons, colonnes méta desktop 📅/👤/📍 masquées en mobile, repli `shareTreeOpen:<TOKEN>:<id>` + tout déplier/replier). Clic mémo → `openEditor` (si `can_edit`), clic nom de projet → board (`PFILTER=p.id; GVIEW='board'`). Lecture/navigation seule (pas d'édition inline ni D&D ; l'édition reste soumise à `can_edit` + invité approuvé). Chevrons agrandis comme côté propriétaire. Carte invité (sous-filtres/provenance) et formulaire d'accès inchangés. Invariants 5 et 8 respectés.
- **Juin 2026 — vue Plan (arbre projets → mémos)** ([TREE-OUTLINE], frontend uniquement, `templates/index.html`, **aucun changement de schéma/API/export**) : nouvelle vue plein écran `state.view='tree'` (item sidebar « 🌳 Plan », conteneur `#tree-board`, rendu par `renderTreeView()`, branchée dans `renderAll`). Affiche toute la hiérarchie en arbre `tree` : chaque projet/sous-projet déplie ses **mémos en feuilles** + ses sous-projets comme une **même fratrie** (connecteurs `├─ `/`└─ `/`│  ` cohérents via `ancestorsLast[]`, classe `.tree-pre`). Nœud « 📥 Inbox » (mémos sans projet) au niveau racine. Chevrons de repli **propres à la vue** (`treeOpen:<id>` localStorage, déplié par défaut, n'affecte ni sidebar ni board) + bouton « Tout déplier / Tout replier ». Lignes mémo : case ✓ (done), pastille de priorité (`prioOf`), titre/extrait, et **colonnes de métadonnées alignées à droite** (📅 `dueInfo` / 👤 assignés `@Nom +N` / 📍 lieu — largeurs fixes `.tcol`, **masquées en mobile** `@media max-width:900px`). Interactions : clic mémo → `openMemoEditor`, clic nom de projet → board (`memoProject=p.id`), clic chevron → repli. **Lecture seule** (pas d'édition inline ni D&D). Réutilise `projectChildren`/`comboIcon`/`prioOf`/`dueInfo`/`stripHTML`. Navigation clavier optionnelle **non implémentée** (évite tout conflit avec `/`). Invariants 6/8 respectés.
- **Juin 2026 — sidebar façon Evernote + arbre `tree`** ([SIDEBAR-EVERNOTE], frontend uniquement, `renderSidebar`, **aucun changement de schéma/API/export**) : la sidebar plate devient des **sections à en-tête repliables** — « Liens » (Tous + catégories + Sans catégorie, ＋ = `addCategory`) et « Projets » (Mémos + Inbox + arbre, ＋ = `addProject`) ; Partages et Corbeille restent **fixes en haut**. En-tête = chevron ▸/▾ + label + bouton ＋ (les anciens `#add-cat`/`#add-proj` inline sont supprimés, CSS résiduel inoffensif). L'arbre des projets est rendu **façon `tree`** : préfixe de connecteurs `├── `/`└── `/`│   ` calculé via un tableau `ancestorsLast` propagé dans la récursion `renderProjTree(p, ancestorsLast, isLast)`, dans un `<span class="sb-tree">` monospace. Projets parents **repliables** : `<span class="sb-proj-chev">` ▸/▾ avant le préfixe (un projet sans enfant n'en a pas), replié = descendants non rendus dans la sidebar (le board n'est pas affecté). États persistés en localStorage : `sbSection:<nom>` (sections, déplié par défaut) et `sbProjOpen:<id>` (projets, déplié par défaut). **Rail/eye préservés** : en mode rail 64px, `renderSidebar` force tout déplié (`rail` → `secOpen`/`projOpen` renvoient true) et le CSS masque en-têtes/connecteurs/chevrons ; `setRail` ré-appelle `renderSidebar` à la bascule pour ne pas laisser d'icônes manquantes. D&D (lien→catégorie, mémo→projet, projet→projet anti-cycle, détache via Mémos), édition (double-clic/clic droit/✎), compteurs, item actif, responsive : inchangés. Emplacement futur prévu pour une section « Étiquettes » [TAG-NAV] (non implémentée). Invariants 6 et 8 respectés (pas de lib externe, GSAP n'anime que la `nav`).
- **Juin 2026 — provenance dans la liste de la carte** (frontend uniquement, **aucun changement d'export**, ni du board ni des marqueurs) : la liste latérale de la pop-in carte (`draw()` dans `openMapDialog`/`openShareMap`) **regroupe les points par projet d'origine** sous des **en-têtes de section** (emoji + nom + compteur, bordure gauche couleur du projet) — ordre : projet courant (« … (ce projet) ») puis sous-projets descendants dans l'ordre de l'arbre, puis « (sans projet) ». Chaque ligne porte une petite **pastille de provenance** (point couleur projet + emoji) quand plusieurs projets coexistent (sections multiples). Le suffixe « — Nom du projet » a été retiré de la liste : `boardMapPoints()` sépare `placeLabel` (lieu seul, affiché dans la liste) de `sub` (lieu + projet, gardé pour la **popup** du marqueur). Helper `usableProjColor()` : couleur d'accent valide ou `''` si vide/invalide/quasi-noire. `provenanceColor(proj)` : renvoie cette couleur si utilisable, sinon une couleur **déterministe par id** (`PROV_PALETTE`, repli d'affichage uniquement, rien n'est stocké) — un projet sans couleur ni emoji (ex. Japon `#000000`) garde une pastille/bordure **stable et distincte** ; lui donner une vraie couleur dans la pop-in projet reprend le dessus. Le cas « (sans projet) » reste sur `var(--border)`. N'altère pas le reste de `draw()` (cases à cocher, override couleur, chips `map_groups` ×, clic = centrer+popup). Invariant 8 respecté.
- **Juin 2026 — filtre par sous-projet sur la carte** ([MAP-SUBFILTER], frontend uniquement, **aucun changement d'export** — la provenance vient de `memos.project_id`) : 2e dimension de focus dans la pop-in carte, en plus des groupes `map_groups`. Barre `#map-subbar` au-dessus des groupes : un chip **par sous-projet descendant présent** parmi les points (récursif) + le projet courant lui-même (« (ce projet) », couvre ses points propres ET son pin). Cliquer = mettre en avant ce(s) sous-projet(s) ; les autres points sont **grisés mais cliquables** (même rendu que le focus de groupe). N'apparaît que sur la carte d'un **vrai projet** (pas all/inbox) et dès qu'**au moins un point provient d'un sous-projet descendant** (`subToken` ≠ projet courant) — donc même avec un seul sous-projet (cas « Japon » sans point propre + un sous-projet porteur des points) ; un projet plat dont tous les points lui appartiennent en propre n'affiche pas la barre (rien à filtrer). **Cumul** : un point est « en avant » seulement s'il satisfait `isSubFocused` ET `isGroupFocused` (`isFocused = les deux`). `fitBounds` se recale sur les points en avant. Persisté par projet en localStorage (`mapSubFilter:<projet>` ; partage `shareMapSubFilter:<token>:<PFILTER>`). Répliqué dans `share.html` pour les invités d'un projet parent partagé. Carte uniquement (liste/board inchangés), invariant 8 respecté (pas de GSAP sur le `<dialog>`).
- **Juin 2026 — groupes de points sur la carte** (export v17, APP_VERSION "17") : étiquettes de groupe sur les mémos (`memos.map_groups`, liste JSON, multi-groupe, `_map_groups_json()`). Dans la pop-in carte (dashboard ET partage) : case à cocher par point (mémos), barre « n sélectionnés → nom du groupe → ＋ Grouper » (ajoute le groupe à tous les points cochés via `PUT … {map_groups}`), chips de filtre en haut qui **mettent en avant** un (ou plusieurs) groupe(s) : cliquer un chip = afficher **seulement** ces points en couleur, les autres restent **grisés** (carte ET liste de droite) mais toujours **cliquables** (pas de masquage). Aucun chip actif = tout normal. L'ensemble des groupes **actifs (focus)** est persisté par projet en localStorage (`mapFocus:<projet>` / `shareMapFocus:<token>:<PFILTER>`). Chips ×-retirables par point. Un point est « en avant » s'il a **au moins un** groupe actif (ou si rien n'est actif). La carte re-zoome (`fitBounds`) sur les points en avant. Filtre **carte uniquement** (la liste/board ne change pas). Côté partage : tout invité approuvé **filtre** (lecture) ; seul `can_edit` peut **grouper/retirer** (`share_update_memo` accepte `map_groups`, exposé dans `share_data`). Export/import v17 : compat v1→v16 (champ absent = `[]`), upsert non destructif (newer wins, comme `assignees`). Cas d'usage : voyage partagé, masquer « midi validé », ne montrer que « restos jour 4 ».
- **Juin 2026 — couleur perso par point** (export v16, APP_VERSION "16") : couleur d'**un point précis** de la carte, indépendante de la priorité/projet. Nouvelles colonnes additives `memos.marker_color` et `projects.marker_color` (`TEXT DEFAULT ''`, validées `_clean_hex_color()` ou `''`). Résolution mémo : `marker_color` perso > couleur priorité > défaut Paramètres ; projet : `marker_color` perso > `projects.color` > vert `#81c784`. Éditable **depuis le détail du mémo** (input `type=color` 📍 + bouton ↺) **ET depuis la liste latérale de la pop-in carte** (pastille → sélecteur, bouton ↺ par ligne, re-render live sans recharger — les points portent `kind`/`id`/`override`). Owner : `PUT /api/memos/<id>` et `PUT /api/projects/<id>` acceptent `marker_color`. Invité approuvé `can_edit` : édite depuis la carte de `share.html` (`PUT /share/<t>/memo|project`), l'override exposé dans `share_data` sous **`point_color`** (à ne pas confondre avec `marker_color` = défaut global). Export/import v16 : compat v1→v15 (champ absent = `''`), upsert non destructif (enrichit si vide, n'écrase pas un override par une valeur vide plus ancienne).
- **Juin 2026 — couleur des points de la carte configurable** (pas de changement du format d'export) : couleur **par défaut** des marqueurs (mémo sans priorité ni couleur de projet), stockée dans `app_state` (clé `map_marker_color`, défaut rouge visible `#e53935`, helper `_map_marker_color(db)`, validée par `_clean_hex_color()` → `#rrggbb` strict, sinon défaut — jamais de HTML). Réglable via Paramètres → **CARTE** (`GET`/`PUT /api/settings` `{map_marker_color}`, input `type=color` + Enregistrer + Défaut). Remplace l'ancien repli bleu clair `#4fc3f7` dans `boardMapPoints()`/`openMapDialog()` (`state.mapMarkerColor`, chargé dans `loadAll`). Les mémos avec priorité gardent la couleur de leur priorité, les projets leur couleur. Côté invité : `share_data` expose `marker_color`, repli appliqué dans `openShareMap()` (`share.html`).
- **Juin 2026 — heure optionnelle sur les mémos** ([MEMO-TIME], export v18, APP_VERSION "18") : heure facultative à côté du `due_date`. Colonne additive `memos.due_time` (`TEXT DEFAULT ''`, `"HH:mm"` 24 h ou `''`), validée par `_clean_due_time()` (regex stricte `DUE_TIME_RE` → `''` sinon, jamais de HTML). **Dépend de la date** : une heure sans date n'existe pas (rejetée à la création), et vider la date vide l'heure — la garde `if not due_date: due_time = ''` s'exécute **après** la replanification récurrente (`_perform_memo_update`), donc un mémo récurrent horodaté **garde** son heure au prochain tour (`_next_due` inchangé, l'heure est portée verbatim). `_memo_snapshot`/révisions, duplication et restauration de révision portent l'heure. **Affichage** (index.html + share.html, helper partagé `fmtMemoTime` dans `_shared.js.html`) : badge `📅 6 nov · 14h30` (minutes toujours, « 14h00 » à l'heure pile), **uniquement dans le badge absolu** (pas dans les dates relatives `dueInfo`) ; champ `<input type="time">` natif visible quand une date est posée ; tri intra-jour = mémos **sans heure d'abord** puis heure croissante. **Partage** : invités lecture la voient (`due_time` dans `_share_memo_dict`), invités `can_edit` la posent/effacent via `share_update_memo` (chemin scopé existant, aucune route nouvelle — invariant 5). **Export/import v18** : émis via `_memo_dict`, compat v1→v17 (champ absent = `''`), upsert non destructif comme `marker_color` (newer-wins n'écrase jamais une heure présente par une absente/plus ancienne). Prérequis de l'agenda à venir ([AGENDA]).
- **Juin 2026 — badge EXIF photo** ([IMAGE-EXIF], **export inchangé**, pas de bump — toujours v18) : à l'affichage d'une image, lecture EXIF **côté serveur à la volée** (lib pure-Python `ExifRead==3.0.0` dans `requirements.txt`, lecture seule, pas Pillow) → badge **lecture seule** `📷 lieu · date de prise de vue` à côté de l'image dans la visionneuse. **Per-image, jamais stocké, aucune écriture mémo** (la spec a volontairement réduit la proposition initiale de pré-remplissage `location`/`due_time`). Helpers `_image_exif(name)` (GPS borné comme `_clean_location` + date `EXIF DateTimeOriginal`→ISO ; `None` si ni GPS ni date → no-op silencieux) et `_exif_dms_to_deg`, cache mémoire `_exif_geo_cache` (un géocodage par coords via `_reverse_geocode`, mémorise `''` → anti-abus Nominatim). Routes GET `/api/image-exif/<name>` (owner) et `/share/<token>/image-exif/<name>` (invité, **même contrôle de scope que `share_image`** — invariant 5). Front : `runImageViewer.exifUrl(name)` optionnel (badge `iv-exif`, repli coords si label vide, masqué sans données). Dégrade proprement (captures/photos strippées = pas de badge).
- **Juin 2026 — itinéraire photo sur la carte** ([PHOTO-MAP], **export inchangé**, donnée dérivée non exportée, pas de bump — toujours v18) : exploiter l'EXIF des photos d'un projet **sans aucune écriture sur les mémos**. Nouvelle table **dérivée** `image_meta` (`filename` PK, `memo_id`, `memo_uid`, `lat`, `lng`, `label`, `taken_at`, `has_gps`, `created_at`) — `CREATE TABLE IF NOT EXISTS`, **jamais exportée** (re-calculable depuis les fichiers). Remplie **à l'upload** (owner `add_memo_image` + invité `share_add_image`, via `_record_image_meta` qui réutilise `_image_exif` — un seul géocodage caché par photo) ; nettoyée à chaque suppression définitive de fichier (`_forget_image_meta` aux 4 sites : suppr per-image owner/invité, purge mémo, `_purge_trash` — accepte `db` ou `conn`, tolère nom/liste/chaîne JSON). **Backfill** des images antérieures : `_backfill_image_meta` (daemon thread à côté de `_backup_loop`, idempotent — skip filenames déjà connus, throttlé `sleep 1.1s` **uniquement** sur géocode réseau réel). Endpoints **GET scopés** : `/api/projects/<id>/photos` (owner, récursif `_project_descendants` → mémos non corbeille → `_project_photos`) et `/share/<token>/photos` (invité, scopé `_share_scope_memos` comme `share_image`/`share_data` — invariant 5). Front (`runMapDialog`, partial partagé) : cfg optionnel `loadPhotos`/`openImage` → **bouton 📷** togglant un **calque photo** (marqueurs `divIcon` 📷 distincts des points-mémo, filtrés par le même focus sous-projet/groupe via `isFocused`), **clic → `runImageViewer`**, et **frise « 🕘 Où on était »** chronologique par `taken_at` dans `#map-list` (photos datées sans GPS listées sans marqueur). Pas de 📷 sur all/inbox (owner). Invariants 5/6/8.
- **Juin 2026 — clustering des marqueurs photo** ([PHOTO-CLUSTER], **front pur**, export/`image_meta`/schéma inchangés) : raffinement de [PHOTO-MAP]. Plugin **Leaflet.markercluster 1.5.3 vendorisé** dans `static/` (`leaflet.markercluster.js` + `MarkerCluster.css` + `MarkerCluster.Default.css`, committés comme leaflet/quill/gsap — **aucun fetch build/runtime**), whitelisté `SHARE_ASSETS` (invité), inclus dans `index.html` (`/static/`) et `share.html` (`/share/assets/`) **après** `leaflet.js`. Dans `runMapDialog` : `photoLayer = typeof L.markerClusterGroup === 'function' ? L.markerClusterGroup({showCoverageOnHover:false, maxClusterRadius:40, spiderfyOnMaxZoom:true, zoomToBoundsOnClick:true}) : L.layerGroup()` (**dégradation propre** = comportement [PHOTO-MAP] si plugin absent). **Badge compteur** sur superposition, **séparation au zoom**, **spiderfy** au même point exact (défauts du plugin). Décision focus : un cluster ne grise pas un membre → photos **en focus** dans le cluster, photos **grisées** dans un `photoGreyLayer` plain séparé (compteur = photos affichées). **Périmètre strict : calque photo uniquement** (points-mémo `markerLayer` non clusterisés). Invariants 5 (whitelist limitée aux 3 fichiers), 6 (vendorisé, 0 CDN), 8 (cluster/spiderfy = marqueurs, jamais le `<dialog>`).
- **Juin 2026 — en-tête contextuel de la pop-in mémo + créateur** ([MEMO-CONTEXT], export v19, APP_VERSION "19") : la pop-in « Éditer le mémo » gagne un **en-tête contextuel** (`#me-context`) au-dessus du titre — **fil d'Ariane** « 📌 Projet : A › B › C » (remonté via `projectAncestorIds(memo.project_id)`, `comboIcon` + ellipsis ; 📥 Inbox si pas de projet) + ligne méta **« 🕒 Créé par @… le … à hh:mm »** (depuis `created_at` + `created_by_display`, masquée si pas de date). Les **commentaires** passent dans un accordéon `<details class="me-acc">` repliable (replié par défaut, « 💬 Commentaires (n) ▸ », rien retiré de la feature). Pied de page : bouton **« 📜 Voir les versions »** à gauche (`margin-right:auto`, style `.me-versions` aligné sur `.task-actions button`) → réutilise `openRevisions(memo)` (UI `#revisions-dialog` + `GET /api/memos/<id>/revisions` + restauration `/api/memos/<id>/restore` existants, owner-only). **Backend** : colonne additive `memos.created_by` (`TEXT DEFAULT ''`) — `''` = propriétaire (résolu à l'affichage par `_owner_name(db)` → `created_by_display`, survit au renommage owner), non vide = invité « Nom <email> » posée par `POST /share/<token>/memos` ; `_memo_dict(row, owner_name=None)` ajoute `created_by_display`, `_share_memo_dict` n'expose que `created_at` (auteur owner-only — invariant 5). Export/import v19 : `created_by` brut exporté (jamais `created_by_display`), compat v1→v18 (absent = `''`), upsert non destructif (newer-wins). **Parité invité** (`share.html`) : `#q-context` = fil d'Ariane scopé `DATA.projects` + date de création **en lecture** (pas de « créé par », pas de versions). Invariants 5, 8 (en-tête = markup statique, accordéon natif `<details>`, pas de GSAP), 9 (classes/tokens existants).

- **Juillet 2026 — auto-refresh sur tout changement de données** ([AUTO-REFRESH], V19.8.17, **export inchangé** — toujours v19) : le poll 15 s du dashboard owner recharge sur **toute** variation des données (autre onglet/appareil/API), plus seulement sur l'activité invité. Helper `_data_version(db)` = empreinte MD5 cheap de colonnes légères (jamais le contenu ; positions incluses car les reorders ne touchent pas `updated_at` ; `app_state` hors `activity_seen_at` ; **hors `last_seen_at`/`session_token`** — volatils), ajoutée à `GET /api/activity` et préfixée à la clé du poll. Si `uiBusy()` (pop-in/saisie), reload **différé** rejoué à la fermeture (listener `close` en capture + tick 100 ms — au dispatch de `close` le focus est encore dans la pop-in). Page invité non concernée (refresh propre au hub/share).
- **Juillet 2026 — session invité mémorisée** ([HUB-SESSION], V19.9.18, export inchangé, zéro route nouvelle) : fin de la re-saisie du PIN à chaque visite du hub (Safari iOS purge le localStorage ~7 j). Colonne additive `guest_hubs.session_token` (jamais exportée/exposée) + cookie **HttpOnly** `dashhubsession` posé au bon PIN et re-posé à chaque `/data` prouvé (**180 j glissants**, `Path=/share/hub/<hub_token>` — deux invités coexistent par navigateur, `SameSite=Lax`, `Secure` conditionnel `X-Forwarded-Proto`). `_hub_proof(db, hub)` : cookie (`compare_digest` avec le token de CE hub) OU header `X-Guest-Token` (repli). Le cookie n'authentifie QUE la lecture `/data` — approbation réservée au PIN, écritures aux jetons par dossier (invariant 5). Révocation : `rotate`/`rotate-pin` régénèrent `session_token`. Spec `docs/specs/HUB-SESSION-persistent-guest-session.md`.
- **Juillet 2026 — édition des invités + dernière connexion** ([GUEST-EDIT], V19.11.19, export inchangé, aucune route publique nouvelle) : pop-in owner « Modifier l'invité » (remplace le rename inline) → `POST /api/guests/update` `{email, name, new_email}` : **re-keyage atomique** `share_guests` + `guest_hubs` (pending inclus, un seul commit), collision e-mail → **409 sans rien modifier** (jamais de fusion silencieuse — invariant 5), lien/PIN/`session_token`/`guest_token` **INTACTS** (transparent pour l'invité), historique (commentaires, `created_by`, `revisions.editor`) **non réécrit**. Et `guest_hubs.last_seen_at` + `_touch_guest_seen` (posé au PIN, au `/data` du hub prouvé et au `/share/<t>/data` direct avec jeton approuvé — **throttle 5 min**), exposé **uniquement** dans `/api/hubs` → badge « 🕐 vu il y a … » dans 🔗 Partages. Spec `docs/specs/GUEST-EDIT-guest-identity-and-last-seen.md`.
- **Juillet 2026 — pop-in Paramètres en onglets + sauvegarde auto** ([SETTINGS-TABS], V19.10.20, frontend pur, export inchangé) : 5 onglets (Identité / Carte / Sauvegardes / Catégories / Priorités — chips `.prio-btn`, actif persisté localStorage `settingsTab`, rangée défilable en mobile façon barre Quill). **Sauvegarde auto** du nom (blur/Entrée) et de la couleur carte (change) — fin des boutons Enregistrer, ✓ discret `aria-live` dans l'en-tête. Onglet Sauvegardes : Export/Import + carte d'état « Dernière sauvegarde auto » + liste des fichiers dans un `<details>` replié. « ＋ Ajouter » en ligne pointillée (`.cat-add-line`). Rien retiré : Horloges → Identité, Historique → Sauvegardes, Projets + Statuts → Catégories. Invariants 8/9.
- **Juillet 2026 — arbre des sidebars + sidebar sticky** ([SIDEBAR-TREE], V19.12.21, frontend pur, export inchangé) : fini les connecteurs texte `├──` dans les **3 sidebars** (owner/share/hub — dérive monospace avec les emojis) → enfants dans des conteneurs `.sb-kids` (**~17 px/niveau + ligne guide verticale** `border-left`), chevron **dans la ligne** (colonne fixe, feuilles = span vide), **une seule icône** (emoji sinon pastille), noms ellipsis + `title`, `.sub` 2rem→1rem. Rail/œil/mobile préservés (`.sb-kids{display:contents}`) ; **vues Plan et pop-in « Déplacer vers… » gardent leurs connecteurs** (volontaire). Et **sidebar sticky à scroller propre** (owner + share desktop : `sticky` sous le header via `--header-h`, `height:calc(100dvh - header)`, `overflow-y:auto`, scrollbar fine). GSAP n'anime que les `.cat-item` avec `clearProps`, jamais le `nav` (invariant 8) ; invariant 9.
- **Juillet 2026 — tableaux dans les mémos** ([MEMO-TABLES], V20.5.30, **export inchangé — toujours v20**, zéro route/schéma) : édition complète de tableaux dans l'éditeur Quill des 3 pages (insérer via bouton ⊞, lignes/colonnes/fusion/suppression via menus contextuels fr), owner + invités `can_edit` ; module **`quill-table-better` 1.2.3 vendorisé** `static/` (MIT, UMD sur `window.Quill`, compatible Quill 2.0.3 — validé sur POC avec mentions `@` et `list: check` AVANT intégration, spec §2) + whitelist `SHARE_ASSETS` ; helper partagé **`applyTableBetter(modules)`** dans `_shared.js.html` (enregistrement unique, no-op si module absent = dégradation propre, tableaux existants toujours rendus par Quill 2 core — pas de perte au save) ; CSS aux tokens 2 thèmes (bordures td/th `var(--border)`, UI flottante du module re-basée `--panel-2`, bloc en fin de `<style>`), mobile = `.ql-editor` scroll horizontal + `min-width` tableau ; **cards** : tableau masqué dans l'extrait clampé mais rendu statique tokens en contenu complet (share lecture seule = la card est la seule surface) et un contenu à tableau ne se clampe plus côté share ; overlay des classes de sélection sérialisées neutralisé hors éditeur ; regex `isRich`/`isRichHTML` étendues à `<table`. Spec `docs/specs/MEMO-TABLES-quill-tables.md`.
- **Juillet 2026 — sous-projets créés par les invités dans le hub** ([GUEST-SUBPROJECT], V19.13.22, `hub.html` seul, **zéro route nouvelle, zéro changement backend**, export inchangé) : barre `subProjBar()` (`.addbar`) sous la barre mémo (vues board + Plan) — nom + sélecteur de dossier parent (**modifiables seulement**, pré-sélection du dossier courant) + Créer ; masquée sans dossier `can_edit`. Routée vers le `POST /share/<share_token>/projects` **existant** du parent choisi ; le serveur revalide tout (scope + `can_edit` + invité approuvé, `parent_id` hors périmètre → repli racine du share, 409 nom dupliqué, création journalisée dans les révisions) — invariants 5/9. `share.html` le permettait déjà ; les invités vivent dans le hub depuis [HUB-DASHBOARD].
- **Juillet 2026 — dossier en mode vote « choisir un »** ([VOTE-DECISION] V1, V20.7.44, spec `docs/specs/VOTE-DECISION-v1.md`, **export INCHANGÉ — toujours v20**) : un **dossier** (projet/sous-projet) bascule en **mode vote**, ses **mémos directs = les options**, voix `(personne, dossier)`, mode **`single`** (extensible **multi** V2 par échange du seul index unique, colonnes inchangées). **Étape 0 export tranchée : voix NON exportées** (donnée d'atelier éphémère, précédent `comment_seen`) → **pas de bump de X**, pas d'entrée d'invariant 1, compat inchangée ; ⚠️ un ré-import ne restaure pas un vote (voix + résultat en base seulement). **Migration additive** : table `memo_votes` (`project_id, memo_id, voter, created_at`) + **index unique `(project_id, voter)`** (single = une voix/personne/dossier **en BASE**) ; colonnes `projects.vote_enabled/mode/deadline/closed/winner_id/winner_ids`. `vote_winner_ids` (JSON) = **gel de l'ex æquo ET marqueur « déjà figé »** (`''` = jamais figé). **État clos DÉRIVÉ** à chaque lecture (`vote_closed=1` OU deadline dépassée), **gel du gagnant PARESSEUX** (`_ensure_vote_snapshot`, idempotent, aucun cron). **Deadline ISO locale = Europe/Paris** (`_APP_TZ`/`zoneinfo`, `tzdata` en requirements, repli fixe). **Voix matchées par e-mail** (`_voter_email`, identifiant stable) → robuste au renommage invité ([GUEST-EDIT]) ; `_cast_vote` toggle (re-cliquer sa voix = retirer) / retarget (single) sans doublon. **Routes owner** (derrière Authelia) : config via `PUT /api/projects/<id>` (`vote_enabled/mode/deadline`, n'affecte jamais les voix) ; `POST …/vote/close` (fige, `{winner_id}` = arbitrage ex æquo) ; `POST …/vote/reopen` (exige deadline redéfinie/effacée, 400 si dépassée, efface le gel, conserve les voix) ; `POST /api/memos/<id>/vote` (400 pas-option, 409 clos). **Invité** : `POST /share/<token>/memo/<id>/vote` sous **`/share/*`** (bypass Authelia, invariant 5) — **invité approuvé requis, `can_edit` NON requis** (voter n'est pas éditer), scope revalidé, 409 clos, voter = « Nom <email> », **e-mail jamais exposé** aux invités (`_vote_display_name` → nom seul). Config/clôture/réouverture **owner-only**. Hub → route vers le `/share/<share_token>` du dossier (zéro route hub). **Exposition** : dossiers portent `vote_enabled/mode/deadline/vote_state (résolu)/winner_id/winner_ids` (`/api/projects`, `share_data`, hub `/data`), owner voit `vote_closed` brut ; mémos-options portent `vote_count/vote_mine/vote_voters/is_winner`. `_data_version` étendu à `memo_votes` (flags projet déjà couverts par `SELECT * projects`). **Cascades** : mémo purgé → voix supprimées (`_purge_memo_row`, comme `memo_comments`) ; dossier supprimé → voix supprimées ; **invité retiré** (`delete_guest`/`delete_share`/`delete_hub`) → `_delete_votes_for_email` (scopé au partage, ou tout pour le hub) ; snapshot figé **ne bouge pas** après retrait si déjà clos. **UI 3 pages** (helpers partagés `fmtVoteDeadline`/`voteStatusLabel`/`voteWinnerText`/`voteWinnerIdSet`) : badge dossier « 🗳 Vote ouvert · clôt le … / clos le … », owner bouton **Clore** (accent) + config/réouverture pop-in projet, par mémo bouton **Voter** (`.prio-btn`, ma voix = chip accent) + compteur + votants (initiales) + **🏆 Gagnant/Ex æquo** figé si clos ; invité Voter réservé à l'approuvé (anonyme = résultats en lecture), clos = désactivé ; **carte : point gagnant en anneau accent** (`cfg.winnerIds`, focus `runMapDialog`, invariant 8). Cas limites (égalité affichée, invité retiré → voix supprimées, revote post-clôture bloqué **serveur**, dossier sans géoloc = vote normal sans mise en avant carte). **Extensions [V20.7.46]** (spec §12, export toujours v20) : **[VOTE-RESET]** = `POST /api/projects/<id>/vote/reset` owner-only (supprime toutes les voix + efface le gel + rouvre, même garde que reopen ; bouton « ↺ Remettre à zéro » + confirm danger dans la pop-in projet) ; **[VOTE-EXCLUDE]** = colonne additive `memos.vote_excluded` (**non exportée**) → mémo « hors vote » : visible/éditable mais **pas une option** (jamais gagnant, filtré dans `_compute_winners`/exposition ; `POST /api/memos/<id>/vote` **et** `/share/<t>/memo/<id>/vote` → 400 ; exclure un mémo à voix → voix supprimées) ; config **owner-only** (case « Hors vote » dans le détail, `PUT /api/memos` hors `_perform_memo_update` → `share_update_memo` ne l'accepte pas), invité = **lecture seule** (badge « 🗳 hors vote »). **[VOTE-MULTI] V2 [V20.8.47]** (spec §12.3, export toujours v20, zéro route nouvelle) : mode « plusieurs choix » — index unique **élargi** `(project_id, voter, memo_id)` (DROP/CREATE dans `init_db()`, single = unicité applicative) ; `_cast_vote(…, mode)` togglé par (voter, mémo) en multi (un 2ᵉ mémo n'écrase pas le 1ᵉʳ) ; gardes/gagnant/ex æquo identiques ; bascule owner via chips « ☝️ Un seul / ☑️ Plusieurs », **multi→single purge** les voix surnuméraires (`_collapse_votes_to_single`, garde la + récente par votant) après confirm danger ; UI 3 pages = cases à cocher en multi (`voteBtnLabel`).
- **Juillet 2026 — votes nommés multiples par dossier** ([VOTE-GROUPS] V20.9.48, spec `docs/specs/VOTE-GROUPS-named-votes.md`, D1-D4, export toujours v20) : un dossier porte **N votes nommés** (tables additives `votes` + `vote_options`, `memo_votes.vote_id` NULL = vote-dossier V1/V2 inchangé, `projects.vote_create` permission héritable). **Deux index partiels** partitionnent `memo_votes` par `vote_id` (`ux_memo_votes_multi … WHERE vote_id IS NULL` **rendu partiel** + `ux_memo_votes_named … WHERE vote_id IS NOT NULL`) → **D1 : un mémo dans plusieurs votes** sans collision ; toutes les requêtes du vote-dossier filtrent `vote_id IS NULL`. Options = adhésion **explicite** (mémos du porteur + descendants), `vote_excluded` ne concerne que le vote-dossier. Permission `_resolve_vote_create` (modèle `_resolve_trip`, défaut racine `'guests'`) : invités créent/gèrent **leurs** votes (match e-mail de `created_by`) sous `/share/*` (routes `POST /share/<t>/votes`, `PUT|DELETE /share/<t>/votes/<vid>`, `.../close|reopen|reset`, `{vote_id?}` sur le vote de mémo), **403 générique**, porteur revalidé dans le scope. Owner : `/api/projects/<id>/votes`, `/api/votes/<vid>` (+ close/reopen/reset/DELETE). Cascades §7 (mémo/dossier supprimés, invité retiré → voix parties mais votes créés survivent, mémo déplacé → retiré des options). UI 3 pages : chips de scrutin, création par sélection sur les cards, pop-in de gestion, chips permission dans la pop-in projet. `_data_version` étendu.
- **Juillet 2026 — le vote sur la carte « par scrutin »** ([VOTE-MAP] V20.10.49, **frontend PUR**, `runMapDialog` du partial + glue par page, **zéro route, export v20 inchangé**) : quand le dossier affiché a des scrutins ([VOTE-DECISION] + [VOTE-GROUPS]), la carte gagne une **barre de chips de scrutin** (`🗳 (dossier)` + un par vote nommé) ; le chip sélectionné pilote l'affichage. **Options** = marqueur v16 + **pastille « n voix »** + **✓** sur mes choix (clic = voter/retirer en place, hors périmètre : voter depuis le popup) ; **mémos hors scrutin** = **repère gris discret** (jamais votable) ; **coche « 🗳 Voix »** (barre d'outils, gabarit Frise, persistée par projet, clés distinctes owner/share/hub) → pastilles on/off + perdants estompés si clos ; **gagnant(s) clos** = marqueur agrandi + anneau accent + 🏆 + étiquette « nom · n voix » + `fitBounds` auto ; évidence **seulement** quand un scrutin est sélectionné (le ring legacy `cfg.winnerIds` est suspendu si `voteData` présent). Chaque page passe `cfg.voteData` (scrutins normalisés + `onVote`) au partial (invariant 5, le partial ne connaît pas le réseau). Compatible voyage (pastilles cumulables avec vignettes ①②), dossier sans vote = carte inchangée.
- **Juillet 2026 — événement d'agenda du gagnant** ([VOTE-V1.1] V20.11.50, spec VOTE-DECISION §8, export v20 inchangé) : un vote **nommé** porte une **`votes.event_date`** optionnelle (colonne additive, ISO local Europe/Paris, non exportée, distincte de `vote_deadline`) = le créneau voté. **À la clôture** (`_freeze_named_vote`, donc clôture manuelle owner, invité créateur, ET deadline via `_ensure_named_snapshot`) : si `event_date` non vide **ET gagnant UNIQUE**, le mémo gagnant est **planifié** (`due_date`/`due_time` écrasés, `updated_at` bumpé). **Ex æquo → aucun write** (l'owner départage puis re-clôt). Réouverture/reset **conservent** la date (non destructif). Zéro route/droit nouveau ; le gagnant réutilise l'affichage agenda existant ([AGENDA], board, carte/frise). UI : champ « 📅 Date de l'événement » (création + gestion, 3 pages) + ligne d'info dans le confirm de clôture. **Suite** : voter depuis le popup carte (backlog).
- **Juillet 2026 — export/import d'un dossier seul** ([EXPORT-SUBTREE] V20.12.52, owner-only, **PAS de nouveau format**) : `GET /api/projects/<id>/export` (derrière Authelia, 404 si inconnu) réutilise `_build_export(db, root_id=...)` avec un **filtre sous-arbre** (dossier + descendants + leurs mémos/commentaires/historique) et les **mêmes exclusions** que l'export global (noms de fichiers d'image, pas de binaires ; **pas de votes/voix/config vote**). JSON **v20 identique** → **avalé par l'import existant** sans code nouveau (upsert par uid/nom, non destructif). Liens/catégories vides (rien ne les référence dans un sous-arbre), **priorités = référentiel complet** (remap par nom à l'import). **Rattachement par NOM** : `proj_names` sur TOUS les projets → le `parent` de la racine porte le nom de son parent réel (hors sous-arbre) ; import raccroche si ce nom existe, sinon racine. Références d'images orphelines **tolérées comme à l'import global** (même générateur/importeur). UI : bouton « ⬇ Exporter ce dossier (JSON) » (menu ⋯ pop-in projet, `<a download>`), rien côté invité. Export global inchangé (refactor `root_id=None` = comportement d'avant). **Addendum [V20.12.53] — import ciblé « ⬆ Importer ici »** : query param optionnel `target_parent_id` sur `/api/import` — rattache **seulement les projets RACINE NOUVEAUX** du fichier (nom inconnu ; projets matchés par NOM) au dossier cible, **jamais un existant** (invariant 2 : l'import ajoute, ne réorganise pas) ; cible validée (existe ET pas un élément du fichier → anti-cycle 400) ; absent = comportement d'avant ; l'import global des Paramètres inchangé (pas de destination). Invariants 1/2/9.
- **Juillet 2026 — masquer/afficher les mémos des sous-projets dans le board** ([BOARD-SUBHIDE] V20.13.54, spec `docs/specs/BOARD-SUBHIDE-collapse-subproject-memos.md`, **frontend PUR 3 pages, export v20 inchangé**) : helper partagé **`subHideBar(cfg)`** (`_shared.js.html`, ADR-001) rend une barre de chips « masquer/afficher » par **sous-projet descendant présent** (provenance `memos.project_id`) + « (ce projet) » + badge « n masqués · tout afficher », sous les tuiles/barre d'ajout (cohabite avec les chips de scrutin [VOTE-GROUPS] qui vivent en haut). Sémantique [MAP-SUBFILTER] inversée (chip actif = masqué, estompé + ✕ + barré). **Affichage SEUL** : le masquage filtre le **rendu des cards uniquement** ; tuiles/compteurs (set complet) et **carte** (`boardMapPoints`/`shareMapPoints`/`hubMapPoints` — pipeline distinct, jamais touché) gardent le périmètre complet. Persistance par projet, **clés distinctes du focus carte** (`boardSubHide:<id>` / `shareBoardSubHide:<TOKEN>:<PFILTER>` / `hubBoardSubHide:<FOCUS>`), orphelins ignorés. Barre seulement sur un vrai projet avec ≥1 mémo de descendant. Invariants 5/6/8/9.
- **Juillet 2026 — prévisualisation d'import + résolution de conflits** ([IMPORT-PREVIEW] V20.14.55, spec `docs/specs/IMPORT-PREVIEW-dry-run-merge-ui.md`, D1-D4, export v20 inchangé) : le flux « ⬆ Importer un dossier ici » ([EXPORT-SUBTREE]) fait un **dry-run** puis une **pop-in de prévisualisation**, rien ne s'écrit avant confirmation. **`POST /api/import?dry_run=1`** = analyse LECTURE PURE (aucune écriture, **pas de commit ni bump `_data_version`**, retour avant `ensure_category/project`) → `{projects (arbre new/merge par nom), memos (new/skip/conflict[active|trashed] par uid + signature `content|title|done|due_date|due_time`), bilan}`. **`POST /api/import`** accepte en plus **`resolutions {uid: overwrite|duplicate|skip}`** : overwrite force la MAJ + **restaure si en corbeille** (`deleted_at=''`) dans le projet visé (résout le « mémo fantôme ») ; duplicate = **uid régénéré** (dédup contenu court-circuité, original intact) ; skip/absent = comportement V20.12 (newer-wins). **D1 : l'import global des Paramètres et un `POST /api/import` sans `dry_run`/`resolutions` restent STRICTEMENT inchangés** (compat scripts). UI pop-in `#import-preview-dialog` : arbre indenté + badges pilules (🟢 `--green` ajouté / 🟣 `--guest` fusion / ⚪ `--muted` ignoré / 🟠 `--yellow` conflit, 2 thèmes), légende, ligne de résolution par conflit (Ignorer pré-actif), « Tout : … » si > 3 conflits, bilan, « Importer N » (N=0 → désactivé). Invariants 1/2/8/9.
- **Juillet 2026 — réactions emoji sur les commentaires + palette configurable** ([COMMENT-REACTIONS] V21.0.56 + [REACTION-PALETTE] V21.0.57, ⚠️ **BUMP EXPORT → v21**, specs `docs/specs/COMMENT-REACTIONS-emoji-votes.md`) : chaque commentaire (owner `GET …/comments`, `share_data`, hub `/data`) porte `reactions:[{emoji,count,mine,voters}]` — **toggle** par (commentaire, emoji, votant) via table additive `comment_reactions (…, UNIQUE(comment_id,emoji,voter))` (`CREATE TABLE IF NOT EXISTS`, purge en cascade). Votant = pattern `created_by` (`''`=owner résolu `_owner_name`, sinon « Nom <email> », match par e-mail). Routes owner `POST /api/comments/<id>/react` + invité `POST /share/<token>/comment/<id>/react` (sous `/share/*`, bypass Authelia invariant 5, **invité APPROUVÉ requis, `can_edit` NON** — réagir ≠ éditer ; scope revalidé ; `voters`=nom seul, e-mail owner-only). Réactions **hors** badge 🔔. Helper partagé `reactionRow` (chips + mini-palette 😊+, pas de GSAP) 3 pages. **[REACTION-PALETTE]** : whitelist passée de constante à **config owner** (`app_state.reaction_emojis`, seed = 6 base, ajout/retrait dans Paramètres → onglet « 😊 Réactions »), validation stricte d'un **seul grapheme emoji** (`_clean_reaction_emoji`, Python pur, zéro lib/CDN, rejette texte/HTML/2-emojis → 400), palette exposée à tous et **exportée si custom** ; réactions à l'import validées en FORMAT (préserve l'historique des emojis retirés). Export/import v21 : `reactions` (voter brut) nichées ; `INSERT OR IGNORE` (ré-import 0 doublon) ; compat v1→v20 (absent = rien). `_data_version` étendu (`comment_reactions` + `app_state`). Invariants 1/2/5/6/8/9.
- **Juillet 2026 — favoris de projets/vues + accueil cliquable + ajout de lien relocalisé** ([FAVORITES] V1→V1.2 V21.1.58→60, [HOME-RESET] + [LINK-ADD-RELOCATE] V21.1.61, **export v21 inchangé**, owner-only, `index.html` + `app.py` — `share.html`/`hub.html` INCHANGÉS) : **[FAVORITES]** épingle des projets ET des vues (Plan/Agenda/Mémos) en tête de la sidebar. Table additive `favorites (id, kind, ref, position, created_at, UNIQUE(kind,ref))` — **NON exportée / NON importée** (donnée de confort locale, comme `comment_seen` ; **aucun bump de format**), purge en cascade à la suppression du projet, `_data_version` étendu. Modèle **typé** `{kind:'project'|'view', ref}` (migration douce depuis le schéma V1 `favorites(project_id PK)` : rename + rebuild + port, sans perte). Routes owner-only `GET/POST/DELETE /api/favorites {kind,ref}` (toggle, 400 invalide, 404 projet inconnu) + `PUT /api/favorites/reorder` (ordre complet, revalide, ignore disparus/invalides — aucun 500). UI : section « ⭐ Favoris » repliable (desktop) ; en mobile/rail une **entrée unique** « ⭐ Favoris » ouvrant la liste dans le main (`#favorites-board`) ; étoile ☆/★ (`.fav-star`) sur les vues + ★ dans la pop-in projet ; **réordonnancement drag-and-drop** `makeFavSortable` en **Pointer Events** (zéro lib — invariant 6 ; souris = drag immédiat, tactile = appui long 0,5 s ; le drag tactile bloque le scroll via `touchmove` non-passif `preventDefault`, `pointercancel` finalise sans tuer le drag). **[HOME-RESET]** : `<h1 id="home-btn">` cliquable → `goHome()` (vue Liens, filtres/recherche réinitialisés, scroll top ; idempotent). **[LINK-ADD-RELOCATE]** : `#add-link` retiré du `<header>` → barre `#links-actions` dans la vue Liens (même id, même `openDialog()` → zéro régression) ; **[V21.1.62]** la barre regroupe **deux boutons côte à côte** — « + Ajouter un lien » + « + Ajouter une catégorie » (`#add-cat-page` → `addCategory()`, même fonction que le « + » sidebar). Invariants 1/2/5/6/8/9.

- **Juillet 2026 — confort UX (✓ enregistré, annuler suppression, états vides) + fix vote carte sous-projet** ([SAVE-INDICATOR] V21.1.63, [UNDO-DELETE] V21.1.64, [EMPTY-STATES] V21.1.65, [VOTE-MAP-SUBPROJECT] V21.1.66, **export v21 inchangé**, frontend pur) : helpers partagés dans `_shared.js.html` — `flashSaved(ok)` (✓/⚠ flottant à chaque patch mémo, branché sur owner `patchMemo` + invité `put`/`putMemo`), `undoToast(msg,onUndo,ms)` (toast « Annuler » après suppression douce, owner `deleteMemo` → `POST /api/trash/<id>/restore`, zéro route nouvelle), `emptyState({icon,title,hint,actionLabel,onAction})` (états vides soignés owner : Liens + board Mémos). **[VOTE-MAP-SUBPROJECT]** (fix bug) : les builders de vote carte (`buildMapVoteData`/`buildShareMapVoteData`/`buildHubMapVoteData`) agrègent le projet courant **ET ses descendants** via le helper partagé `voteScrutinsForProject` → le scrutin d'un sous-projet apparaît sur la carte du parent (côté invité, `projDescendants` borné à `DATA.projects` = scope partage, invariant 5) ; chip préfixé du nom du sous-projet, `onVote` met à jour le porteur, aucune route (données déjà exposées, token parent scope les descendants). Invariants 5/6/8/9.

- **Juillet 2026 — widget Pomodoro / minuteur / chronomètre** ([POMODORO] V1→V1.2 V21.1.67→68, **export v21 inchangé**, frontend pur, état 100 % local localStorage, ZÉRO route/schéma/donnée partagée, parité owner/invité/hub via `initPomodoro()` dans `_shared.js.html`) : bouton flottant ⏱ + panneau, 3 modes (🍅 Pomodoro cycles travail/pause, ⏳ Minuteur, ⏱ Chrono). **Source de vérité = timestamps absolus** recalculés depuis `Date.now()` (exact en arrière-plan, recalage `visibilitychange`/`focus`). Alerte triple (dés)activable : 🔔 Son (Web Audio, **bip synthétisé** = zéro asset, invariant 6), 📳 Vibration (`navigator.vibrate` guardée, no-op iOS), 🖥 Notification desktop (`requestPermission` au geste, dégradation propre). Widget + lanceur **déplaçables** (Pointer Events souris+tactile, `clampPos` viewport, positions persistées `pomoPos`/`pomoLaunchPos`), **afficher/masquer** (`pomoHidden` : case Paramètres owner + ✕ + pastille `#pomo-reshow` universelle), **titre d'onglet** = temps restant (restauré partout), **anneaux de progression** SVG sur panneau ET lanceur (même `stroke-dashoffset` que l'affichage), **Pomodoro bicolore** (`ringColor()` : travail = `--accent`, pause = `--green`). Pas de GSAP (invariant 8), tokens (invariant 9).
- **Juillet 2026 — widget convertisseur de devises ¥€** ([FX-CONVERTER] V23.2.90, **export v23 INCHANGÉ** — aucune donnée utilisateur, pas de bump X, pas d'entrée d'invariant 1, spec `docs/specs/FX-CONVERTER-currency-widget.md`) : widget flottant calqué sur le Pomodoro (`initFxWidget(cfg)` dans `_shared.js.html`, parité owner/share/hub, `cfg={fetchUrl}` par page). **Backend** : taux BCE base EUR via **Frankfurter** (`api.frankfurter.app`, gratuit/sans clé, **urllib stdlib** — `requirements.txt` inchangé), **cache technique** `app_state.fx_cache` (`{date, base, rates, fetched_day, fetched_at}`), **refresh paresseux 1 fetch/jour max** (gate = jour de fetch **Europe/Paris** via `_APP_TZ` — robuste aux week-ends où la date BCE ne change pas ; aucun cron/thread ; multi-workers = dernière écriture gagne, sans conséquence). Échec réseau → sert le dernier cache ; jamais de cache → `{date:null, rates:null}` ; **jamais de 500/crash** ; cache **auto-réparant** (valeur corrompue → refetch). Helpers `_fx_today`/`_fx_fetch_rates`/`_fx_rates`. **Exclusions** : NON exporté (`_build_export` n'émet l'`app_state` que clé par clé — ré-import = 0 ajout) ; **exclu de `_data_version`** comme `activity_seen_at` (volatile → pas de faux « données modifiées » sur le poll 15 s). **Routes** (lecture seule) : owner `GET /api/fx` (derrière Authelia), invité `GET /share/<token>/fx` + hub `GET /share/hub/<hub_token>/fx` — **token/hub revalidé, PAS d'approbation ni `can_edit`** (taux publics), **sous `/share/*`** (bypass Authelia existant, invariant 5, aucun préfixe de 1er niveau ; token invalide → 404, jamais de redirect Authelia). **Front** (invariant 6 pas de lib/CDN navigateur — le seul appel externe est le fetch **serveur** ; invariant 8 pas de GSAP ; invariant 9 tokens/classes existants) : lanceur `#fx-launch` (`¥€`) + panneau `#fx-panel` **déplaçables** (Pointer Events souris+tactile, `clampPos`, positions persistées `fxPos`/`fxLaunchPos`), ✕ masquer + pastille `#fx-reshow` + préférence `fxHidden` (case Paramètres owner `#fx-visible-cb` à côté du Pomodoro ; share/hub via ✕/pastille). Conversion **bidirectionnelle live** (2 champs + 2 sélecteurs + swap `↔`), taux croisé **côté client** (`montant × rate(to)/rate(from)`, EUR=1), arrondi 2 déc. (**0 pour JPY**), couple persisté `fxPair` (défaut **JPY→EUR**), sélecteurs JPY/EUR/USD/GBP/CHF en tête puis alpha, ligne « taux BCE du {date} » (surlignée `--yellow` si date servie ≠ aujourd'hui = repli réseau).
- **Juillet 2026 — train quick wins** ([V23.3.91→95], frontend pur, **export v23 inchangé**, spec `docs/specs/HEADER-ADD-CONTEXT-…`) : **[HEADER-ADD-CONTEXT]** « + » contextuel par bloc en mobile ≤ 900 px (en-tête « 🔗 Liens » + mini-menu Lien/Catégorie via `card-menu` ; « + » du summary Mémos → focus `#memo-quick` ; desktop inchangé) ; **[SHARE-OPEN-AS-GUEST]** bouton ↗ « Ouvrir en tant qu'invité » (`guestOpenBtn`, `window.open _blank noopener`) à côté des 4 📋 de la vue Partages ; **[MEMO-TITLE-FALLBACK]** mémo sans titre → accroche du contenu en style titre (gras) sans dupliquer le texte (helpers partagés `memoLeadText`/`memoRestText`/`MEMO_EMPTY_LABEL`), card vide corrigée (HTML sans texte → placeholder), placeholders harmonisés ; **[MEMO-EDITOR-IMAGES]** section « 📷 Photos » dans la pop-in d'édition (helper partagé `renderMemoPhotos`, routes/flux images de la card, parité owner/share/hub `can_edit`, refresh sans reload complet, masquée si vide + lecture seule) ; **[VIEWER-FILMSTRIP]** bande de vignettes défilable dans `runImageViewer` (> 1 image ; owner/share/hub/carte d'un coup ; courante en accent, clic = saut direct, auto-scroll, style auto-contenu).
- **Juillet 2026 — images dérivées côté serveur** ([IMAGE-THUMBS] V23.4.96, **export v23 INCHANGÉ** — donnée dérivée, pattern `image_meta`, spec `docs/specs/IMAGE-THUMBS-derived-images.md`) : **Pillow** (épinglé `requirements.txt`) génère deux tailles JPEG q82 (orientation EXIF via `ImageOps.exif_transpose`) dans **`data/uploads/derived/`** — **`t_`** ~400 px (vignettes cards/bande/carte/section 📷) et **`s_`** ~1600 px (image de la visionneuse). **Originaux jamais modifiés** ; ⬇ = brut. Servies par **`?size=t|s`** sur les routes image EXISTANTES (owner `/uploads/<nom>`, invité `/share/<token>/image/<nom>` — **scope invité inchangé, invariant 5** ; sans param / valeur inconnue / GIF / image < cible / échec Pillow → **original**, jamais de 404). Génération à l'upload (`_gen_derived` dans `_save_uploaded_image`) + **backfill daemon** idempotent (`_backfill_derived`, modèle de `_backfill_image_meta`) + filet « génère à la volée » (`_derived_ready`). **Purge en cascade** avec l'original (`_delete_derived` via `_delete_image_files` + les 2 suppressions per-image). **Jamais exporté** (l'export ne porte que les noms d'originaux) ni dans `_data_version`. Front : helper partagé `imgSize(url, sz)` (thumbs = `t`, image visionneuse = `s`, ⬇/EXIF = original). Gain mesuré : photo 9,2 Mo → vignette `?size=t` ≈ 20 Ko.
- **Juillet 2026 — noms de dossiers dupliqués (unicité par dossier parent)** ([PROJECT-NAME-PER-FOLDER] V25.0.113, ⚠️ **BUMP EXPORT v24 → v25**, spec `docs/specs/PROJECT-NAME-PER-FOLDER-duplicate-names.md`) : fin de l'`projects.name TEXT NOT NULL UNIQUE` **globale** (héritage d'avant les projets imbriqués). **D1** unicité **PAR PARENT** (frères, racine incluse — `COALESCE(parent_id,0)`, les NULL SQLite étant distincts) : « restaurants » sous deux voyages devient légal, homonymes frères interdits. **D2** `uid` stable sur les projets (invariant 3, backfill `init_db()`). **D3** bump v25. **D4** collision au déplacement (D&D/PUT `parent_id`) → **409**, owner **et** invité, jamais de 500. **Migration** (`init_db`, idempotente, sûre multi-workers) : rebuild de `projects` (pattern [FAVORITES V1.1] rename→create sans UNIQUE→port en préservant les `id`→drop) + `CREATE UNIQUE INDEX ux_projects_name_parent` + colonne `uid` + backfill. **Backend** : garde unique `_sibling_name_taken(db, name, parent, exclude_id)` + message « un dossier de ce nom existe déjà à cet endroit » sur `create_project` (racine), `update_project` (rename **et** déplacement — fin du 500 latent), `share_add_project`/`share_update_project` (scopées au parent visé, invariant 5), provisioning d'espace invité (dédup re-scopée au parent). **Export v25** : chaque projet émet `uid` + `parent_uid` (name/parent nom **restent** émis), chaque mémo `project_uid` ; [EXPORT-SUBTREE] doublé `proj_uids`. **Import** : résolution projet **uid-d'abord** puis **(nom, parent résolu)** — création **topologique** (parent résolu AVANT insert → aucune collision « tout à la racine »), mémos par `project_uid` puis nom ; upsert par uid **non destructif** (nom local jamais écrasé — pas d'`updated_at` projet, on ne détruit pas un renommage local) ; « Importer ici » évalue la racine nouvelle par uid-puis-chemin ; dry-run [IMPORT-PREVIEW] merge/new uid-d'abord. Compat v1→v24 : sans uid, résolution (nom, parent) — vieux exports = noms globalement uniques → chemin toujours résolvable → rendu identique (invariant 2). **Front** : `api()` owner remonte le message serveur aux pop-ins ; D&D projet→projet 409 = `notify()` + `loadAll()` (revert de l'état fantôme) ; guests parsent déjà `e.error`. Tests (test client sur copie) 33/33 : migration (ids préservés, uid posés, idempotent), unicité par parent (racine + sous-dossiers, D4), rename→409 (plus de 500), export v25, ré-import v25 = 0 ajout, import v24 réel = 0 doublon, renommage local préservé, sous-arbre uid, provisioning, routes invité.
- **Juillet 2026 — capture rapide de mémo (widget flottant + note rapide sticky)** ([QUICK-MEMO] + [MEMO-QUICK-STICKY] V25.1.114, **frontend pur, export v25 INCHANGÉ, zéro route/schéma** — `app.py` non touché, spec `docs/specs/QUICK-MEMO-widget-capture-rapide.md`) : helper partagé **`initQuickMemo(cfg)`** + **`qmFolderOptions(projects)`** dans `_shared.js.html` (gabarit Pomodoro/FX à l'identique, invariant 9) — lanceur flottant ✏️ + panneau déplaçables (Pointer Events, `clampPos`, positions persistées `qmPos`/`qmLaunchPos`), masquable (`qmHidden` + pastille re-show + case Paramètres à côté de Pomodoro/FX), titre + textarea + sélecteur de dossier indenté (📥 Inbox en tête), défaut = dossier courant du board sinon Inbox, rafale (panneau reste ouvert), `flashSaved()`. Owner → `POST /api/memos` (+ file Inbox idempotente [OFFLINE] hors ligne, D5). Invité → `POST /share/<token>/memos` EXISTANT, affiché seulement si `canAddNow()` ([GUEST-ROLES]), scope revalidé serveur (invariant 5, zéro route). **[QUICK-MEMO-HUB] V1.1 (V25.1.115, D4 révisée)** : le widget est aussi activé dans `hub.html` (les invités réels passent par le hub [ONE-LINK-MULTI], pas par `/share` direct) via `maybeInitQuickMemo()` dans `render()` — sélecteur **multi-partages** (options = `editableProjects()` groupées en `<optgroup>` par partage, chaque option porte `share_token|project_id`, `cfg.inbox:false`, défaut = dossier ouvert `FOCUS` sinon 1ʳᵉ racine éditable), submit sur le `POST /share/<token>/memos` du partage choisi (X-Guest-Token du token, invariant 5) ; hub 100 % lecture = pas de widget. Le widget générique (partial) accepte désormais une liste de dossiers **plate OU groupée** et `cfg.inbox` pour masquer l'Inbox générique. Widget hors des zones re-rendues (survit au poll 15 s). **[MEMO-QUICK-STICKY]** : `#memo-quick` enveloppé `#memo-quick-wrap` sticky bas (fond opaque `var(--panel)` + z-index) + « ＋ » d'en-tête MÉMOS révélé desktop. **[MEMO-JUMP-BOTTOM] (V25.1.116)** : bouton ⤓ « Aller en bas » / ⤒ « Remonter » dans l'en-tête MÉMOS (`#memo-jump-btn`, gabarit `.mini-add-btn`, `preventDefault`+`stopPropagation` dans le `<summary>`) — scroll fluide de la colonne (fenêtre), toggle par détection au scroll ; l'en-tête MÉMOS devient **sticky-haut** (`top:var(--header-h)`, miroir de la note sticky-bas) pour rester cliquable, statique en mobile ≤ 900. **[MEMO-JUMP-VIEW] (V25.1.117)** : mêmes ⤓/⤒ dans la **vue Mémos** — helper `memoJumpBar(dir)` (recréé par `renderBoard`, sans état) rend 2 barres `.memo-jump-bar` (bouton `.prio-btn` discret sans accent) : ⤓ sous `#memo-add-bar` (au-dessus de la 1ʳᵉ section), ⤒ après la dernière section ; affichées seulement si **> 8 mémos** (comptage DOM `.task` ; `done>8` pour le filtre Terminés ; `entries>8` pour l'Historique). Invariants 6/8/9. `node --check` OK, rebuild OK, export toujours 25.
- **Juillet 2026 — pop-in « jour » de l'Agenda** ([AGENDA-DAY-POPIN] V25.2.118, spec `docs/specs/AGENDA-DAY-POPIN-popin-jour.md`, **frontend pur 3 pages + partial, export v25 INCHANGÉ, zéro route/schéma**, `app.py` intact) : helper partagé **`openAgendaDay(cfg, iso)`** + `isoShift` dans `_shared.js.html` — **`<dialog>` natif** (hérite du style `dialog` + `dlg-in`, jamais de GSAP — invariant 8 ; `uiBusy()` couvre le poll), créé une fois par page, re-rendu à l'ouverture/nav. Remplace le panneau du jour sous la grille + l'état `agendaSelDay` (D1). Lignes ≥ 44 px (pastille + titre + heure badge + chevron) → éditeur (pop-in fermée avant, un seul top-layer) ; nav **‹ jour ›** franchissant les mois (`onNav` maj `agendaYM`) ; **« ＋ Mémo ce jour »** = création EXISTANTE avec `due_date` préremplie (owner add-bar via `agendaPendingDate` ; share/hub via `/memos` + PUT date, gardes de rôle) ; **« +N » = vrai bouton** ; en-têtes de jour tappables en mobile ; D&D souris intact + **garde anti clic-fantôme** post-drop (`agendaDropGuard`, D7). Source unique par page (`agendaSource`/`shareAgendaSrc`/`hubAgendaSrc`). CSS `.agd-*` aux tokens (3 pages). **Amendement drill-down [V25.2.119]** : à la fermeture de l'éditeur ouvert DEPUIS la pop-in jour (save/annuler/Échap), la pop-in se **rouvre sur le même jour** — helper en état module (`agdCfg`/`agdIso`), `_agdEditorArm` accroche un `close` one-shot sur l'éditeur (sync row-path, sondé pour la création async), `agendaDayRefresh()` (en tête des 3 rendus agenda) re-render la pop-in ouverte depuis les données fraîches après reload ; « ＋ Mémo ce jour » unifié en création→éditeur→retour (owner 1 `POST` avec `due_date` ; share/hub `POST`+`PUT`). Invariants 5/6/8/9. `node --check` 4 surfaces OK, rebuild OK, export 25.
- **Juillet 2026 — tailles de pop-ins** ([DIALOG-SIZE] V25.2.120, frontend pur, export v25 inchangé) : base `dialog` owner 450→**min(560px,92vw)** + classes `.dlg-wide` (680) / `.dlg-compact` (420) ; riches (`#create-dialog`, `#import-preview-dialog`, `#vote-manage-dialog`) en 680, confirmations (`#confirm-dialog`, `#notify-dialog`) en 420 ; `#memo-edit-dialog` (640) + carte/aperçu image inchangés. Hub base 620 gardée + mêmes classes ; share `dialog` = **wrapper transparent** (largeur = contenu, bump non applicable) + **fix** de la carte de la pop-in jour share (fond/bordure). Invariant 9.
- **Juillet 2026 — espace personnel de l'invité (hors partages) + retrait du provisioning `guest_space`** ([GUEST-HOME] V26.0.126, ⚠️ **BUMP EXPORT v25 → v26**, spec `docs/specs/GUEST-HOME-espace-perso-invite.md`, D1-D6) : à **l'approbation** (register PIN — nouveaux ET ré-approbation d'un existant —, `hub_approve`, approbation manuelle owner `update_guest`, octroi owner `/api/guests/grant`), un invité reçoit — **UNE seule fois par e-mail** — SON espace, HORS de tout dossier partagé. Helper **idempotent `_ensure_guest_home(db, email, name)`** : **dédup GLOBALE par e-mail** (`_find_guest_home` = scan `projects.created_by` matché par `_voter_email`, robuste au renommage [GUEST-EDIT]) → si l'espace existe, **aucun dossier créé**, on (re)garantit juste l'accès (D1). **Emplacement** : dossier racine **« 👥 Invités »** (`created_by=''`, owner, créé au 1ᵉʳ besoin) contenant un **« 🏠 Prénom »** par invité (`created_by = « Nom <email> »`) ; prénom = 1ᵉʳ mot du nom, repli partie locale e-mail, collision de frères (interdite v25) → suffixe « · email-local » (D2). **Accès = partage dédié** `shares` kind=project **role=editor** + PIN aléatoire + `share_guests` **auto-approuvé** (`_ensure_home_access`, idempotent) → l'espace apparaît dans le **hub** sans PIN à saisir ; droits couverts par role editor + **`_owns_guest_space_folder`** (created_by) = éditeur plein dans SON sous-arbre, **CONSERVÉ** (invariant 5) (D3). **RETRAIT de l'ancien provisioning** : `_provision_guest_spaces` + ses 2 appels (`hub_data`/`share_data`) + le flag/UI « 🏠 Espace invités » des pop-ins projet supprimés ; **colonne `projects.guest_space` gardée MORTE** (invariant 1, pas de migration destructive), plus exposée par `/api/projects` ni écrite par `update_project` ; **`role_floor` (zone) conservé** (D4). L'owner garde la main (« 👥 Invités » = projets normaux supprimables, cascade partages) (D5). **BUMP v26** : les projets émettent `created_by` (import restauré uid-d'abord, création + enrichissement non destructif, absent v1→v25 = ''), partages/guests toujours non exportés → une ré-approbation raccroche l'espace via la dédup created_by (D6). Tests (test client sur copie) : migration OK, `guest_space` non exposé, export v26 + created_by sur chaque projet, ré-import = 0 ajout, import v25 sans created_by = OK, register PIN → « 👥 Invités »/« 🏠 Prénom » + partage editor + guest approuvé, idempotence (2ᵉ lien = 0 espace), 2 invités = 2 espaces, collision prénom → suffixe, restauration sur base VIERGE + ré-approbation = raccroché (pas dupliqué). `py_compile` + `node --check` OK.
- **30 juillet 2026 — naissance du design system « LUCIOLE » + 3 lots UI** (série **V26.5.141 → V26.10.151**, **frontend pur, export v26 INCHANGÉ, zéro route/schéma**, `app.py` intact ; doc de référence **`docs/design/LUCIOLE-boutons.md`**, pointée par l'invariant 9) : journée d'itérations Fabien × (Claude Code + Cowork) qui donne un **langage de boutons unique aux 3 pages**. **[ADD-BAR-POLISH] V26.5.141** sort « Ajouter » de l'aplat accent plein (gabarit des boutons de card) et supprime le chevron de « Détails » (état porté par la classe `.on`). **[ADD-BAR-GLOW] V26.6.142-143 + V26.7.145-146** construisent le style : bordure fine accent **figée**, **lueur « caméléon »** qui suit la souris (lissage rAF 0.11, boucle active seulement au survol, interpolation hex maison — zéro lib, invariant 6), **micro-lift** 2 thèmes, **fond imprégné** au survol, mode **clair « verre » monochrome** (accent réservé au geste, spot bleu profond `#1e88e5`), état déplié dit par la **matière + le libellé** « Détails » ⇄ « Réduire ». **[HDR-GLOW] V26.7.144** porte la lueur sur la barre d'actions du board via le partial (parité 3 pages ; lueur clippée dans `.hdr-glowwrap` — ne JAMAIS poser `overflow:hidden` sur `.hdr-btn`, gotcha V26.2.136). **[LUCIOLE-POPIN] V26.7.147** convertit le pied de l'éditeur de mémo (`lucioleize`, variantes `.luc-neutral`/`.luc-primary`) et **[VERSIONS-ICON] V26.7.148** remplace l'emoji 📜 par l'icône trait `#ic-history` (`currentColor`). Puis 3 lots fonctionnels : **[TRASH-PREVIEW] V26.8.149** (aperçu **lecture seule** d'un mémo de la corbeille avant restauration/purge — contenu riche au même pipeline que les cards mais checklists inertes, sous-tâches, photos `?size=t`, fichiers ; pied Luciole + danger **rouge** ; zéro route, `/api/trash` renvoyait déjà les mémos complets) ; **[GUEST-ADD-BAR] V26.9.150** (barre d'ajout invitée alignée sur `[ADD-BAR-V2]` owner via le nouveau helper partagé **`guestMemoAddBar(cfg)`** — rangée 2 repliable, écriture `POST /share/<t>/memos` **puis** `PUT /share/<t>/memo/<id>` existants, **zéro route**, invariant 5 ; sélecteurs harmonisés « nom seul » ; « Créer » en icône dossier trait + Luciole neutre) ; **[SUBHIDE-V2] V26.10.151** (chips `[BOARD-SUBHIDE]` dans un **cadre « SOUS-DOSSIERS »** collé à la liste + actions **Tous/Aucun/Inverser**, **double-clic = « seulement lui »** et **appui long 0,5 s** en tactile, `⤓ Aller en bas` hors du cadre et porté aux invités — masquage toujours **affichage seul**, tuiles/compteurs/carte au périmètre complet). **Mutualisation** : `mabMixHex`/`attachLuciole`/`mabGlowButton`/`lucioleize`, `guestMemoAddBar` et `memoJumpBar` vivent dans `templates/partials/_shared.js.html` (fonctions pures — règle du partial, ADR-001), `hdrMixHex` dédoublonné ; le **CSS Luciole reste par page** (convention inchangée).

- **30 juillet 2026 — liens symétriques entre mémos** ([MEMO-LINKS] **tranche A**, V27.0.153, ⚠️ **BUMP EXPORT v26 → v27**, spec `docs/specs/MEMO-LINKS-liens-memos.md` — tranches B (exposition consentie) et C ([TIMELINE-SUGGESTIONS]) **hors périmètre**) : un mémo peut être **rattaché à un autre** (le resto « Zà Zà » à l'« Hôtel Florence 28→31 »), lien **symétrique** visible des deux côtés. **D1** table additive `memo_links` + index unique de **paire non ordonnée** (détail dans l'invariant 1) ; corbeille = liens masqués mais conservés, purge = cascade. **D2** section **« 🔗 Relié à »** dans l'éditeur des 3 pages (helper partagé `memoLinksSection`) : picker façon recherche globale qui classe les **géolocalisés PROCHES en tête** (« à 450 m », haversine `geoDistanceM` du partial), puis même dossier, puis pertinence texte ; chip + **×** pour délier. **D3** chips sur les **cards des deux côtés** (helper `memoLinkChips`) : bordure **pleine** « 🔗 titre » côté source, **pointillée** « ⤺ titre » côté référencé, max 3 + « +n », titres en **texte** (jamais de HTML) ; clic = ouvre l'éditeur du mémo relié (mécanique [MEMO-CARD-CLICK]). **D4 droits** : owner tout ; invité **approuvé** + capacité d'écriture (matrice [GUEST-ROLES], action `own`) sur les **deux** mémos, tous deux **dans le scope du partage** — mémo hors scope (espace perso, autre partage) → **403 explicite** (l'exposition consentie est la tranche B) ; routes invité sous **`/share/…`** (bypass existant, invariant 5) ; les liens voyagent dans les payloads mémo **existants** (`links`), **zéro route de lecture**, et un lien hors scope du lecteur est **omis**. **D8** export v27 (voir invariant 1). Tests (test client, **copie** de base) **26/26** : migration, symétrie in/out, une seule ligne par paire, auto-lien 400, plafond 20, corbeille masque/restaure, purge cascade, export par uid, ré-import = 0 doublon, import v26 sans `memo_links` = OK, uid inconnu ignoré. **Sauvegarde export v27 à refaire par Fabien après déploiement.**

## Sauvegardes

Thread quotidien (`_backup_loop`, démarré à l'import) : export JSON + copie SQLite (`conn.backup()`) dans `data/backups/`, rotation `BACKUP_KEEP_DAYS` (7 j). Idempotent par fichier-du-jour (multi-workers OK). Ne jamais committer `data/`.

## Backlog

Voir [IDEAS.md](IDEAS.md). Le README documente le modèle de données et l'API complète.

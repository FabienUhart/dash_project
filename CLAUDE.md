# CLAUDE.md

Contexte pour Claude Code. À lire avant toute modification ou commit.

## Le projet en une phrase

Dashboard perso auto-hébergé sur un Zimaboard (page d'accueil navigateur) : liens vers services self-hosted avec catégories, mémos post-it, statuts online/offline, recherche, horloge/météo.

## Architecture

- **`app.py`** : tout le backend. Flask + SQLite, pas d'ORM, pas de blueprint. La migration de schéma est dans `init_db()` (ALTER TABLE additifs + backfill, jamais destructif — ne JAMAIS dropper de colonne ou de données).
- **`templates/index.html`** : tout le frontend en un seul fichier (CSS + HTML + JS vanilla, pas de framework, pas de build). Layout "3 zones" : sidebar catégories / cards / colonne mémos.
- **`data/dashboard.db`** : SQLite, monté en volume Docker. Contient les vraies données de l'utilisateur — ne jamais la modifier ou supprimer dans un commit.
- **`data/uploads/`** : images des mémos (noms `uuid4().hex.ext`, validés par regex côté serveur). Même volume Docker, même règle : ne jamais y toucher dans un commit. Le JSON d'export ne contient que les noms de fichiers.
- Déploiement : `docker compose up -d --build` (gunicorn, port 8099, derrière Caddy + Authelia en prod).

## Invariants à respecter

1. **Compat ascendante des sauvegardes** : `/api/import` doit toujours accepter les exports v1 (liens seuls), v2 (+ catégories/mémos), v3 (+ uid/dates), v4 (+ done/due_date/priority/subtasks sur les mémos, color sur les catégories), v5 (+ projets de mémos, rattachés par nom), v6 (+ images sur les mémos, noms de fichiers seulement), v7 (+ tags sur les liens, normalisés minuscules sans #), v8 (+ tags sur les projets), v9 (+ `recurrence` sur les mémos et liste `history` des tâches effectuées, dédupliquée par (memo_uid, done_at)), v10 (+ liste `priorities` ; `memos.priority` est remappé par NOM de priorité à l'import, jamais par id brut) v11 (+ `emoji` sur catégories, projets et mémos), v12 (+ `parent` sur les projets, résolu par NOM en deux passes à l'import, jamais par id), v13 (+ `location` JSON {lat, lng, label} sur mémos et projets, validée par `_clean_location()` — coordonnées bornées, jamais de HTML) v14 (+ `title` et `assignees` (liste de noms libres, dédupliquée, nettoyée par `_assignees_json()`) sur les mémos, `description` sur les projets, liste `comments` à la racine — rattachés par `memo_uid`, dédupliqués par (memo_uid, created_at, author) ; un mémo peut avoir un titre SANS contenu : la validation est « titre OU contenu », et la dédup sans uid utilise le titre quand le contenu est vide) et v15 (+ `priority` (0-3) et `parent_created_at` sur les `comments` — le `parent_id` est résolu à l'import en rattachant le commentaire dont le (memo_uid, created_at) correspond, donc les réponses survivent ; les mémos en corbeille — `deleted_at` non vide — sont **exclus** de l'export ; le statut « vu » (`comment_seen`) n'est PAS exporté). Toute évolution du format incrémente `version` dans l'export et reste importable.
2. **L'import n'est jamais destructif** : il ajoute, met à jour (uid identique + `updated_at` plus récent) ou enrichit les champs vides (match nom+URLs sans uid). Il ne supprime ni n'écrase jamais un champ rempli avec une donnée plus ancienne.
3. **`uid`** : UUID stable qui suit chaque lien/mémo à travers exports/imports. Généré à la création et backfillé par `init_db()`. Ne jamais le régénérer pour une ligne existante.
4. **Les URLs locales** (`192.168.1.x`) servent au check de statut ET à la récupération des favicons côté serveur (contournement d'Authelia). Le cache favicon (`_favicon_cache`, en mémoire) doit être invalidé quand les URLs d'un lien changent.
5. **Pas d'auth dans l'app** : la sécurité est assurée par le reverse proxy. Ne pas ajouter de login. **Exception volontaire** : les routes `/share/<token>/...` sont publiques (règle bypass Authelia) et protégées uniquement par le jeton — elles ne doivent JAMAIS exposer autre chose que la ressource partagée (mémo ou projet ciblé, ses sous-tâches et ses images). Toute écriture via `/share/` exige en plus un invité **approuvé** (header `X-Guest-Token`, approbation obtenue par le code PIN du lien). Toute nouvelle route publique doit valider le token et rester dans ce périmètre. Les uploads d'images (propriétaire comme invité) passent par `_save_uploaded_image()` qui vérifie la **signature binaire** du fichier — ne jamais accepter un upload sans cette vérification.
6. **Un seul fichier HTML** : pas de séparation CSS/JS du code maison, pas de build. Dépendances front autorisées uniquement si **auto-hébergées dans `static/`** (actuellement : Quill 2 pour l'édition riche des mémos — dégrade en textarea si le fichier manque ; `gsap.min.js` pour les animations — dégrade proprement si absent ; `leaflet.js`/`leaflet.css` ; `favicon.svg`). Pour les invités, ces fichiers sont servis par la route publique `/share/assets/<nom>` (liste blanche `SHARE_ASSETS`) car `/static/` est derrière Authelia. Pas de CDN au runtime, sauf les appels existants : open-meteo, icons.duckduckgo.com (fallback favicon), nominatim.openstreetmap.org (géocodage à la sauvegarde uniquement, échec silencieux) et tile.openstreetmap.org (tuiles de la carte Leaflet, auto-hébergée dans `static/` : `leaflet.js` + `leaflet.css`, marqueurs vectoriels `circleMarker` donc aucune image Leaflet requise).
7. **Suppression douce** : supprimer un mémo (UI propriétaire OU invité) ne fait JAMAIS de `DELETE` SQL — ça pose `memos.deleted_at` (corbeille). Les mémos en corbeille sont exclus de `/api/memos`, du scope partage et de l'export. La purge définitive (fichiers images + shares + commentaires) n'arrive QUE via la corbeille (routes `/api/trash`) ou la purge auto `_purge_trash()` après `BACKUP_KEEP_DAYS` jours. La restauration owner-only est `/api/trash/<id>/restore` (≠ `/api/memos/<id>/restore` qui restaure une *révision*).
8. **Animations** : ne JAMAIS animer un élément `<dialog>` lui-même via GSAP (`y`/`scale`/`autoAlpha`) — poser un transform GSAP sur un dialog le sort du top-layer Chromium et casse le centrage + le `::backdrop`. L'entrée des pop-ins est gérée en CSS (`@keyframes dlg-in`/`dlg-bd` sur `dialog[open]`, sous `prefers-reduced-motion`). GSAP n'anime que des éléments hors top-layer (cards, sidebar, tuiles) ou des enfants d'un dialog, jamais le dialog.

## Comment tester avant de commit

```bash
# syntaxe
python3 -m py_compile app.py

# lancer sur une COPIE de la base (jamais sur data/dashboard.db directement)
cp data/dashboard.db /tmp/test.db
DB_PATH=/tmp/test.db flask --app app run -p 8099

# scénarios critiques à vérifier :
# 1. la migration passe sur une base existante (l'app démarre, GET /api/links OK)
# 2. ré-import d'un export complet → 0 ajout, tout en "skipped"
# 3. import v1 sans uid → pas de doublon, champs vides enrichis
# 4. CRUD catégories/mémos + reorder
```

`backup*.json` est gitignoré (données perso) — ne jamais le committer.

## Historique des évolutions majeures

- **v1 (origine)** : liste de liens à plat, 2 URLs par lien, statut, mémo par lien, export/import basique.
- **Juin 2026 — refonte "3 zones"** : catégories (table + sidebar + drag & drop), mémos indépendants (table `memos`, colonne post-its), recherche instantanée (`/`), favicons via serveur, horloge + météo, panneau Paramètres (export/import/gestion catégories), responsive mobile, import anti-doublons puis import avec `uid` + dates (mise à jour des éléments connus au lieu de dupliquer).
- **Juin 2026 — vue Mémos façon Planify** : page dédiée (sidebar → Mémos) avec tuiles de filtres, sections par échéance (En retard / Aujourd'hui / À venir / Sans date / Terminés repliable), cases à cocher, priorités P1-P3, sous-tâches JSON, liens cliquables, couleurs de catégories (export v4), puis projets de mémos avec Inbox, couleurs et drag & drop depuis la sidebar (export v5), puis images sur les mémos (upload dans `data/uploads/`, miniatures + visionneuse, export v6), puis tags `#` reliant liens et mémos + pop-in de création projet/catégorie avec couleur (export v7), puis tags sur les projets + recherche scopée (sélecteur Tout/Liens/Mémos/Projets + préfixes `p#` `l#` `m#`) et remplacement de tous les confirm/alert natifs par des pop-ins (export v8), puis tâches récurrentes (cocher replanifie sur le rythme prévu, `_next_due()`) + historique des tâches effectuées (table `memo_history`, tuile 📜, purge manuelle) (export v9), puis priorités configurables (table `priorities` seedée P1 rouge/P2 jaune/P3 vert, couleurs dynamiques, gestion + pop-in dans Paramètres) (export v10), puis partage externe par lien à jeton (table `shares`, routes publiques `/share/<token>`, page `templates/share.html`, lecture seule ou modifiable, bypass Authelia requis — non inclus dans l'export), puis invités identifiés par e-mail validés par le propriétaire (table `share_guests`, header `X-Guest-Token`), attribution des modifications et versions restaurables (`memo_revisions` avant/après, fil 🔔 Activité, restauration owner-only via `/api/memos/<id>/restore`), puis attribution visible (badge 👤 sur les mémos touchés par un invité, champ `guest_editor` dans `/api/memos`) + rechargement auto du dashboard toutes les 15 s (sauf pop-in ouverte ou édition en cours), puis listes à cocher façon Evernote (Quill `list: check`, `li[data-list]` cliquables sur la card ET la page partagée, champ "+ Ajouter un article" pour les invités), droits invités complets (texte, date, priorité, photos JPG/PNG validées par **signature binaire** — `_looks_like_image()`, gif/webp réservés au propriétaire), puis code PIN à 4 chiffres par lien (`shares.pin`, généré/modifiable, code correct = invité approuvé d'office) et page 🔗 Partages (vue dédiée : liens + PIN éditables, invités, modifications groupées par invité avec avant/après dépliable — remplace la pop-in Activité), enfin double-clic/clic droit sur catégories-projets de la sidebar pour ouvrir la pop-in d'édition (avec bouton Supprimer), et emojis personnalisables sur catégories, projets et mémos (champ + palette dans la pop-in, input dans le détail du mémo, affichés sidebar/cards/board/page partagée) (export v11), puis projets imbriqués (`projects.parent_id` avec anti-cycle via `_resolve_parent()`, drag & drop projet→projet dans la sidebar, arbre indenté, la vue ET le partage d'un parent incluent récursivement les mémos des descendants — avertissement à la création du lien) (export v12).
- **Juin 2026 — parité page partagée + gouvernance + backup** : footer version (`APP_VERSION`, affiché "v12 · créé par Fabien U."), la page partagée d'un projet devient un mini-dashboard pour invités validés — sidebar hiérarchique (navigation, compteurs), tuiles de filtres scopées, création de mémos (sélecteur de projet) et de **sous-projets**, drag & drop mémo→projet et projet→projet, édition de projets par double-clic/clic droit (nom/emoji/couleur, renommages journalisés), éditeur **Quill** (servi via `/share/assets/` car `/static` est derrière Authelia), notes libres par défaut avec listes à cocher opt-in (bouton ☑ Liste) — le tout strictement cantonné au périmètre du partage (routes `PUT /share/<t>/memo|project`, scope vérifié serveur). Côté propriétaire : récap des invités sous le titre des projets/mémos partagés (chips ✏️/👁), et dans la page 🔗 Partages une section **INVITÉS par personne** (tous les accès d'un e-mail, hiérarchie complète dépliée, droits, dates, copie de lien, refus/retrait ciblé). Emojis et points de couleur affichés **côte à côte** (`comboIcon()`). Enfin **sauvegardes automatiques quotidiennes** (voir section Sauvegardes), horloges multi-fuseaux (heure de Paris auto hors de France + fuseau choisi en localStorage), et **géolocalisation** sur mémos et projets (capture GPS navigateur, badge 📍 → lien OpenStreetMap — pas de carte embarquée ni de géocodage externe, invités inclus via le partage) (export v13, APP_VERSION "13").
- **Juin 2026 — labels courts + liste des points carte** : `_geocode_search()` renvoie un label court (« nom, ville » via addressdetails Nominatim ; l'adresse complète reste dans la clef `full` pour le sélecteur, filtrée par `_clean_location()` au stockage), badges plafonnés en largeur (ellipsis, adresse complète au survol), et le dialog carte (dashboard ET page partagée) gagne un panneau latéral listant les points — clic = centrage + popup.
- **Juin 2026 — v14 : titres, descriptions, assignés, commentaires** : champ `title` sur les mémos (gras sur la card, input 🏷 dans le détail et dans les outils invités, mémo valide avec titre seul), `description` sur les projets (textarea dans la pop-in projet, affichée sous le titre du board — éditable au clic — et sur la page partagée, modifiable par les invités via leur pop-in projet), **assignés multiples** `memos.assignees` (saisie libre — on peut rattacher quelqu'un qui n'est PAS encore invité et l'affilier plus tard —, suggestions datalist = « Fabien » + invités approuvés + noms déjà utilisés, badges @nom sur les cards, inclus dans la recherche), et **commentaires signés** (table `memo_comments`, fil sous le détail du mémo, badge 💬 n cliquable, routes owner GET/POST `/api/memos/<id>/comments` + DELETE `/api/comments/<id>` owner-only, route invité POST `/share/<t>/memo/<id>/comments` — can_edit + invité approuvé + scope vérifié —, auteur invité affiché en violet 👤, commentaires invités comptés dans le badge d'activité 🔔, inclus dans `share_data` et dans l'export/import v14). `share_data` expose aussi `members` (propriétaire + invités approuvés du lien) pour les suggestions d'assignés côté invité (export v14, APP_VERSION "14").
- **Juin 2026 — refonte visuelle + corbeille + QR + commentaires v2** (export v15, APP_VERSION "15") :
  - **Refonte visuelle** (tokens CSS resserrés, glass header, cards avec hover, focus-rings) et **GSAP** (`static/gsap.min.js`, self-hosté, whitelisté `/share/assets/`) : stagger des cards/tâches, count-up des tuiles, intro header/sidebar. ⚠️ l'entrée des **pop-ins est en CSS** (`@keyframes dlg-in`/`dlg-bd`), pas GSAP — voir invariant 8.
  - **Mentions `@`** : blot Quill `mention` (`<span class="mention" data-mention>`) + menu d'autocomplétion dans l'éditeur ET dans le champ commentaire (`attachInputMention`). Sélection = insère `@Nom` + ajoute aux assignés. Source = `assigneeSuggestions()` / `shareSuggestions()` (toi + invités + noms déjà utilisés, saisie libre). La pop-in « Modifier le mémo » de la page partagée a enfin un champ assignés général (`#q-people`) + un titre (`#q-title`).
  - **Signal d'accès** : un assigné/mention qui n'a pas accès au partage s'affiche en **ambre `⚠ @Nom`** (badge, puce, mention inline). Dashboard : accès = propriétaire ou invité approuvé d'un partage couvrant le projet du mémo (ou un ancêtre). Partage : accès = `DATA.members`.
  - **Corbeille** (invariant 7) : `memos.deleted_at`, vue 🗑 sidebar (Restaurer / Supprimer définitivement / Vider), purge auto 7 j (`_purge_trash`). Côté partage, l'invité approuvé peut supprimer (→ corbeille) et restaurer (tuile 🗑 ; `trash` dans `share_data`, routes `DELETE`/`POST .../restore` et `.../seen`).
  - **Ouvrir un projet à un invité existant** depuis 🔗 Partages : `POST /api/guests/grant` (réutilise un partage projet aux mêmes droits sinon en crée un, pré-approuve l'invité, renvoie lien+PIN).
  - **Favicon** `static/favicon.svg` (whitelisté partage). **Dupliquer un mémo** `POST /api/memos/<id>/duplicate` (copie tout sauf images, `done` remis à 0, titre + « (copie) »). Supprimer/Dupliquer déplacés dans un **menu ⋯** en haut de la pop-in mémo.
  - **QR code** : `GET /api/qr?data=` → SVG (lib `qrcode` ajoutée à `requirements.txt`, pure Python). Bouton ▦ sur chaque lien/accès → pop-in QR (fond blanc) + récap cible/droits/PIN/personnes.
  - **Carte** : `boardMapPoints()` utilise désormais le même filtrage que la liste (recherche + filtre + projet) → plus de point « fantôme » d'un mémo masqué.
  - **Commentaires v2** : mentions `@`, **réponses imbriquées** (`memo_comments.parent_id`, un seul niveau, `_valid_parent_comment` aplatit les réponses de réponses), **priorité** (`memo_comments.priority` 0-3, pastille rouge/ambre/vert), **accusés de lecture** (table `comment_seen`, marqué auto à l'ouverture — owner « Fabien », invité = nom/email ; endpoints `POST /api/memos/<id>/comments/seen` et `POST /share/<t>/memo/<id>/seen` ; affichage « 👁 vu par … » en excluant soi-même).
- **Juin 2026 — thème clair/sombre, sidebar repliable, pop-ins partout, fix formulaire invité** (UI/UX uniquement — **aucun changement du format d'export**, toujours v15) :
  - **Thème clair / sombre** : palette claire sous `:root[data-theme="light"]` (surcharge des tokens + des surfaces à rgba sombre en dur : header / sidebar / panneau mémos / cards), bouton 🌙/☀️ dans le header (`#theme-btn`), persisté `localStorage` clé `theme`, script inline dans le `<head>` pour l'appliquer **avant le rendu** (anti-FOUC). Présent sur `index.html` ET `share.html`. Sombre par défaut.
  - **Sidebar repliable** (réalisé via le skill `emil-design-eng`, branche `ui-modernization-gsap`) : 👁 `#sidebar-eye-btn` masque toute la sidebar (classe `sb-hidden`, largeur→0, icône → `#ic-eye-off`) ; ☰ `#sidebar-rail-btn` la replie en rail d'icônes 64px (classe `sb-rail`, `overflow:hidden` pour éviter le débordement des pastilles). Indépendants, persistés `localStorage` `sbHidden`/`sbRail`, animations GSAP backIn/backOut (sur le `nav`, jamais sur un `<dialog>` — invariant 8).
  - **Suppression projet dans un menu ⋯** : comme les mémos, le bouton Supprimer de la pop-in projet passe dans un menu `card-menu` (`#create-menu-btn` / `#create-menu`) ; le pied de page ne garde qu'Annuler / Enregistrer.
  - **Plus aucune boîte native** : tous les `alert()` restants de `share.html` remplacés par la pop-in `notify(msg, {error})` (markup `#notify-dialog` + fonction, miroir d'`index.html`). Règle : ne jamais utiliser `alert`/`confirm`/`prompt` natifs, toujours `notify()` / `confirmPopin()`.
  - **Fix formulaire d'accès invité** (`share.html`, `renderGuestBanner()`) : le refresh auto (`setInterval(load, 15000)`) reconstruisait le formulaire à chaque tick et **effaçait la saisie** (prénom/e-mail/code). Garde : si l'invité est anonyme et que `#guest-form` existe déjà, on **ne reconstruit pas**. Mauvais PIN → erreur **inline** (`#guest-error`) au lieu d'un `alert()`, **sans vider** prénom/e-mail (seul le code est remis à zéro + refocus). Validation à la touche Entrée.

## Sauvegardes

Thread quotidien (`_backup_loop`, démarré à l'import) : export JSON + copie SQLite (`conn.backup()`) dans `data/backups/`, rotation `BACKUP_KEEP_DAYS` (7 j). Idempotent par fichier-du-jour (multi-workers OK). Ne jamais committer `data/`.

## Backlog

Voir [IDEAS.md](IDEAS.md). Le README documente le modèle de données et l'API complète.
